{
  "description": "hwpx 인도 전 검증 안전망. hwpx를 만들거나 고친 호출을 Bash와 kordoc MCP(generate_document·patch_document) 양쪽에서 감지한다. 정합 단계(postprocess_hwpx.py·md2hwpx.py)는 구조 검증(validate_hwpx.py structural)과 후처리 흔적(R043 필수 멤버)까지 확인해 실패 시 block하고, 생성 단계(MCP)는 zip·XML 무결성만 보고 남은 후처리·검증을 비차단 리마인더로 돌려준다. 검증 스크립트를 못 찾으면 아무 것도 하지 않는다. 초안 안전망: 20_draft.md를 쓰거나 고치면 결정론 린트·문체 감사를 돌려 위반이 있으면 block한다(파생 md는 대상 아님).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|mcp__.*__(generate_document|patch_document)",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/verify_hwpx_hook.py\""
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/lint_draft_hook.py\""
          }
        ]
      }
    ]
  }
}
