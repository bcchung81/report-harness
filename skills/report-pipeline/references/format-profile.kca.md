# KCA 표준보고서 양식 프로파일 (번들 시드)

> 출처: `form/250609_(표준보고서) KCA 문서 양식.hwp` (2026-07-22 kordoc 분석).
> **번들 사본**: `skills/report-pipeline/assets/250609_표준보고서_KCA_문서양식.hwp` (64KB 원본
> 그대로 — 배포 환경에서 form/ 부재 시 실측 재검증·template 참조용).
> 이 파일은 KCA 환경의 기본 format-profile 시드다. 다른 기관 양식이 제공되면 §6-0 추출 절차로
> `{state_dir}/format-profile.{기관}.md`를 생성해 이 시드 대신 사용한다.

## 1. 편집용지 (양식 명시값)

- 여백: 좌 20 · 우 20 · 위 10 · 아래 15 (mm)
- 머리말 15 · 꼬리말 10 (mm)
- 줄간격: 160%

## 2. 계층별 폰트·사이즈 (양식 명시값)

| 요소 | 폰트 | 크기 | 비고 |
|---|---|---|---|
| 문서 제목 | HY헤드라인M | 24pt | 표지/첫 페이지 상단 — '26.9.24 사용자 확정으로 양식 명시값 20pt에서 올림, 한 줄에 안 들면 장평·자간을 조여 한 줄(postprocess `fit_title`) |
| 발신정보 | — | — | `< '25. 00. 00.(월), {본부} {팀} >` 형식, 제목 바로 아래 (팀명은 작성 부서 — 양식 예시는 경영기획팀) |
| □ (1단 제목) | HY헤드라인M | 15pt | **항상 볼드** (R024, '26.7.24 사용자 확정 — postprocess 부여) |
| ㅇ (2단 요지) | 휴먼명조 | 15pt | |
| - (3단 상세) | 휴먼명조 | 15pt | |
| ＊ (참고·주석) | 맑은고딕 | 13pt | |
| ☞ (결론 유도) | 휴먼명조 | 15pt | ＊·※와 동일 띄어쓰기·내어쓰기 (R025, '26.7.24 사용자 확정) |
| 표 제목 | — | **12pt** | 양식 문서는 `< 표 제목 >` 예시. **실무 코퍼스는 `[ 대괄호 ]`(개념도·집계)와 `< >`(참고·근거) 병존** — style-guide §9 우선. 크기는 12pt(R023, '26.7.24 사용자 확정 — 폰트 유지·높이만 치환). **캡션은 별도 문단이 아니라 표 `hp:caption`(side=TOP) 내장 + CENTER·볼드** (R034, '26.7.28 사용자 확정 — 260331 실무본 실측 원형) |
| 표 본문 | 맑은고딕 | 12pt | 셀 전체 12pt 강제 (R023) |
| 참고N 블록 제목 | HY헤드라인M | 16pt | `참고1` 라벨 셀 + 제목 셀의 표 형태 헤더. **붙임 배너도 이 양식 준용 — HY헤드라인M 16pt·별도 페이지 시작(pageBreakBefore)** (R027, '26.7.24 사용자 확정). OLE 실측 셀 스타일: 라벨 4변 SOLID 0.5mm #1B1760·채움 #2B2D63(남색)·흰 글자 / 스페이서 좌변만 SOLID / 제목 셀 상·하변 SOLID / 행 높이 28.3pt·셀 폭 21.1mm/2.0mm/잔여 ('26.7.24 재실측 — COLORREF 0x00BBGGRR 바이트 순서 반영, 최초 기록 #60171B/#632D2B는 R/B 반전 오류) |

> [!note] 실무본 260331 charPr·fontface 실측 재확인 ('26.7.28)
> R008 폰트 체계는 실무본(260331_AI OCR 결과 보고.hwpx) 실측과 **전부 일치** — □ HY헤드라인M
> 15pt / ㅇ·대시 휴먼명조 15pt / 표 셀 맑은 고딕 / 발신 줄 12pt(R018) / 본문 괄호 13pt(R033) /
> 캡션 12pt 볼드(R023·R034). ※·＊ 문단은 실무본에 부재해 실측 불가(양식값 맑은고딕 13pt 유지).
> 미반영 관찰 3건(기존 확정 규칙과 충돌해 단일 실무본 관찰만으로 뒤집지 않음): ① 제목 박스
> 18pt(하네스 20pt=양식 명시값 — 실무본은 긴 제목 축소 가능성, 제목 안 괄호 `(자체LLM)`은
> 15pt로 축소) ② 표 셀 11.5pt 지배·9.5~12pt 혼재(R023 12pt 강제는 사용자 확정 유지)
> ③ □ 절 제목 charPr 비볼드(HY헤드라인M 자체가 두꺼움 — R024 볼드 강제는 사용자 확정 유지).

## 3. 계층 기호 체계

```
□ 1단 — 절 제목 (HY헤드라인M 15pt 볼드, R024)
 ㅇ 2단 — 요지 (휴먼명조 15pt)
   - 3단 — 상세 (휴먼명조 15pt)
＊ 참고·각주 (맑은고딕 13pt)
※ 참조 표기 (본문 내 안내)
☞ 결론 유도 (휴먼명조 15pt — ＊·※와 동일 선두 3칸·내어쓰기 45pt, R025)
```

## 4. 문서 골격

1. 표지(제목 표 + 연월 `2025. 6.` + 기관 로고) — 표지 있는 보고서의 경우
2. 본문 제목 (HY헤드라인M 24pt)
3. 발신정보 `< '연. 월. 일.(요일), 본부 팀 >`
4. □/ㅇ/- 개조식 본문 (+ 표·도식)
5. 참고N 블록 (HY헤드라인M 16pt 헤더 표) — 필요 시
6. 붙임 목록

## 5. 서술량·줄바꿈 (실보고서 코퍼스 분석 반영)

- ㅇ·- 항목의 서술량: **최대 2줄** (사용자 명시 규칙). 2줄 초과 시 하위 계층으로 분리하거나 문장을 나눈다.
- 상세 수치·빈도 관례는 `style-guide.md`의 코퍼스 분석 결과를 따른다.

## 6. kordoc generate_document 파라미터 매핑 (export 필수 적용 — rules R008)

| 양식 요소 | 양식 값 | kordoc 파라미터 |
|---|---|---|
| 제목 (박스) | HY헤드라인M 24pt | `body_title_box=True` + `sizes.bodyTitle=24` (+ fonts.heading) |
| □ 절 제목 | HY헤드라인M 15pt | `fonts.heading="HY헤드라인M"` + `sizes.dae=15` |
| ㅇ·- 본문 | 휴먼명조 15pt | `fonts.body="휴먼명조"` + `body_pt=15` |
| ㅇ 부호 | ㅇ(이응) | `bullet2="ㅇ"` (실 enum 값 U+3147) |
| ※·＊ 참고 | 맑은고딕 13pt | `fonts.ref="맑은고딕"` + `sizes.cham=13` |
| 표 본문 | 맑은 고딕 12pt | `fonts.table="맑은 고딕"` + `sizes.table=12` |
| 줄간격 | 160% | `line_spacing=160` |
| 붙임 배너 | 3열 배너 표 | md 단계부터 `| 붙임 1 | | 제목 |` 표로 작성·유지 (R009) |

## 7. 계층 간격 (원본 바이너리 실측)

> 출처: `form/250609_(표준보고서) KCA 문서 양식.hwp` OLE 스트림 직접 파싱(HWPTAG_PARA_SHAPE 등),
> 2026-07-22 확정. 상세 원자료: claudian vault
> `reports/20260722/1313_하네스-AI성과-관리체계/research/fetched/양식-문단간격/추출결과.md`.

**핵심 발견**: 이 양식은 문단모양(PARA_SHAPE)의 "문단간격 위/아래" 필드를 계층 간격 구현에
쓰지 않는다 — □/ㅇ/-/＊ 등 실제 텍스트를 담은 모든 문단모양은 위/아래 간격이 **전부 0**이다.
대신 **글자크기만 줄인 빈 줄(스페이서 문단)을 계층 사이에 하나씩 삽입**해 시각적 간격을
만드는 방식이다. 즉 "간격 pt"의 정답은 paraPr 여백이 아니라 스페이서 문단의 글자크기다.

| 경계(위 요소 → 아래 요소) | 확정 간격 | HWPUNIT(charPr height) |
|---|---|---|
| 발신 줄 → □ (제목표 직후 첫 헤딩) | 12pt | 1200 |
| □ → ㅇ | 6pt | 600 |
| ㅇ → - | 3pt | 300 |
| - → ＊ | 3pt | 300 |
| ＊ → 다음 □ (두 번째 이후 □ 상단, 블록 구분) | 8pt | 800 |
| ＊ → 표(캡션 내장 후 — 구 '＊ → 표 캡션') | 10pt | 1000 |
| ㅇ/대시/※ → 표(캡션 내장 후 — 구 'X → 캡션' 승계) | 6pt | 600 |
| ※·＊ → ㅇ (단서·각주 뒤 요지 복귀) | 6pt | 600 |

### 계층별 들여쓰기·정렬 (R061 — 렌더 실측 '26.8.7)

| 계층 | 리터럴 공백 | 내어쓰기(hang) | 크기 | 정렬 |
|---|---|---|---|---|
| □ | 0칸 | 22.5pt | 15pt | JUSTIFY |
| ㅇ | 1칸 | 30pt | 15pt | JUSTIFY |
| 대시 | 3칸 | 37.5pt | 15pt | JUSTIFY |
| ※·＊ | 3칸 | 39pt | 13pt | JUSTIFY |

＊·※는 '26.9.24 사용자 정정으로 선두 5칸(52pt) → 3칸(39pt) — 표 아래 ※가 표와 어울리게(R019). 내어쓰기는 `left=0·intent=-hang` 인코딩으로 넣는다 — 첫 줄은 리터럴 공백이 위치를 잡고 둘째 줄이 hang 위치(첫 줄 본문 시작)에 정렬된다. 네 계층 모두 SVG 렌더에서 정렬 오차 2pt 이내로 실측 확인.

- **※·＊ → ㅇ 6pt는 실측이 아니라 정합 추론값**(R038, '26.7.28 사용자 확정): 표준양식은 ※
  기호 자체가 부재(코드포인트 전수 스캔 확정)하고 실무본 260331에도 ※·＊ 문단이 없어 이
  전환의 실물이 없다 — R013 체계의 X→ㅇ 전환(□→ㅇ·ㅇ→ㅇ·대시→ㅇ)이 전부 6pt인 정합값을
  적용했다. 미정의 상태에서는 간격 0으로 붙어 보이는 결함이 있었다(20260728건).

- "8pt"는 **모든 □ 앞이 아니라 발신 줄 직후 첫 □ 한정**이다. 이후 반복되는 □(새 블록 시작)는
  8pt가 아니라 15pt 일반 빈 줄(블록 구분)이다.
- 발신 줄 정렬은 **오른쪽(우측)** 확정(paraShapeID=17, 속성1 정렬비트=2) — R012(`<right>` 래핑).
- 정렬·글자 처리 확정('26.7.28 사용자 확정): 본문 계층 문단(□·ㅇ·대시) **양쪽 정렬 JUSTIFY**(R032) /
  본문 서술 중 `(…)` 괄호 구간 **13pt**(ㅇ 선두 괄호 리드 제외, R033 — run 경계를 넘는 구간도
  문단 전체 텍스트 기준 분할 처리, R039) / 서술 중 용어 뒤 ＊ 표지
  **위첨자 `<hh:supscript/>`**(선두 ＊ 각주 문단은 평문, R031) / 본문 콘텐츠 표 **우측 정렬**·붙임
  배너 **좌측**·제목 박스 CENTER(R015 정정·R035) / 붙임·참고 배너 **제목 셀 문단 양쪽 정렬
  JUSTIFY**(라벨·스페이서 셀은 CENTER 유지 — 배너 표 배치 LEFT(R035)와는 별개 층위, R037) /
  **강조 2계층**: 강조=볼드, 특히 강조=`==…==` → 노란 음영 charPr `shadeColor="#FFFF00"`+볼드
  (260331 실무본 실측 인코딩, R040).
- `skills/report-pipeline/scripts/postprocess_hwpx.py --spacing`이 이 표의 확정값으로 kordoc
  `generate_document` 산출물(스페이서 문단이 없는 상태)에 스페이서 문단을 삽입해 재현한다(R013).
- ☞ 결론 유도 기호의 인접 간격은 ※·＊ 준용 3pt(R025). 제목 박스 상단여백은 postprocess가
  앵커 문단 줄간격 100% 치환 + 표 outMargin top 0으로 제거한다(R022 개정 '26.7.24) —
  **상단 얇은 행(3.8pt)은 양식 원형(1열×3행)의 그라데이션 배경 밴드이므로 삭제 금지**
  (양식 OLE 실측: 0행 3.8pt 그라데이션 채움·1행 제목·2행 3.8pt 4변 실선 밑줄).
  **후처리 `apply_title_box_form`이 복원하는 확정값은 양식 OLE가 아니라 파이프라인 인도본
  계보다**(R084) — 0행 `winBrush #0080C0`·1행 제목(28.5pt, 채움 없음)·2행 `gradation RADIAL
  #0080C0→#3CBFFF`, 밴드 3행 모두 4변 `NONE`. 인도본 실측 2건(20260815 협의체건·20260831
  microduck건)과 바이트 일치하며, 이 값이 코드 상수 `TITLE_BOX_TOP_FILL`·
  `TITLE_BOX_BOTTOM_FILL`·`TITLE_BOX_BAND_HEIGHT`·`TITLE_BOX_TITLE_HEIGHT`와 대조된다
  (`test_title_box_form_matches_profile`). 양식 OLE 원본과의 색·방향 차이(원본은 상·하 모두
  LINEAR 90° `#3057B9↔#DFE6F7`)는 R022에 기록된 **기지의 미고침 사항**이지 드리프트가 아니다.
- **표 총 폭 상한**: 모든 표(머리말 배너·제목 박스 포함)의 총 폭(sz + outMargin 좌우)은
  본문 폭(좌우 20mm 문서 170mm = 48190 hu) **미만** — 본문 폭 − 283 hu(1.0mm)를 상한으로
  초과분을 비례 축소한다(R036·R042 정정, '26.7.28). 본문 폭과 **정확히 같으면**(slack 0)
  같은 문단에 선행 요소가 있을 때 한컴이 표를 다음 줄로 내려 표 위에 15pt 빈 줄이 생긴다 —
  제목표 상단 여백 6차 조사의 확정 원인(실기동 30.29 → 25.02mm). 실보고서 12건의
  '배너 폭 == 본문 폭' 관례에서 1.0mm 이탈은 실기동 무감지.


## 8. 도식 배색 (R090 — 표 제외, 이미지·표 도식 공통)

도식 색은 **역할**로 정하고 명세에는 색 값이 아니라 역할(`"tone": "accent"`, 일정 `"status"`)만 쓴다 — LLM이 색을
고르지 않는다. 기준은 범정부 디자인시스템(KRDS) 색 원칙: 글자 명도대비 **4.5:1**·그래픽 **3:1** 이상, 강조색은
**5% 이하**(도식당 1곳), **색만으로 정보를 전하지 않는다**(점선·굵기·라벨을 함께). 남색·청색은 머리말 배너·제목 박스와
같은 기관 색이라 유지한다. 코드 상수(`render_diagram.py`)와 이 표를 `test_value_drift.py`가 대조하고, 대비·흑백 휘도
차는 같은 테스트가 계산으로 검사한다.

| 역할 | 상수 | 값 | 쓰는 곳 | 대비·흑백 |
|---|---|---|---|---|
| 구조 | `NAVY` | #1B1760 | 머리 글자·연결선·흐름 ▶ | 흰 바탕 15.7:1 |
| 보조(선) | `BLUE` | #0080C0 | 화살표·선 — **글자를 얹지 않는다** | 흰 바탕 4.3:1(그래픽 3:1 충족) |
| 보조(채움) | `BLUE_FILL` | #006BA6 | 흰 글자를 얹는 청색 — 비교 화살표 몸통·일정 막대·PDCA 끝 칸 | 흰 글자 5.75:1 |
| 테두리 | `LINE` | #7F8FA6 | 카드 테두리 | 흰 바탕 3.3:1 |
| 머리(기본) | `HEAD` | #DCE6F1 | 카드 머리 | 남색 글자 12.4:1 |
| 머리(개선·결과) | `HEAD_STRONG` | #BDD7EE | 비교도 개선(오른쪽) 머리·결과 칸·상위 층 | 남색 글자 10.6:1 |
| 이전(비교 왼쪽) | `HEAD_OLD` | #F2F2F2 | 비교도 종전 머리 + **점선 테두리** | 개선 머리와 휘도 차 0.23(종전 #E7E6E6은 0.01로 흑백에서 같았다) |
| 이전 글자 | `OLD_TEXT` | #404040 | 종전 머리 글자 | 10.4:1 |
| 강조(채움) | `ACCENT` | #B34700 | `"tone": "accent"` 카드 머리(흰 글자) — 도식당 1곳 | 흰 글자 5.5:1 |
| 강조(선) | `ACCENT_LINE` | #D55E00 | 강조 카드 굵은 테두리(두 번째 채널) | 흰 바탕 3.9:1 |
| 강조(옅은 바탕) | `ACCENT_SOFT` | #FBE5D6 | 차트 강조 범주 바탕 등 | 남색 글자 12.9:1 |
| 상태 완료 | `STATUS.done` | #1B1760 | 일정 막대 — 흰 글자 + 라벨 | 휘도 0.02 |
| 상태 진행 | `STATUS.doing` | #006BA6 | 일정 막대 — 흰 글자 + 라벨 | 휘도 0.13 |
| 상태 예정 | `STATUS.plan` | #BDD7EE | 일정 막대 — 남색 글자 + 라벨 | 휘도 0.66 |

- **상태를 초록·청색·회색으로 두지 않는 이유**: 초록 #228738과 청색 #0080C0은 휘도가 0.18·0.19로 흑백 인쇄에서
  구분되지 않는다 — 같은 청색 계열의 **명도 3단계**로 두고 막대 안에 `완료·진행·예정` 라벨을 쓴다.
- **강조는 주황 한 곳**: 청색의 보색이라 색각이상(적록)에서도 청색과 갈린다. 다만 흑백에선 청색 막대와 휘도가
  비슷하므로 굵은 테두리를 함께 쓴다. 강조가 2곳 이상이면 `render_diagram.check_palette_use`가 거부한다.
- **차트 계열 순서**(`CHART_SERIES`): #006BA6 → #1B1760 → #7F8FA6 → #BDD7EE — 이웃 계열의 명도가 번갈아 흑백에서도
  갈린다. 한 범주를 강조할 때만 `ACCENT`.

### 8-1. 도식 카드 본문 (R093 — 표 도식·리뷰 공통)

카드 본문 항목은 `• ` 부호 + 서술형 문장이다. 두 줄 이상으로 넘어가면 둘째 줄이 부호 아래가 아니라 **첫 줄 글자와
같은 자리**에서 시작한다('26.9.25 게이트② f12). 한글 표는 본문 계층(§7 R061)과 같은 인코딩 — `left=0·intent=-hang` +
부호 뒤 탭 + 내어쓰기 자동 탭(`autoTabLeft=1`) — 이라 부호 글자폭과 관계없이 줄 앞이 맞는다. 리뷰 화면은 부호를
내어쓰기 폭의 고정 칸에 넣는다. 코드 상수(`diagram_table.py`)와 이 표를 `test_value_drift.py`가 대조한다.

| 항목 | 상수 | 값(HWPUNIT) | 비고 |
|---|---|---|---|
| 카드 항목 내어쓰기 | `CARD_HANG` | 1500 | 본문 10pt 기준 부호 1글자 + 반 칸(15pt) — 줄 수 추정도 모든 줄에서 이만큼 좁게 센다 |
