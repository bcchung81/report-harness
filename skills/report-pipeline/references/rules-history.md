# rules-history — 규칙 확정 경위·실측 로그 (배포 시드)

> rules-seed.md와 함께 배포되는 경위 로그의 시드다. 결번(폐지·이관) 기록이 여기 남아
> `consolidate_rules.py`·테스트가 죽은 참조와 결번을 판정한다. 운영 축적분과 함께 동기화한다.

> `rules.md`는 **따를 값**만 담는다. 그 값이 왜 그 값인지의 조사 경위·실기동 계측·폐기된 가설은
> 여기에 남긴다. 규칙 본문이 1,000자를 넘으면 이 파일로 내리고 규칙에는 `경위 → rules-history.md#R0NN`만 건다.

## R042

- R042 [export] **표 총 폭(sz + outMargin 좌우)은 본문 폭 '미만' — 여유 283 hu(1.0mm) 확보. R036('이내')의 계열 정정(R036 폐기 아님 — 폭 상한 원칙·비례 축소 메커니즘은 유지, 상한값만 정정)**: R036이 초과 표를 본문 폭과 **정확히 같게**(slack 0) 축소한 결과, 같은 문단에 표보다 앞선 요소가 있으면 한컴 엔진이 표를 다음 줄로 내려 표 위에 **15pt 빈 줄**(charPr#0 height 1500 = 5.29mm)이 생겼다 — 사용자가 6회 보고한 제목표 상단 여백의 정체는 이 줄바꿈 빈 줄이다(넘쳐서가 아니라 **딱 맞아서** 생긴 문제). 실기동 A/B 실측: slack 0 = 제목표 상단 30.29mm / 제목표 폭만 축소 = 25.03mm(여백 소멸) / 본 수정판 재계측 25.02mm·머리말 배너 좌석 불변(로고 상단 12.28→12.23mm, 한 줄 렌더 유지). 처리: `apply_fit_page_width`가 총 폭 > 본문 폭 − FIT_PAGE_SLACK(283 hu — 제목표·배너 자산의 outMargin 퀀텀과 동일한 문서 그리드 최소 단위, 시각적으로 무감지)인 표를 본문 폭 − 283으로 비례 축소한다(셀 폭 합 == 표 sz 정확 일치 유지·내부 그림 동반 축소 — R036 메커니즘 그대로). slack 0이던 머리말 배너에도 동일 적용(당장은 선행 요소가 없어 안 꺾이지만 취약 상태 해소 — 'KCA 실보고서 12건 배너 폭 == 본문 폭' 관례에서 1.0mm 이탈은 실기동 무감지 확인), slack이 이미 283 이상인 본문 표(1801)는 비대상·멱등. **위생 동반 수정**: 그림 축소 시 sz·curSz만 줄이면 파생 캐시가 도너 원값으로 스테일된다(6차 실측: kcaHdrLogo scaMatrix 0.238583 = 도너 14315×1905 잔존) — scaMatrix e1/e5 = curSz/orgSz, rotationInfo centerX/Y = curSz/2로 재계산한다(transMatrix·scaMatrix e3/e6은 offset 파생이라 불변, 실기동 렌더 정상 확인). R022(앵커 기하)·R030(머리말 부재)·R041(앵커 150%)은 전부 실재한 별개 결함이었으나 6차 확정된 이 원인(등호 slack 0)과는 다른 축이다. 이 결함은 정적 XML로 판정 불가 — 실기동 픽셀 계측이 결정타(5·6차 동일, kordoc reflow는 머리말 미렌더로 부적합). 단위 주의: 1 hu = 1/7200 inch → 283 hu ≈ 1.0mm(6차 제시 '283 hu = 0.1mm'는 환산 오기, 15pt = 1500 = 5.29mm와 교차 검증) (근거: '26.7.28 사용자 확정 — 6차 실기동 A/B + 수정판 실기동 재계측 30.29→25.02mm)

### '26.9.25 규칙 문구 줄이기 전 원문

- R042 [export] **[R036 상한값 정정]** 표 총 폭(sz + outMargin 좌우)은 본문 폭 **미만** — 여유 283 hu(1.0mm)를 남긴다. 본문 폭과 정확히 같으면(slack 0) 같은 문단에 선행 요소가 있을 때 한컴이 표를 다음 줄로 내려 표 위에 15pt 빈 줄이 생긴다. `apply_fit_page_width`가 초과 표를 본문 폭 − 283으로 비례 축소하고, 내부 그림의 scaMatrix·rotationInfo 파생 캐시도 함께 재계산한다. **정적 XML로는 판정 불가 — 실기동 픽셀 계측이 결정타** (근거[실측]: '26.7.28 사용자 확정 6차 실기동 A/B 30.29→25.02mm · 경위 → `rules-history.md#R042`)

## R043

- R043 [export] **hwpx는 한컴 정본 패키지 프로파일로 정합해 인도한다 — kordoc 최소 패키지는 내부망 자료교환 반입에서 hwpx로 판별되지 않는다(octet-stream → 미등록 확장자 반려)**: kordoc 산출물은 mimetype·container.xml·content.hpf·header·section·PrvText만 있는 최소 OCF라서 hwpx 확정 마커인 version.xml이 없고, 디렉터리 엔트리 3개(META-INF/·Contents/·Preview/)·전량 STORED라는 정품에 없는 지문을 가진다 — mimetype+container.xml 구조는 EPUB류 일반 OCF와 지문이 같아 심층 구조 검사 엔진이 판별에 실패한다(타 hwpx는 동일 시스템 정상 유통 — 시스템 미등록이 아니라 산출물 문제, 사용자 확정). postprocess `canonicalize_package`가 플래그 무관 상시 적용: version.xml·settings.xml(content.hpf manifest 등재)·META-INF/manifest.xml·container.rdf 보강(한컴 정품 도식Pool.hwpx 실측 정본 템플릿), 디렉터리 엔트리 제거, container.xml rootfiles 정본화(PrvText·container.rdf), 엔트리 순서·압축 프로파일(mimetype·version.xml·미디어 STORED, XML DEFLATED) 정품 일치, 멱등. `validate_hwpx.py structural`이 OCF 시그니처(첫 엔트리 mimetype STORED·offset 0·extra 0·38바이트째 평문 application/hwp+zip)와 필수 멤버(version.xml·container.xml·content.hpf·header·section)·디렉터리 엔트리 부재를 검증해 회귀 차단. **'26.7.29 보강(실반려 파일 실측)**: 한글에서 재저장한 본은 정본 패키지가 되는 대신 기본 빈 JScript 스텁 Scripts/headerScripts·sourceScripts(확장자 없는 멤버 + hpf에 application/x-javascript로 등재되는 활성콘텐츠)가 삽입된다 — 압축 내부까지 검사하는 반입 엔진이 이것을 '등록되지 않은 확장자'로 반려하며, 실반려 오류 문구의 'header script'가 이 멤버명이다. canonicalize_package가 Scripts/ 멤버 전량 제거 + hpf item/itemref 등재 철회를 함께 수행한다(스텁은 기능 0, 기관보고서 인도본에 매크로 불필요) (근거: '26.7.29 내부망 자료교환 반입 거부 — 한컴 정품 대조 + 실반려 파일 실측)

## R048

- **push → pull 전환 ('26.8.15)**. 종전 R048은 "사용자가 명시 요청·승인했을 때만 하네스가
  vault로 복사"하는 승인 게이트형 push였다. 실측 결과 **적재율 0%** — `reports_dir` 7건 중
  6건이 조사를 수행해 `research/fetched/` **32폴더·raw 99건**(모드 Q 산출 22건 제외 시 순 77건)·
  `_manifest.jsonl` 31개 124행·`provided/` 10파일을 쌓았으나 claudian `acquired/`에 승격된 것은
  **0건**이었다.
- 실패 원인 3가지. ① **트리거 부재** — 승격이 사용자 요청 조건인데 종료 훅은 "적재 후보 N건"
  한 줄 보고가 전부고, pipeline은 "게이트 최대 3회"가 설계 원칙이라 적재를 4번째 게이트로
  넣을 수도 없다. 보고서가 나오면 세션이 끝나고 아무도 요청하지 않는다. ② **네임스페이스 충돌**
  — 슬러그가 건 로컬인데 착지는 flat `acquired/fetched/{슬러그}/`다. 실측에서 `코드베이스분석`
  (2건)·`vault-사전지식`(2건)이 겹쳐, 규약대로 복사하면 claudian §2.1 파괴 금지를 위반한다.
  ③ **스키마 불일치** — 프론트매터 `tool`↔`fetched_by`, manifest `source_url`↔`url`,
  `status`·`license_note` 부재, `confidence` 미수용, 날짜 정밀도 차이. 변환 없이 복사하면
  §4.8 위반 파일 99개가 생긴다.
- 전환 결정: 세 문제 모두 **수확하는 쪽에서만 풀 수 있다**(vault가 자기 네임스페이스·스키마·
  장부를 안다). 하네스는 vault에 대해 완전 무쓰기가 되고, claudian `/ingest` 모드 H가
  작업폴더를 읽어 pull한다. 멱등은 `research/_promoted.json` 마커로 잡는다 — 하네스 작업폴더에
  vault가 쓰는 유일한 파일이며, 그 외 작업폴더 파일은 건드리지 않는다.
- 부수 효과: `provided/` 제공자료가 처음으로 착지 규약을 갖는다(원본→`raw/`, 파싱본→
  `acquired/parsed/`). 종전 규약은 모드 R만 다뤄 제공자료가 vault로 갈 길이 없었고, 그래서
  사람이 손으로 `raw/`에 복사하던 것이 "raw만 적재되고 research는 누락"의 정체였다.

### '26.9.25 규칙 문구 줄이기 전 원문

- R048 [export] **산출물·조사자료는 `reports_dir`(하네스 report/)에 쌓는 것이 정본이고, vault(claudian) 적재는 하네스가 하지 않는다 — 수확은 vault가 pull한다**: 작업폴더 `{reports_dir}/{YYYYMMDD}/{HHMM}_{슬러그}/`가 **유일한 1차 저장소**이며 그 자체로 완결된다(vault 없이도 파이프라인 전 구간 동작). 처리 원칙 — ① export·research 종료 시 인도 보고에 **"vault 적재 후보 N건 — claudian `/ingest`로 수확"** 1줄만 알린다, ② **하네스는 vault의 어떤 파일도 쓰지 않는다** — `acquired/`·`raw/`·`wiki/log.md` 장부까지 예외 없다(사용자가 적재를 요청해도 하네스가 직접 복사하지 않고 vault 쪽 `/ingest`로 넘긴다), ③ 착지 판단(조사 raw→`acquired/fetched/{건}--{주제}`, 제공자료 원본→`raw/`, 파싱본→`acquired/parsed/`)·복합 슬러그 네임스페이싱·스키마 변환·멱등 마커(`research/_promoted.json`)는 전부 vault 계약이므로 claudian `CLAUDE.md` §5.2.1이 소유한다. 배경: vault raw에 자동 적재하면 원본 수집물과 파생 보고서가 뒤섞여 출처 추적이 깨진다. 여기에 '26.8.15 실측으로 **승인 게이트형 push의 실패**가 확인돼 pull로 전환했다(적재율 0% — 32폴더·raw 99건 전량 미승격) (근거[실측]: '26.8.15 사용자 확정 · 경위 → `rules-history.md#R048`)

## R050 [R062로 흡수·이관 — '26.8.7]

- R050 [draft] **[R062로 흡수·이관됨 — 본 규칙 폐지]** **계층 문구는 1줄로 끝내지 말고 2줄 밀도로 쓴다 (자수 기준은 R062가 정정 — 1줄 35자·상한 90자) — 렌더 기준 60~90자(휴먼명조 15pt, 1줄 = 35자 실측 — R062가 정정)**: R028이 "2줄 이내" 상한만 정한 탓에 40~60자짜리 한 줄 문구가 양산됐는데, 이는 보고서가 아니라 목차처럼 읽히고 근거·판단이 빠진 앙상한 서술이 된다. **상한(2줄)과 함께 하한(2줄에 가깝게)을 둔다** — 1줄로 끝나는 항목이 나오면 ① 인접 항목과 통합해 한 문장으로 합치거나(연결어 ~하며·~하고·~여서), ② 그 판단의 **근거·수치·조건·귀결**을 덧붙여 보강한다. 보강할 내용이 없으면 그 항목은 애초에 쓸 필요가 없는 항목이므로 삭제한다. **금지**: 자수를 채우려고 같은 말을 다르게 반복하거나 수식어를 늘리는 것 — 늘어난 분량은 반드시 새 정보(근거·수치·귀결)여야 한다. 표 셀·※ 단서·＊ 각주는 대상이 아니다(단서는 원래 짧다). 자가검산은 `**`·`==` 마커를 제거한 순수 글자수 기준 (근거[관례]: '26.8.3 사용자 확정, cert-poc PoC 결과보고건)

> 폐지 사유: 자수 기준(78~115자)이 실측과 어긋나 R062가 정정했고, 고유하게 남던 '2줄 밀도 하한' 취지는 R062 본문에 흡수했다. 피참조 0건이라 안전하게 제거.

## R053 [R026으로 흡수·이관 — '26.9.6]

- R053 [draft] **[R026으로 흡수·이관됨 — 본 규칙 폐지]** **본문은 자기완결 — 괄호로 붙임에 설명을 미루지 않는다**: `(상세 붙임1 참조)`처럼 본문 문장 안에서 설명을 유보하면 읽는 사람이 두 번 이동해야 하고 본문이 목차로 전락한다. 개선본은 이 표기를 6회→0회로 전량 제거했다. 붙임은 **검증하려는 사람만 펴보는 원자료**이지 본문의 각주가 아니다(R026 분담과 같은 층위 — 본문은 요지를 스스로 끝내고, 붙임은 그 요지를 뒷받침하는 전수 데이터를 따로 담는다). **예외**: 절 맺음의 `※ 세부내용은 붙임 참조`는 코퍼스 합법 관례이므로 유지한다(문장 안 유보가 아니라 절 종료 후 안내). 린트 `annex-crossref`가 괄호 유보형만 검출 (근거[관례]: 동일 대조 — cert-poc)

> 폐지 사유: R053은 R026(본문=요지·붙임=근거 분담)의 운영 세칙이고 본문 자체가 "R026 분담과 같은 층위"라고
> 적고 있었다. 두 규칙을 함께 읽어야 판단이 서던 구조라 R026 본문에 괄호 유보 금지·※ 예외·린트
> `annex-crossref`를 그대로 흡수했다. 피참조 0건이라 안전하게 제거하고, 외부 참조(md-profile 룰 표·
> lint 주석·테스트 주석)는 R026으로 갱신했다.

## R072

- 경위('26.8.10): 사용자가 vault 위키 페이지의 한글 변환을 요청했고, 그 시점의 활성 스킬은 `claudian-research`(vault QUERY)였다. 세션 초입에 `/report-export`를 호출하긴 했으나 인자가 "팩트체크만 재실행"이어서 팩트체크에서 멈췄고, **`hwpx-recipe.md`를 세션 내내 한 번도 읽지 않은 상태**였다. 그 결과 kordoc `generate_document`를 `preset="개조식"`+표지·목차 옵션만 주고 직접 호출했다.
- 이탈을 만든 판단 4겹: ① 스킬 컨텍스트가 중간에 갈아타 hwpx 지침이 로드되지 않음, ② **입력이 `20_draft.md`가 아니라는 이유로 "파이프라인 밖"으로 분류**(핵심 오판 — 입력 형태로 적용 여부를 가름), ③ `generate_document` 스키마 설명이 "정부 표준 보고서='개조식'"으로 프리셋을 직접 안내해 레시피보다 먼저 눈에 들어옴, ④ 왕복 `parse_document`가 11개 장·표 27개를 정상 재현해 "검증했다"는 오인을 줌.
- 실측 피해: `validate_hwpx.py structural` exit 1 — `missing required member version.xml` + 디렉터리 엔트리 3개(`META-INF/`·`Contents/`·`Preview/`). R043이 '26.7.29 내부망 자료교환 실반려 건으로 기록해 둔 조건을 그대로 재현한 산출물이었다. 서식도 R008 프로파일 파라미터를 하나도 전달하지 않아 kordoc 내장 범용 정부 서식이 적용됐고, 후처리 미실행으로 계층 스페이서·양쪽정렬·표 폭 정합·머리말 배너가 전부 빠졌다(줄간격 100% 문단 1개 vs 정식 산출물 4개).
- 조치: 작업폴더 `20260810/1835_reachy-mini-부품스펙`을 규약대로 신설하고 md-profile 초안을 작성해 전 구간 재실행(lint 0 → prep 0 → R008 전량 전달 생성 → `--all` 후처리 → structural 0). 후처리가 `version.xml`·`settings.xml`·`manifest.xml`·`container.rdf` 4종을 보강하고 디렉터리 엔트리 3개를 제거했다. compare 29건은 전량 볼드·h1 `markdown-leftover`로 R021 절차(XML 리터럴 0건 확인) 무해 판정.
- 부수 확인: `bullet2`에 스키마 설명대로 `ᄋ`(U+110B 초성)을 넣어 `invalid_enum_value` 실패 — 실제 enum은 `ㅇ`(U+3147)·`○` 2값이다. R008 괄호주석("U+110B 초성 아님")이 경고한 사례가 그대로 재현됐고 주석이 실제로 작동함을 확인해 규칙을 유지했다.

### '26.9.25 규칙 문구 줄이기 전 원문

- R072 [export] **hwpx를 만드는 모든 호출은 변환 레시피를 탄다 — 적용 여부는 '입력이 무엇인가'가 아니라 '출력이 hwpx인가'로 가르고, 산출 의도가 보이는 즉시 하네스 규격부터 로드한다**(R081 흡수 '26.9.24): 원문을 "파이프라인 밖 일회성 변환"으로 분류해 R008·후처리·검증 없이 뽑은 이탈('26.8.10)과, 하네스를 조회조차 않고 kordoc 기본 preset(개조식+표지·목차)으로 뽑아 반려된 일('26.9.10)이 같은 뿌리다. **처리** — ① 산출 의도가 확인되면 `harness_config.load_config()`로 경로를 해석하고 `hwpx-recipe.md`·`style-guide.md`·`md-profile.md`를 읽은 뒤 착수, ② **문서 종류와 무관하게 항상 타는 3단계** — R008 프로파일 파라미터 전량 전달 → `postprocess_hwpx.py --all` → `validate_hwpx.py structural`, 하나라도 빠진 산출물은 인도하지 않는다, ③ 해당 없는 규칙(R012·R009·도식 마커)만 비적용하되 **R043 패키지·R042 표 폭은 파일 유효성이라 예외 없음**, ④ 원문은 md-profile 초안을 먼저 만들어 태우고 산출은 `{work_dir}/final/`(R048 확장), ⑤ 왕복 `parse_document`는 내용 확인이지 구조 검증이 아니다. **[생성 경로 한정 → R073]** **자가점검**: 인도 직전 머리말 배너·붙임 배너·발신 줄 12pt가 있는가 — 셋 다 후처리 산출물이라 하나라도 없으면 파이프라인을 탄 적이 없다 (근거[실측]: '26.8.10 사용자 지적 · '26.9.10 kca-ax-invest 반려 · 경위 → `rules-history.md#R072`)

## R073 — 개정 건 재생성 금지, 원본 제자리 개정 ('26.8.18)

- 발단: 사용자 보유 hwpx(v2, 한글에서 손수 조정)를 기준선으로 v3 개정본을 만들면서, 개정 내용을
  md 초안으로 다시 쓰고 `generate_document`로 뽑았다. 하네스 검증은 전부 통과했다 — lint 0,
  회귀 227, `structural` exit 0, `compare`는 R021 무해 판정, 후처리 전 항목 적용. 그런데
  사용자 반려: **"기존에 맞춰놓은 양식들이 다 깨져서 나온다."**
- 원인: `generate_document`는 **문서를 새로 짓는** 도구다. 원본을 참조하지 않으니 사람이 맞춰 둔
  서식을 승계할 방법이 애초에 없다. `extract_profile`+`profile_path`도 표 서식 일부만 재현한다.
- 붕괴폭 실측 — `Contents/section0.xml` 서식 참조 종류:

  | 지표 | v2 원본 | 재생성본 | 제자리 개정본 |
  | --- | --- | --- | --- |
  | borderFill 참조 종류 | 33 | 25 | 33 |
  | charPr 참조 종류 | 73 | 30 | 73 |
  | paraPr 참조 종류 | 45 | 16 | 45 |
  | 병합 표(rowspan/colspan) | 보존 | 평탄화 | 보존 |
  | 셀 내 줄바꿈 | 보존 | 소실 | 보존 |

- **검증 체계의 사각**: 표 개수는 23으로 동일해 왕복 `compare`가 통과했다. 되읽기는 텍스트·표
  개수·수치만 보므로 **서식 붕괴를 원리적으로 검출하지 못한다**. 그래서 규칙은 "검증을 더 하라"가
  아니라 **"그 경로를 쓰지 마라"**로 세웠다.
- 전환한 경로와 실적: 원본을 복사해 `section0.xml`만 고쳤다 — 텍스트 치환 51건(전체일치 40 +
  run 내부 부분치환 11), 문단 삽입 11건(같은 계층 문단 복제로 서식 상속), 표 행 분리 1건
  (`hp:tr` 복제 + `rowAddr` 2~5 재배정 + `rowCnt` 5→6). 미적용 0건.
  `header.xml`·`settings.xml`·`BinData/*`·`content.hpf`·`manifest.xml` MD5 바이트 동일.
- **패키지 프로파일 정정(R043 관계)**: 1차 재압축에서 멤버를 일괄 DEFLATED로 넣었더니 원본이
  STORED로 저장한 `version.xml`·`BinData/image1.PNG`·`Preview/PrvImage.png`가 바뀌었다.
  R043의 정품 프로파일(BinData=STORED)을 강제하는 것도 답이 아니다 — **한글이 실제로 저장한
  원본이 `BinData/image2.BMP`를 DEFLATED로 넣었기 때문**이다. 즉 개정 경로에서 정답은 규칙
  준수가 아니라 **원본 `ZipInfo` 전량 계승**(압축 방식·타임스탬프·외부속성·멤버 순서)이다.
  원본이 자료교환 반입을 통과하는 파일이면 산출본도 같은 판별을 받는다.
- 자료교환 반입 감사 결과(양쪽 동일): `Scripts/` 활성콘텐츠 0 · 디렉터리 엔트리 0 ·
  확장자 없는 멤버는 `mimetype` 1건뿐 · mimetype STORED 선두 · OLE 0 · DRM 없음 · 매크로 0 ·
  외부 URL 0(검출된 14건은 전부 hancom.co.kr 네임스페이스 선언) · `content.hpf` 작성자 메타는
  전부 `'text'` 자리표시자라 개인정보 없음.
- 남은 한계: `Preview/PrvText.txt`·`PrvImage.png`는 v2 시점 캐시라 탐색기 미리보기에는 개정 전
  내용이 보인다(한글에서 한 번 저장하면 갱신). 신설 문단은 단일 run 복제라 괄호 축소(13pt)
  서식을 상속하지 않는다.

### '26.9.25 규칙 문구 줄이기 전 원문

- R073 [export] **기존 hwpx가 있는 개정 건은 재생성하지 않고 원본을 제자리로 고친다 — `generate_document`는 문서를 새로 짓는 도구라 사람이 맞춰 둔 서식을 승계하지 못한다**: 사용자 조정본을 다시 뽑았더니 `section0.xml` 서식 참조 종류가 borderFill 33→25·charPr 73→30·paraPr 45→16으로 주저앉고 병합 표가 평탄화, 셀 내 줄바꿈이 소실됐다. 표 개수는 같아 **왕복 compare가 통과해도 서식은 이미 죽어 있다**. 판정 기준은 입력이 md냐가 아니라 **개정 대상 hwpx 원본이 있느냐**다. **제자리 개정 4원칙** — ① 텍스트는 `hp:t`만 교체(paraPr·charPr 불변), ② 신설 문단은 같은 계층 기존 문단을 복제해 서식을 상속하고 첫 `hp:run`만 남긴 뒤 `linesegarray`(레이아웃 캐시)는 제거, ③ 표 행 추가는 `hp:tr` 복제 + 후속 `rowAddr` 재배정 + `hp:tbl/@rowCnt` 증가, ④ **`postprocess_hwpx.py`를 돌리지 않는다** — 원본이 이미 양식 정합이라 사용자 조정을 덮어쓴다(R072 3단계는 생성 경로 한정). **재압축은 원본 `ZipInfo` 전량 계승**(압축 방식·타임스탬프·외부속성·멤버 순서) — R043 정품 프로파일을 새로 씌우면 오히려 원본과 달라져 자료교환 반입 판별이 갈린다. **검증**: header.xml·settings.xml·BinData·content.hpf MD5 바이트 동일 + 서식 참조 종류 원본 일치 + `structural` exit 0 (근거[실측]: '26.8.18 1814_AI성과측정-방안개선 v3 — 경위는 rules-history.md)

## R087 경위 — 작업폴더 파생물이 낡는 구조 ('26.9.10)

- **측정**: 인도 10건 전수에서 `20_draft ↔ 40_prepared` 대조 결과 **4건이 낡음**(2242 201조각·
  0923 122조각·1634 125조각·1912 8조각). 최장 간격 12일. 낡은 쪽은 언제나 파생물이었고 초안이
  앞서 있었다 — 사람이 고치는 것은 초안 하나뿐인데 파생 4종이 옆에 나란히 있는 구조가 원인이다.
- **변환 자체는 무손실**이었다. `43_convert_input → hwpx → 되읽기`를 문장 단위로 대조하니 4건
  모두 소실 0(3기관 618조각·1814건 1,767조각 포함). 처음 손실로 보인 47조각은 되읽기가 붙이는
  `\~` 이스케이프였다. 즉 증상("초안대로 안 담긴다")의 실체는 변환 손실이 아니라 판 어긋남이다.
- **파생물이 정말 파생인지 확인**: `40_prepared → 43_convert_input`을 30줄 규칙(개조식 기호 →
  마크다운 위계·제목 h1·발신줄 래핑)으로 재현하니 10건 중 9건 100%, 1건 99.8% 일치했다. 남은
  0.2%는 도식 마커 2줄로 Pool 조회가 필요한 판단 단계였다. 도식 매핑을 인자로 주면 100%가 된다.
- **compare 기준 전환 근거**: 정합 6건에서 `40_prepared` 기준과 `20_draft` 기준의 검출 결과가
  완전히 같았다(73·0·2·1·1·0). prepared는 초안의 결정론 파생이므로 기준을 초안으로 올려도
  판정력이 줄지 않는다.
- **research 구조**: 폴더로 조사 단위를 표현한 결과 한 건에 하위 폴더 69개·`_manifest.jsonl`
  63개가 흩어졌다. 평탄화 이관 326건 수행, 이미지 상대 참조는 이동과 함께 재작성했다.
- 이 항목은 '26.8.12 판본 보존 설계(R086)의 후속이다. 그 설계의 `revise`(사후 아카이브)는
  `begin`(판본 폴더 선할당)으로 대체됐다 — 사후에 내리면 그 사이가 비어 낡을 틈이 생긴다.

### '26.9.25 규칙 문구 줄이기 전 원문

- R087 [export] **작업폴더 루트에는 사람이 고치는 파일만 둔다 — 판본·파생물은 `history/`에, 인도본은 `final/`에 `r{NN}_{YYYYMMDD}_` 접두어로 쌓는다**(R086 흡수 '26.9.24): 초안 판본이 세 갈래 표기(`_v6`·`.교차검증전`·`.이전판`)로 현행본 옆에 쌓이고 `final/`에 hwpx가 여럿이라 현행본을 가릴 수 없었으며, 루트의 파생물은 초안과 함께 낡았다('26.9.10 전수 10건 중 4건, 최대 201조각). **처리**(`archive_revision.py`) — ① `snapshot --label`은 현행 초안을 `history/drafts/`로 **복사**(이동 아님), ② `begin`이 변환 시작 시 `history/rNN_{시각}/`을 선할당하고 `40_prepared`·`43_convert_input`·`40_roundtrip`·`40_qa`를 그 안에 쓴다(루트 파생물 0, 딸린 스냅샷도 함께 이동), ③ `43_convert_input`은 `to_kordoc_input.py`, `40_qa`는 `qa_report.py`가 결정론으로 만든다, ④ `compare` 기준은 `20_draft`, ⑤ 인도본 hwpx·`research/`는 이력으로 복사하지 않는다(`research/`는 `{YYYYMMDD-HHMM}_{조사내용}` 평면). **판본의 진실은 `final/` 파일 이름**이다 — index가 지워져도 인도본만 있으면 복원된다 (근거[실측]: '26.9.10 작업폴더 10건 전수 — 파생물 39건·research 326건 이관 · 경위 → rules-history R087)

## R081 [R072로 흡수·이관 — '26.9.24]

- R081 [export] **hwpx 생성 요청을 받으면 어떤 경로로 왔든 하네스 규격을 먼저 로드한다 — 조회 없이 생성기 기본 프리셋으로 뽑지 않는다**: '26.9.10 투자 적정성 검토건에서 하네스를 한 번도 조회하지 않고 kordoc 기본 `preset="개조식"`+표지·목차로 산출해 KCA 양식과 무관한 문서를 인도했고 사용자가 "내가 원하는 보고서 양식이 아닌데"로 반려했다. R072가 이미 "판단 기준은 입력이 아니라 출력 — hwpx를 만드는 모든 호출에 적용, 파이프라인 밖 변환이라는 분류 자체가 이탈 경로"를 명시했으나, 규칙을 어긴 것이 아니라 **규칙이 있는 곳에 진입조차 하지 않은 것**이 원인이다. **처리** — hwpx·보고서 산출 의도가 확인되는 즉시 ① `harness_config.load_config()`로 state_dir·assets_dir를 해석하고 ② `hwpx-recipe.md`·`style-guide.md`·`md-profile.md`를 읽은 뒤에 초안·변환에 착수한다. 생성기 호출은 R008 프로파일 파라미터(preset="보고서"·fonts·sizes·bullet2·levels·body_title_box·line_spacing) 전달과 §3.5 `postprocess_hwpx.py --all`, §4 `validate_hwpx.py structural`까지가 1회 단위이며, 셋 중 하나라도 빠진 산출물은 인도하지 않는다. **자가점검**: 인도 직전 "머리말 배너·붙임 배너·발신 줄 12pt가 있는가"를 확인한다 — 셋 다 후처리 산출물이라 하나라도 없으면 파이프라인을 탄 적이 없다는 뜻이다 (근거[관례]: '26.9.10 kca-ax-invest 반려)

> 폐지 사유: '26.9.24 통폐합(R079 이후 +9건) — 같은 주제의 R072와 함께 읽어야 판단이 서던 구조라 고유 내용을 R072 본문에 흡수했다. 번호는 재사용하지 않는다.

## R082 [R074로 흡수·이관 — '26.9.24]

- R082 [draft] **lint_md_profile 통과는 게이트의 절반이다 — audit_style를 연속 실행해야 초안이 확정되고, 생성기 자체 린터의 문체 경고는 style-guide 하위다**: 같은 건에서 종결어미를 두 번 틀렸다. 1차로 `~이다/~한다` 서술체로 쓰고, kordoc `generate_document`가 돌려준 문체 경고("'~다' 서술형 종결 — 실측 0%")를 보고 `~임/~함/~음`으로 "교정"해 style-guide §4 철칙(**"~함/~임/~음" 종결은 본문에서 쓰지 않는다**)을 정면으로 위반했다 — 정답은 동작명사 종결(구축·추진·확보·검토·필요·예정)이다. R074가 같은 유형('26.8.15 3기관 협의자료 전 문장 `~함/~됨`)을 이미 기록했으므로 **2회째 재발**이다. 원인 둘 — ① `audit_style.py` 미실행(`ending-forbidden`으로 첫 줄에서 잡혔을 사안), ② 외부 린터를 기관 규범보다 우선한 판단. **처리** — 초안 확정 조건을 `lint_md_profile.py` exit 0 **그리고** `audit_style.py` violations 0으로 못박는다(warning은 사람 판단). kordoc의 문체·표기법 경고(`DA_ENDING`·`DATE_2DIGIT_YR`·`MONEY_CHEONWON`·`AI_EM_DASH`)는 **전부 style-guide 하위**이며 이를 근거로 원문을 고치지 않는다 — 이미 smoke0 lesson('26.7.22)이 같은 결론을 냈다 (근거[관례]: '26.9.10 kca-ax-invest, R074 재발)

> 폐지 사유: '26.9.24 통폐합(R079 이후 +9건) — 같은 주제의 R074와 함께 읽어야 판단이 서던 구조라 고유 내용을 R074 본문에 흡수했다. 번호는 재사용하지 않는다.

## R083 [R045로 흡수·이관 — '26.9.24]

- R083 [draft] **ㅇ 아래 대시는 1개다 — md-profile 산문이 아니라 lint 구현이 기준값이다**: `md-profile.md` §1-1과 §3 룰 표는 "한 ㅇ 항목 아래 대시 나열은 2개까지 — 3번째부터 bullet-overflow"로 기술하나, `lint_md_profile.py`는 `bullet_run == 2`에서 위반 처리한다(R045 '26.8.15 강화 시 구현만 1개로 조이고 산문을 갱신하지 않은 잔재). '26.9.10 건에서 문서를 신뢰해 2개 기준으로 초안을 쓴 결과 `bullet-overflow` 10건이 났다. **처리** — ① 상세가 2개 이상 필요하면 대시를 늘리지 말고 **상위 ㅇ를 둘로 쪼개** 각자 1개를 갖게 하거나 ※·표로 배출한다, ② `md-profile.md` §1-1·§3 표의 "2개까지"를 "1개"로 정정해 문서·테스트·구현 1:1 정합(§5-3)을 회복한다. 규칙값이 문서와 구현에서 갈릴 때는 **린트가 실제로 튕기는 값**이 기준이다 (근거[관례]: '26.9.10 kca-ax-invest)

> 폐지 사유: '26.9.24 통폐합(R079 이후 +9건) — 같은 주제의 R045와 함께 읽어야 판단이 서던 구조라 고유 내용을 R045 본문에 흡수했다. 번호는 재사용하지 않는다.

## R086 [R087로 흡수·이관 — '26.9.24]

- R086 [export] **현행 산출물과 이력을 폴더로 가른다 — 판본은 파일명에 붙이지 않고 `history/`에, 인도본은 `final/`에 `r{NN}_{YYYYMMDD}_` 접두어로 쌓는다**: 실측('26.9.10 작업폴더 10건) 초안 판본 15개가 `_v6`·`.20260907-교차검증전`·`.이전판-2종` **세 갈래 표기**로 현행 파일 옆에 쌓여 있었고, `final/`에 hwpx가 여럿인 건이 6건이라 어느 것이 현행본인지 파일만 보고는 판별되지 않았다. 작업폴더가 인터페이스인데 그 안에 판본이 섞이면 다음 단계가 무엇을 읽어야 할지 이름으로 알 수 없다. **처리** — `archive_revision.py`가 위치와 표기를 맡는다: ① `snapshot --label <사유>`는 현행 초안을 `history/drafts/`로 **복사**한다(이동 아님 — 현행본이 제자리에 없으면 다음 단계가 깨진다), ② `revise`는 재변환 직전 변환 세트 5종을 `history/rNN_{시각}/`으로 내리고 그 판본에 딸린 스냅샷을 함께 옮긴다, ③ 인도본 hwpx는 이력으로 복사하지 않는다 — `final/`에 접두어로 남으므로 중복이다. `research/`도 복사하지 않는다(append-only 자산). **판본의 진실은 `final/` 파일 이름**이다 — index가 지워져도 인도본만 있으면 복원된다. 스냅샷은 게이트 시점에 사람이 사유를 붙여 뜬다(훅 자동은 Edit마다 쌓여 이력이 잡음이 되므로 기각). `revise`는 판본 번호와 초안 지문이 같으면 무동작이다(멱등) (근거[실측]: '26.9.10 작업폴더 10건 전수 — '26.8.12 판본 보존 설계의 구현·확장)

> 폐지 사유: '26.9.24 통폐합(R079 이후 +9건) — 같은 주제의 R087와 함께 읽어야 판단이 서던 구조라 고유 내용을 R087 본문에 흡수했다. 번호는 재사용하지 않는다.

## R003

### '26.9.25 규칙 문구 줄이기 전 원문

- R003 [draft] **수치·인용의 확정/추정 판정은 작업 단계에서 유지하되 인도본 본문에는 노출하지 않는다**: 종전 문구가 "태깅과 출처를 남긴다"로만 되어 있어 `[확정]`·`[추정]` 표기가 본문 문장 끝에 그대로 찍힌 채 hwpx까지 인도됐다('26.8.15 3기관 협의자료 — 본문 41개소). 이 표기는 **하네스가 근거 신뢰도를 관리하는 내부 장치**이지 수신자에게 보일 문서 요소가 아니다. 판정 자체는 계속 수행하되 **분석·조사 산출물(05_analysis·research/)에 남기고**, 인도본에서는 ① 확정 사실은 그냥 서술하고 ② 추정·2차 인용은 `※` 단서나 붙임 「확인 방법과 범위」에서 그 범위를 밝힌다. 린트 `confidence-tag-in-body`가 본문 잔존을 검출한다 (근거[실측]: '26.8.15 사용자 지적)

## R005

### '26.9.25 규칙 문구 줄이기 전 원문

- R005 [draft] 표는 6열 이하, 병합은 md-profile §1-2 표기(머리글 `A > B` 2단·칸 `〃` 세로 병합·첫 열 굵게 = 라벨 열)로만 쓰고 후처리가 hwpx 병합 표로 만든다('26.9.24 정정 — 종전 '병합 금지'는 GFM 한계 때문이었고 경영관리 프레임워크 틀이 2단 머리행·병합 구분 열을 요구), 나열 3개+속성 2개↑는 표 전환

## R006

### '26.9.25 규칙 문구 줄이기 전 원문

- R006 [export] 이미지는 170×90mm 이내만 주입, 캡션에 출처 필수

## R008

### '26.9.25 규칙 문구 줄이기 전 원문

- R008 [export] generate_document 호출 시 KCA 프로파일 파라미터를 반드시 전달한다 — fonts(heading=HY헤드라인M, body=휴먼명조, ref=맑은고딕, table=맑은 고딕)·sizes(dae15·cham13·table12·bodyTitle24)·bullet2=ㅇ(U+3147, 한글 이응 — U+110B 초성·U+25CB 원 기호 아님)·**levels={"1":{"bold":false}}(미전달 시 kordoc 기본이 ㅇ 계층 전체를 굵게 처리해 R016의 '괄호 리드만 볼드'를 깨뜨린다 — '26.9.11 실측: charPr bold=1을 ㅇ 문단 19개 전 run이 참조)**·body_title_box·line_spacing160 (근거[관례]: 20260722건 양식 미적용 · '26.8.10 format-profile.kca.md §71 정합 정정 — U+25CB 오생성 사례 · '26.9.11 levels 누락 실측). **제목표 3행 규격은 제목 24pt('26.9.24 사용자 확정 20→24, 한 줄 맞춤)·밴드 1pt·밴드 줄간격 70%다** — '26.9.11 실인도본 19건 전수 실측으로 확정(format-profile §2 표기와 일치). kordoc은 sizes.bodyTitle을 반영하지 않아 25pt로 산출하므로 postprocess `apply_title_box_size`가 24pt를 적용하고, `apply_title_box_form`이 밴드 문단을 1pt·70%로 낮춘다. **밴드를 제목 행에서 복제하면 제목 크기 글꼴이 따라와 빈 문단 줄 높이(25pt×130%≈3250hu)가 밴드 셀 높이(382hu)를 밀어내 밴드가 제목 행만큼 두껍게 렌더된다** — '26.9.11 사용자 지적. 이미 3행인 문서도 재실행 시 소급 수리한다(멱등, `bands_repaired`)

## R013

### '26.9.25 규칙 문구 줄이기 전 원문

- R013 [export] 계층 간격은 postprocess_hwpx.py --spacing으로 양식 실측값(발신→□8pt/□→ㅇ6pt/ㅇ→ㅇ6pt/대시→ㅇ6pt/**ㅇ→-3pt**/-→＊3pt/블록15pt/＊→표10pt)을 재현한다. **ㅇ→대시만 3pt**('26.9.10 사용자 확정 — 종전 6pt는 상위 ㅇ와 그 하위 대시를 다른 덩어리로 벌려 놓아 계층이 목록으로 읽혔다. 나머지 전환값은 불변) (근거[실측]: 동일 · ㅇ→대시는 '26.9.10 사용자 확정)

## R015

### '26.9.25 규칙 문구 줄이기 전 원문

- R015 [export] **[예외 → R035 / 캡션 → R034]** **('26.7.28 사용자 확정으로 정정)** 본문 콘텐츠 표는 **우측 정렬**(표 래퍼 문단 RIGHT paraPr), 캡션은 별도 문단이 아니라 **표 caption 내장**(→R034). 예외: 제목 박스 CENTER 유지, 붙임·참고 배너 표는 좌측(→R035). postprocess `apply_table_alignment`가 배정 (구 규칙 '캡션·표 가운데 정렬'은 폐기)

## R016

### '26.9.25 규칙 문구 줄이기 전 원문

- R016 [draft][export] ㅇ 괄호 리드는 볼드(**(한 계)**) — 코퍼스 관례. **리드만 굵게 하고 문중 괄호(수치·시점·근거)는 볼드가 아니라 13pt(R033·R039)다.** 린트 `lead-not-bold`가 `**` 누락을 검출하고, 리뷰 뷰어는 굵은 글꼴이 있는 명조(나눔명조)로 그려 볼드가 hwpx와 같게 보인다 ('26.9.24 f7 — AppleMyungjo 폴백은 볼드가 없어 리드가 보통 굵기로 보였다) (근거[관례]: 20260722건 사용자 확정)

## R019

### '26.9.25 규칙 문구 줄이기 전 원문

- R019 [export] **[정렬 확장 → R061 / ☞ 확장 → R025]** 계층 시작은 원래 폰트의 리터럴 띄어쓰기(□0·ㅇ1·대시3·＊※3칸), 줄바꿈 하위는 자동 들여쓰기 — 내어쓰기 폭은 **그 폰트 글자 단위**로 산정: □ 1.5글자(22.5pt)·ㅇ 2글자(30pt)·대시 2.5글자(37.5pt)·＊※ 3글자(39pt), left=0·intent=-글자폭 (한글 내어쓰기 의미론: 첫 줄=left=0 → 띄어쓰기만이 첫 줄 위치 결정, 랩 줄=|intent| 위치. left에 값을 넣으면 첫 줄이 밀림 — 금지). pt는 폰트 크기가 아니라 글자폭 환산 거리 — 리포트에는 글자 단위 병기. 표 셀 텍스트 CENTER, 표-문단 간격 캡션→표 3pt·표→※ 3pt·문단→캡션 6pt **'26.9.24 정정 — ＊※ 선두 5칸(52pt) → 3칸(39pt)**: 표 아래 ※가 표 왼쪽 끝에서 떠 보여 사용자가 3칸으로 지정(☞도 R025로 따름) (근거[관례]: 20260722건 사용자 확정 — --star-indent 훅은 폐기)

## R020

### '26.9.25 규칙 문구 줄이기 전 원문

- R020 [export] 편집용지 여백은 양식 규격 강제 — 좌우20·위10·아래15·머리말15·꼬리말10mm (kordoc preset 위 15mm 결함 후처리, 근거: 20260722건 제목표 상단 여백)

## R022

### '26.9.25 규칙 문구 줄이기 전 원문

- R022 [export] 제목표 상단여백 미세 제거는 앵커 문단 줄간격 100% 치환 + 표 outMargin top 0 — 단 **이 두 치환만으로는 사용자가 보는 상단 여백이 사라지지 않는다('26.7.28 3차 조사 정정)**: 치환 적용본의 제목표 위치는 한컴 저장 실무 보고서와 0.2mm 이내 일치(제목 텍스트 상단 28.1mm vs 실무본 27.9mm, 실무본 lineseg 캐시 vertpos=0 교차검증)했으므로 기하 여백은 이미 0이고, 잔존 '여백'의 실체는 **머리말 부재**(→R030). **상단 얇은 행(3.8pt)은 양식 원형의 배경 밴드이므로 행 삭제 금지**(행 삭제 시 그라데이션 소실 회귀, 20260724건 2회 왕복으로 확정). 밴드 채움 실측 정정: kordoc 산출본은 상단 행 단색 #0080C0·하단 행 RADIAL #0080C0→#3CBFFF이고, 양식·실무본 원형은 상·하단 모두 LINEAR 90° #3057B9↔#DFE6F7(상단 진→연, 하단 연→진) — 색·방향 차이는 미고침 관찰 사실 (근거[관례]: 20260724건 사용자 확정 + 20260722건 양식 OLE 실측 + 20260728건 0839 hwpx·260331 실무본 XML 실측). **'26.7.28 4차 재검증(앵커 문단 가설)**: 사용자 관찰 "문단에 [표1]로 내용"은 실무본과 동일 구조(제목표 = 앵커 문단 속 treatAsChar 표) — 앵커 문단 기여 실측 0pt(prev=0·줄간격 100%·outMargin top 0), kordoc 렌더 밴드 상단 25.31mm vs 실무본 제목 상단 27.95mm로 오히려 타이트. 본문 쪽 제거 가능 잔여 없음

### '26.9.25 원인 서술 정정

- '잔존 여백의 실체는 머리말 부재(→R030)'라는 서술을 R041·R042 기준으로 정정했다(R030 절 참조). 밴드 행 삭제 금지와 채움 색·방향 관찰은 유효하다.

## R025

### '26.9.25 규칙 문구 줄이기 전 원문

- R025 [export] ☞ 등 결론 유도 기호는 ＊·※와 동일 계층 처리 — 선두 3칸 리터럴 띄어쓰기 + 내어쓰기 left=0·intent=-3글자폭(15pt 본문 기준 45pt — '26.9.24 R019 정정에 따라 5칸·60pt에서 변경), 인접 간격 3pt 준용 (근거[추론]: 20260724건 사용자 확정, R019 확장)

## R026

### '26.9.25 규칙 문구 줄이기 전 원문

- R026 [draft] **조사 기반 보고서는 본문=두괄식 결정·요지, 붙임=「검토 근거 상세」로 분담하고 본문은 자기완결로 쓴다**: 본문에 쓴 근거의 원문 인용·수치 산출 근거·차용 논리는 붙임(2p 이내, R009 배너 표)에 담아 제3자가 검증할 수 있게 한다. **본문에서 괄호로 붙임에 설명을 미루지 않는다** — `(상세 붙임1 참조)`처럼 문장 안에서 설명을 유보하면 읽는 사람이 두 번 이동해야 하고 본문이 목차로 전락한다(개선본은 이 표기를 6회→0건으로 제거). 붙임은 **검증하려는 사람만 펴보는 원자료**이지 본문의 각주가 아니다. **예외**: 절 맺음의 `※ 세부내용은 붙임 참조`는 코퍼스 합법 관례이므로 유지한다(문장 안 유보가 아니라 절 종료 후 안내). 린트 `annex-crossref`가 괄호 유보형만 검출한다 (근거[관례]: 20260724건 사용자 확정 · cert-poc 개선본 대조 — R053 흡수·이관 '26.9.6)

## R027

### '26.9.25 규칙 문구 줄이기 전 원문

- R027 [export] 붙임·참고 배너(3열 표, 첫 셀 '붙 임'/'붙임 N'/'참고N')는 양식 참고 블록 정합 — 셀 글자 HY헤드라인M 16pt, 배너 앵커 문단 pageBreakBefore=1로 별도 페이지에서 시작, R023 12pt 강제 대상에서 제외. **셀 스타일은 양식 '참고1' OLE 재실측값('26.7.24 정정)**: 라벨 셀 4변 SOLID 0.5mm #1B1760+채움 #2B2D63(남색)+흰 글자, 스페이서 좌변만 SOLID, 제목 셀 상·하변 SOLID, 행 높이 28.3pt, 셀 폭 라벨 21.1mm·스페이서 2.0mm·제목 잔여. HWP COLORREF는 0x00BBGGRR — hex를 그대로 읽으면 R/B가 반전되며 최초 기록(#60171B/#632D2B 적갈)이 이 오류였음. 색 실측은 반드시 바이트 순서 검증 후 기록 (근거[실측]: 20260724건 사용자 2회 확정 — 교차검증: 표 헤더 bf14=#D2EBEE·제목박스 그라데이션 #3057B9 남색 계열 정합)

## R028

### '26.9.25 규칙 문구 줄이기 전 원문

- R028 [draft] **붙임은 각 1장 이내 — 신설 시 절 3개·표 3개를 상한으로 세고, 조사 과정 서술은 붙임이 아니라 참고자료로 내린다**: 계층형 문구(ㅇ·대시)는 렌더 기준 2줄 이내(휴먼명조 15pt, 1줄 35자 = 90자 상한 — R062)로 쓰고, 넘치는 상세는 표 전환·항목 분리·삭제로 압축하며 본문 계층 문구도 동일 상한을 준용한다(20260724건 사용자 확정). '26.9.3 3기관 협의자료는 1차 축약을 거쳤는데도 붙임·별첨이 226줄·13절·표 13으로 본문(114줄)의 두 배가 되어 "중복·조사과정 장편 제거" 재요청을 받았다 — **1장 상한만 적어 두고 절·표 수를 세지 않아** 붙임을 신설할 때마다 검사가 걸리지 않은 것이 원인이다. **이관 판정 3가지** — ① 수치 정정 경위·조사 방법론·판본 이력처럼 수신자 판단에 무관한 조사 과정 서술, ② 설계 단계에나 쓰는 사양·전량 명세, ③ 게이트⓪ 결정으로 본문에서 뺀 항목의 원자료. 셋 다 별도 참고자료 파일(`2N_설계단계참고_{주제}.md`)로 내리고, 붙임에는 본문 요지를 검증하는 데 실제로 쓰이는 표만 남긴다 (근거[실측]: '26.9.3 3기관 협의자료 재축약 — 붙임·별첨 226줄→66줄·표행 151→63·중복 쌍 2→0)

## R030

### '26.9.25 규칙 문구 줄이기 전 원문

- R030 [export] 제목표 상단여백의 **확정 원인은 머리말(hp:header) 부재** — kordoc 산출본은 머리말이 없어 편집용지 위 10mm+머리말 15mm(R020 양식 규격)=25mm가 통째로 빈 흰 띠로 남고, 이것이 제목표 위 여백으로 보인다(줄간격·outMargin·행높이·편집용지 후보는 전부 XML 실측으로 배제 — R022 참조). 양식 원형(표준양식 .hwp Section0 head ctrl)과 실무 보고서는 이 영역을 **KCA 로고 배너 표(1×2, 높이 11.3mm, 로고 png+슬로건 bmp)**가 채운다. 처리: postprocess `--header-banner`(--all 포함)가 실무본 이식 자산(assets/kca-header-banner/: fragment.xml+resources.xml+이미지 2종)을 id 재배정·manifest 등록과 함께 주입한다. hp:header 기존재 시 스킵(멱등). 편집용지 여백 축소로 대응 금지(양식 규격 위반) (근거[관례]: 20260728건 0839 3차 조사 — 260331 실무본 이식, 주입 전후 kordoc 렌더 바이트 동일로 본문 무영향 확인). **'26.7.28 4차 재검증**: 주입된 배너 서브트리는 실무본 원형과 id 재배정 외 완전 동일(diff 실측), 참조 id·manifest 전부 유효, 배너 라인 39.4pt < 머리말 영역 42.5pt로 본문 밀림 없음 — 배너가 여백을 늘리지 않았고 되돌릴 사유 없음. 잔여 흰 영역 = 위 10mm + 배너 아래 3.15pt(실무본 동일, 양식 구조). **kordoc reflow 렌더는 머리말·hp:caption·pageBreakBefore를 그리지 않으므로 렌더 스크린샷으로 상단여백·캡션·배너 페이지 시작을 판정하지 말 것 — 한글 실물 확인 필요**

### '26.9.25 원인 서술 정정

- 3·4차 조사('26.7.28)는 제목표 상단 여백의 확정 원인을 머리말 부재로 보고, 4차 재검증에서 '배너 라인 39.4pt < 머리말 영역 42.5pt로 본문 밀림 없음'이라 적었다. 같은 날 이어진 조사에서 두 판단이 모두 정정됐다 — R041: 도너 앵커 줄간격 150%가 배너 줄 높이를 17.0mm로 부풀려 머리말 영역 15mm를 넘기고 본문을 밀었다(주입 자체는 유효, 앵커 100% 강제로 보정). R042(6차 확정): 사용자가 6회 보고한 여백의 정체는 표 폭 slack 0으로 표가 다음 줄로 내려가 생긴 15pt 빈 줄이며, R022·R030·R041은 실재한 별개 결함이었다(R042 절 참조).
- 규칙에 대체·정정 표기가 없어 '확정 원인'이 유효 서술로 남아 있었다. 배너 주입(양식 규격)은 유효하므로 규칙은 두고 머리말 문구를 주입 지시로 바꾸며 `[원인 정정됨 → R041·R042]` 표기를 달았다(코드 변경 없음).

## R036

### '26.9.25 규칙 문구 줄이기 전 원문

- R036 [export] **[상한값 정정 → R042 · 메커니즘은 본 규칙 유효]** **표 총 폭(표 폭 + outMargin 좌우)은 편집용지 본문 폭을 넘지 않는다** — 초과 시 표 폭·셀 폭·내부 그림을 같은 비율로 축소한다(`postprocess_hwpx.py` `apply_fit_page_width` — 플래그와 무관하게 항상 실행. '26.7.29 코드 실측 정정, 종전 '--all 포함' 표기는 오기). 근거: KCA 실보고서 12건 전수 실측에서 **머리말 배너 표 폭 == 본문 폭**이 예외 없이 성립(좌우 20mm 문서 12건 → 배너 170mm / 좌우 15mm 문서 3건 → 배너 179mm+여백 = 180mm). 머리말 배너 자산을 좌우 15mm 문서(260331)에서 이식한 결과 좌우 20mm 문서에서 **배너 10mm·제목박스 1mm 초과**가 발생했고, 이것이 R022·R030 3차례 조사에서 못 찾은 **제목표 상단 여백의 실제 원인**이다 — 여백 속성이 아니라 **폭 초과**였다. **'26.7.28 6차 정정 → R042**: 축소 목표 '이내'(본문 폭과 정확히 같게)가 slack 0을 만들어 또 다른 여백을 낳았다 — 상한을 '미만'(여유 283 hu)으로 정정 (근거[실측]: 20260728건 4차 조사, 사용자 3회 재보고)

## R037

### '26.9.25 규칙 문구 줄이기 전 원문

- R037 [export] 붙임·참고 배너 표(R027 3열)의 **제목 셀(3번째) 문단은 양쪽 정렬(JUSTIFY)** — 라벨('붙임 N')·스페이서 셀은 가운데 정렬(CENTER) 현행 유지. R035(배너 표 자체의 좌측 **배치**)와는 별개 층위 — 이 규칙은 표 **안** 제목 셀의 문단 정렬이다. postprocess `apply_annex_banner` ⑤가 배정하고 `apply_center_cell_text`는 배너 제목 셀을 제외한다(R023 12pt의 배너 셀 제외와 같은 패턴), 요약 JSON `annex_banner.title_justified` 보고 (근거[실측]: '26.7.28 사용자 확정)

## R038

### '26.9.25 규칙 문구 줄이기 전 원문

- R038 [export] **※·＊ 단서/각주 뒤 ㅇ 복귀 전환 간격 6pt(600)** — R013 전환 표에 미정의라 간격 0으로 붙어 보이던 결함의 보완(TRANSITIONS `cham→yo`·`star→yo` 추가). 값은 실측이 아니라 **정합 추론**: 표준양식(250609)은 ※ 기호 자체가 부재(코드포인트 전수 스캔)·실무본(260331)도 ※·＊ 문단 부재로 이 전환의 실물이 없어, R013 체계의 X→ㅇ 전환(□→ㅇ·ㅇ→ㅇ·대시→ㅇ) 전부 6pt인 정합값을 적용. R013의 전환 표 확장이다 (근거[추론]: '26.7.28 사용자 확정 — 20260728건 1633 ※→ㅇ 무간격 지적)

## R039

### '26.9.25 규칙 문구 줄이기 전 원문

- R039 [export] 본문 괄호 13pt(R033)는 **괄호 구간이 run 경계를 넘어도 적용** — run 단위가 아니라 **문단 전체 텍스트 기준**으로 `(…)` 구간을 찾고, 걸친 run들을 각각 분할해 걸친 부분만 그 run 고유 charPr의 13pt 복제본으로 치환한다(서식 보존 — 볼드 run 안 괄호는 13pt 볼드). 문장 안 볼드(`**T1**` 등)로 run이 쪼개져 약 70%가 건너뛰어진 결함의 항구 수정(1127건 13/63 → 20260728건 13/45 — 2회 반복 관찰 승격). `cross_run_skipped`는 분할 불가 run(그림 등 중첩 개체)에 걸린 구간만 남으며 **통상 0이어야 한다**(스모크 실측: 32→0). R033의 예외(ㅇ 선두 리드 15pt 볼드·표 셀·＊※ 각주 비대상)는 그대로 유지 (근거[관례]: '26.7.28 사용자 확정)

## R040

### '26.9.25 규칙 문구 줄이기 전 원문

- R040 [draft][export] **강조 2계층 — 강조=볼드(style-guide §6 현행), 특히 강조=노란색 음영 하이라이트**: 초안 md는 `==문구==`로 표기(md-profile §1-3, 문서당 극소수 지점만·짝 불일치는 lint `highlight-unpaired` 검출)하고, kordoc 변환은 리터럴 통과 → postprocess `apply_highlight`가 마커 제거 후 해당 구간 run에 charPr **`shadeColor="#FFFF00"`+볼드** 복제본 배정(run 경계 걸침도 분할 처리, 잔존 `==`는 compare `markdown-leftover` 최종 검출). 인코딩은 260331 실무본 'AI검증 후 최종결과물 변환' run(charPr id=82: shadeColor="#FFFF00"+bold, 문서 내 유일 음영 run) 실측 — **hwpx XML 색은 #RRGGBB 직독**(#FFFF00=노랑, 사용자 관찰 정합. 교차검증: 같은 문서 제목박스 그라데이션 #3057B9=남색 정합, BGR로 읽으면 #B95730 적갈로 불일치). R027의 COLORREF 0x00BBGGRR 반전은 **hwp OLE 바이너리 전용**이며 hwpx XML엔 미적용 — 바이트 순서 검증 완료 (근거[실측]: '26.7.28 사용자 확정)

## R041

### '26.9.25 규칙 문구 줄이기 전 원문

- R041 [export] 머리말 배너(R030) 주입 시 **앵커 문단 paraPr lineSpacing을 PERCENT 100으로 강제 + 머리말 subList textWidth를 현 문서 본문 폭으로 보정** — 제목표 상단 여백의 실원인은 도너(260331, 좌우 15mm)에서 이식된 **앵커 lineSpacing 150%**: treatAsChar 배너 표(11.3mm)의 줄 높이를 17.0mm로 부풀려 머리말 영역 15mm(4251)를 초과시키고 신규 조판 시 본문을 ≈4.6mm 밀어냈다(실기동 A/B: 150%=30.42mm / 100%=25.79mm / 실무본 25.5mm — 0.3mm 이내 일치). 배너 셀 내부 문단(160%)은 비대상, 기주입 문서도 재실행 시 소급 수리(멱등). 미세조정안(115%·FIXED)은 기각 — 배너를 실무본 좌석(로고 12.5mm)까지 내리려면 줄 높이가 다시 15mm를 넘어 본문이 밀리므로 lineSpacing만으로는 양립 불가(XML 기하 판정), 본문 위치 정합(이 결함의 본질)을 택함. R030(머리말 주입 자체)은 유효 — 이 규칙은 '주입된 앵커의 lineSpacing이 여백의 실제 원인이었다'는 R030 계열 정정이다. R022(앵커 줄간격·outMargin)·R036(배너 폭 초과)은 실재한 별개 결함이었으나 세로 여백의 원인은 아니었다. textWidth 51026(도너 180mm)→본문 폭 실측치 치환 (근거[실측]: '26.7.28 실기동 A/B 실측 + XML 기하 판정)

## R044

### '26.9.25 규칙 문구 줄이기 전 원문

- R044 [draft] **구성요소 이름을 가운뎃점으로 늘어놓고 "N단 구조"·"N중 차단"·"N종 구현"으로 부르는 라벨 나열 금지 — 무엇을 하는지 서술한다**: `핵심결론·본문·보충안내·출처표기 4단 구조로 규격화`, `인젝션·개인정보·내부정보 3중 차단 적용`, `진입 가드·질의 재작성·지식 검색·컨텍스트 정제·생성 전송 5단계로 처리` 같은 표기는 항목명만 나열해 독자에게 **동작·효과를 전달하지 못한다**(목차를 본문에 옮겨 적은 상태). 대체 원칙 — ① 그 구조가 **무엇을 보장하는지**를 술어로 쓴다(`결론을 먼저 제시하고 근거를 뒤에 붙이는 순서로 고정해 질문 요지에 즉답`), ② 구성요소 목록 자체가 필요하면 **표로 배출**한다(style-guide §3 나열 3개 초과+속성 2개↑ 표 전환과 동일 판단), ③ 이미 같은 목록을 지탱하는 표가 있으면 본문은 **그 표가 말하지 않는 요지·판단만** 쓴다(R026 본문·붙임 분담과 같은 층위). **적용 예외**: 기술스택·자료 규모처럼 고유명사·수치 자체가 정보인 사양 나열(`Node.js 24.x · Next.js 16.2 · React 19.2`)과 style-guide §4의 조사 생략 병렬 압축(`근거 소실·실적 누락·작성부담 해소` — 이것은 항목명이 아니라 **해소 대상 상태**의 병렬)은 대상이 아니다. 판별 기준은 "가운뎃점으로 묶인 것이 **고유명사·상태**인가, 아니면 **내가 방금 만든 구조의 부품명**인가" — 후자면 위반 (근거[관례]: '26.7.30 사용자 확정, 이음5G AX파일럿 결과보고건)

## R045

### '26.9.25 규칙 문구 줄이기 전 원문

- R045 [draft] **상위 계층 하나에 딸리는 하위 계층은 최대 1개 — 초과분은 통합 서술로 합친다**('26.8.15 2개→1개 강화 · R083 흡수 '26.9.24): ㅇ 아래 대시는 1개까지만 둔다(□ 아래 ㅇ도 같은 원칙을 준용하되, 절 단위 ㅇ 수는 절 구성상 필요한 만큼 허용). 하위 항목이 늘면 **개수를 줄이는 방향이 아니라 문장을 합치는 방향**으로 처리한다 — 성격이 가까운 항목끼리 하나의 서술로 묶고, 그래도 안 합쳐지면 상위 ㅇ를 둘로 쪼개 각자 1개씩 갖게 하며, 상세는 대시를 늘리지 말고 ※·표로 배출한다(style-guide §3 배출구 원칙·R044 ②와 동일 층위). **금지 예시**: 한 ㅇ 아래 대시 4개를 나란히 다는 것(계층이 목록으로 전락). 린트 `bullet-overflow`가 2개째부터 검출하며, **문서 산문과 린트 구현이 갈리면 린트가 실제로 튕기는 값이 기준**이다('26.9.10 md-profile에 남은 '2개까지'를 믿고 쓴 초안에서 bullet-overflow 10건 — md-profile 정정 완료) (근거[관례]: '26.7.30 사용자 확정 이음5G AX파일럿 · '26.9.10 kca-ax-invest)

## R046

### '26.9.25 규칙 문구 줄이기 전 원문

- R046 [draft] **본문 서술의 날짜는 `'yy.m월` 월 단위로 표기 — 연월일 풀 표기(`2026. 7. 30.`·`'26. 7. 30(목)`)를 쓰지 않는다**: 보고 본문에서 일자·요일까지 특정하면 문서가 특정 시점에 고착돼 재사용·후속보고 때마다 손봐야 하고, 대부분의 서술에서 일자는 정보 가치가 없다. `'26.7월 실측 결과`, `'26.6월 기준 진도율 70%`처럼 **월 단위로 끊는다**(월 단위는 붙여쓰기 — style-guide §5 `'25.6월` 관례). 기간 범위는 물결로 `'26.3월 ~ '26.7월`, 연도만 필요하면 `'27년`, 반기 단위는 `'26년 하반기`. **예외 2가지**: ① **발신 줄**은 style-guide §1 철칙(`< '26. 7. 30.(목), 본부 팀 >`)이 지배하므로 실제 보고일을 일자·요일까지 적는다, ② 특정 일자 자체가 보고의 핵심 사실인 경우(회의 개최일·시행일·마감일 등)는 `'26.6.23(화)` 축약형으로 적되 4자리 연도는 쓰지 않는다. kordoc 변환기가 내는 `[DATE_2DIGIT_YR] 연도는 네 자리로` 경고는 기관 관례에 반하므로 **무시한다**(경고이지 오류가 아님) (근거[관례]: '26.7.30 사용자 확정)

## R047

### '26.9.25 규칙 문구 줄이기 전 원문

- R047 [draft] **본문 수치는 팩트체크를 통과한 값만 쓰고, 신뢰를 깎는 과잉정밀·단발측정 수치를 피한다**: 표본 1회 측정치를 소수점까지 적는 것(`응답 0.59초`)은 정밀해 보이지만 재현성이 없어 **오히려 신뢰를 떨어뜨린다** — 이런 값은 `1초 이내`처럼 **의미 있는 구간으로 올림**하거나 판정 결과(`정상`)로 대체한다. 판단 기준 3가지 — ① 제3자가 같은 방법으로 재보면 같은 값이 나오는가(아니면 구간화), ② 자릿수가 측정 정밀도를 넘지 않는가(단발 curl 측정에 소수 2자리는 과잉), ③ 근거 원문에 그 수치가 실제로 있는가(R003 확정/추정 태깅과 연동). 코드 상수·자료 건수·공표 실적처럼 **원본에 확정값이 있는 수치는 그대로 인용**한다(`488건`·`상위 6건`·`유사도 0.4`·`20회/60초`). export 단계 경량 팩트체크(`validate_hwpx.py numbers`)가 근거 없는 수치를 걸러내지만, 과잉정밀은 기계가 못 잡으므로 **집필 단계에서 판단**한다 (근거[관례]: '26.7.30 사용자 확정)

## R049

### '26.9.25 규칙 문구 줄이기 전 원문

- R049 [draft] **□ 절 하나에 딸리는 ㅇ는 최대 3개 — 초과하면 통합 서술로 합치거나 절을 나눈다**: R045가 "ㅇ 아래 대시 1개"를 규정하면서 "□ 아래 ㅇ 수는 절 구성상 필요한 만큼 허용"으로 열어 두었으나, 실제로는 한 절에 ㅇ가 6개까지 늘어나 계층이 목록으로 전락하는 결함이 반복됐다. **처리 순서** — ① 성격이 가까운 ㅇ끼리 하나의 요지로 합치고 각자의 상세는 대시로 내린다(예: '초기 구축'+'한계 확인' → 한 요지의 두 대시가 아니라, 시간 순 서사면 그대로 두 요지로 유지), ② 그래도 4개 이상이면 **부수적 사실은 ※(단서·후속 참조)로 배출**한다 — ※는 ㅇ 수에 계상되지 않으므로 신규 역량·운영 기능처럼 본문 요지에서 밀려난 항목의 정규 배출구다, ③ 그래도 남으면 □ 절 자체를 둘로 쪼갠다(단 사용자가 절 구성을 지정한 경우 절 신설 금지 — ①②로 해결). 붙임의 □도 동일 적용. R045(ㅇ→대시 1개)와 한 쌍으로 **1절 = ㅇ 3 × 대시 1 = 최대 6항목**이 계층형 본문의 밀도 상한이다 (근거[관례]: '26.8.3 사용자 확정, cert-poc PoC 결과보고건)

## R051

### '26.9.25 규칙 문구 줄이기 전 원문

- R051 [draft] **ㅇ 괄호 리드는 3~5음절 구체 명사구 — 2음절 추상어를 벌려쓰기로 때우지 않는다**: `(품 질)`·`(배 포)`·`(구 축)`·`(교 육)`처럼 2음절로 두면 그 절이 무엇을 다루는지 리드만 보고 알 수 없다. 실무 개선본은 이를 전부 **의미 해상도를 올린 명사구**로 교체했다 — `(서비스 품질)`·`(서비스 배포)`·`(서비스 구축)`·`(고급 교육)`, 나아가 `(이용 양상)`→`(AI맥락 주입)`·`(확 산)`→`(접근성 보장)`처럼 **리드 자체를 더 정확한 개념으로 재명명**한 사례도 있다. 자간(벌려쓰기) 문제가 아니라 **어휘 선택** 문제다. **적용 범위 한정**: style-guide §2의 2음절 벌려쓰기 관례는 **표 헤더 전용**으로 남긴다(`구 분`·`시 기`·`회 차`·`비 고`·`단 계`·`역 할` — 개선본에서도 유지됨). 린트 `lead-too-short`가 ㅇ 선두의 2음절 괄호 리드를 벌려쓴 형태·붙인 형태 모두 검출한다 (근거[관례]: '26.8.4 사용자 개선본 2건 대조 — ICT어드바이저에서 6건 전량 교체)

## R052

### '26.9.25 규칙 문구 줄이기 전 원문

- R052 [draft] **각주가 필요한 기술용어는 각주를 달지 말고 용어를 바꾼다 — 각주 상한 4개**: 개선본은 용어 각주를 7개→0개, 8개→4개로 줄였는데 각주를 삭제한 것이 아니라 **각주가 필요한 단어를 안 쓰게** 고쳤다. `holistic＊ 단일콜(문항당 1콜+11-enum)` → `문항당 1회 호출`, `run_pipeline() 단일 소스로 CLI·웹 공유` → `명령행과 웹 어느 쪽으로 실행해도 동일한 결과`, `A01~A21 분류체계` → `11개 유형으로 분류`, `hp:equation 스크립트 직접 추출` → 삭제. 판단 기준 — **수신자(원장·협의체·현업 부서장)가 그 단어를 모르면 각주가 아니라 다른 단어를 찾는다.** 코드 식별자(`함수명()`·`파일명.확장자`·내부 enum·XML 태그)는 보고서 본문에 쓰지 않는다. 린트 `footnote-overflow`가 5개째부터 검출 (근거[관례]: 동일 대조 — cert-poc 용어＊ 7→0)

## R055

### '26.9.25 규칙 문구 줄이기 전 원문

- R055 [draft] **향후 계획은 실행 액션 — 주체·시점·이미 일어난 근거사실을 함께 적는다**: `외부용역 사업 전환을 추진 중(업체 미팅 '26.7.1 완료)`처럼 **이미 발생한 사실**을 괄호로 붙여 실행 의지를 뒷받침한다. 성과 나열이나 일반론("품질을 지속 개선")으로 절을 채우지 않는다. 각 ㅇ는 `누가(주체) 무엇을(액션) 언제까지(시점)`가 읽히도록 쓰고, 시점은 R046에 따라 `'yy.m월`·`'yy년 하반기` 단위로 적는다 (근거[관례]: '26.8.4 cert-poc 개선본 — 사업 전환·기술지원·확장 연계 3항목 전부 주체·근거사실 포함)

## R056

### '26.9.25 규칙 문구 줄이기 전 원문

- R056 [draft] **표 캡션은 일련번호 없이 표가 무엇을 보여주는지 서술한다**: `[ 표1. 추진 총괄 요약 ]`이 아니라 `[ 월별 바이브코딩 교육 진행 ]`·`[ 질의 1건의 처리 흐름 ]`·`[ 출력강제 방식별 실험 결과 ]`처럼 **측정 축·단위·조건**을 캡션에 담는다. 표본이 한정적이면 조건을 병기한다 — `[ 사용량·품질·API 실측(테스트운영) ]`. 본문에서 표를 번호로 부르지 않으므로(style-guide §8 무참조 관례) 번호는 유지비용만 남는다. 린트 `caption-numbered`가 검출 (근거[관례]: '26.8.4 개선본 2건 — 캡션 11개 전부 번호 없음)

## R057

### '26.9.25 규칙 문구 줄이기 전 원문

- R057 [draft] **연결어를 반복하지 않는다 — 한 절에서 같은 연결어 3회 초과 금지**: 실무 개선본은 한 문서에 연결어 10종을 쓰는데(`하고`6·`하여`3·`하며`3·`함께`2·`되어`·`만큼`·`도록`·`없이`·`순차`·`시켜`) 하네스 산출본은 3종(`하고`4·`도록`2·`우선`)에 그쳐 문장이 단조롭다. **연결어 후보** — 병렬·나열: `~하고`·`~하며`·`~와 함께`·`~해`, 목적: `~하고자`·`~도록`·`~위해`, 원인·근거: `~되어`·`~발생해`·`~인 만큼`·`~에 따라`, 수단: `~으로`·`~을 통해`·`~시켜`, 배제·한정: `~없이`·`~만으로는`·`~에 국한되지 않는`, 첨가: `~도`·`~까지`, 동시: `~면서`·`~되며`. 같은 어미가 인접 두 문장에 연달아 오면 하나를 바꾼다. **주의**: 다양성 자체가 목적이 아니라 **관계를 정확히 표시**하는 것이 목적이다 — 인과를 `~하고`로 뭉개지 말고 `~되어`·`~발생해`로 드러낸다 (근거[관례]: '26.8.4 개선본 2건 연결어 분포 실측)

## R058

### '26.9.25 규칙 문구 줄이기 전 원문

- R058 [draft] **대시는 ㅇ 서술의 근거를 명시하는 부연 — 같은 내용을 표현만 바꿔 반복하지 않는다**: ㅇ가 판단·조치를 진술하면 대시는 **그 판단이 선 근거**(왜 그렇게 했는가·무엇이 그것을 뒷받침하는가)를 댄다. 실측 예 — `ㅇ (과제 선정) 규정·지침·법령이 분산되어 담당자별 해석이 갈리는 문제를 해소하고자 과제로 선정` / `- 외부 기관 문의 대응 시 부서별·담당자별 답변 차이가 발생해 근거 조문에 기반한 해석 일관성 확보가 시급`: ㅇ는 '선정했다', 대시는 '왜 시급했는가'. **금지 형태** — ㅇ의 명사구를 풀어 쓰기만 한 대시(`ㅇ … RAG 파이프라인으로 구축` / `- RAG 파이프라인은 검색과 생성을 분리한 구조`), ㅇ에 이미 있는 수치를 되풀이하는 대시. 근거로 쓸 것이 없으면 **대시를 달지 말고 ㅇ만 둔다** — 빈 부연은 분량만 늘린다 (근거[관례]: '26.8.4 사용자 확정)

## R059

### '26.9.25 규칙 문구 줄이기 전 원문

- R059 [draft] **향후 계획에서도 주어를 생략한다**: style-guide §4의 주어 생략 관례(`주어(우리 기관·당 팀)는 생략`)를 향후 계획 절에도 그대로 적용한다 — `ICT자격본부 검정관리팀이 외부용역 사업 전환을 추진 중` → `외부용역 사업 전환 추진 중`. 수행 주체를 반드시 드러내야 하면 **주어가 아니라 방향·대상으로 녹인다**(`이음5G사업팀으로 운영 이관`·`검정관리팀 대상 기술 자문`). 보고 문서의 주체는 발신 줄(`< 날짜, 본부 팀 >`)이 이미 밝히고 있으므로 본문에서 반복하면 군더더기가 된다 (근거[관례]: '26.8.4 사용자 확정 — 개선본의 주어 명시 사례는 채택하지 않음)

## R060

### '26.9.25 규칙 문구 줄이기 전 원문

- R060 [draft] **계층 문구를 항목 나열로 채우지 않는다 — 구분자 밀도가 넘으면 표로 배출한다**: R044가 "N단 구조·N중 차단" 같은 **라벨 나열**을 금지했으나, 라벨을 붙이지 않고 항목만 길게 늘어놓는 형태는 그대로 남아 반복 재발했다(실측: '26.8.5 reachy-mini건 붙임 2줄 — `국가기밀·외교국방·국가기반시설 정보통신망 구축, 대규모 정보시스템 구축(10억 이상), 개인정보 100만명 이상 처리, 망 연동, 무선네트워크 시스템 구축 등이 대상`, `웹시스템·화상회의·CCTV·백업·콜센터 등 단일기능 시스템 구축과 방화벽·스위치·라우터·서버·교환기 등 정보보호제품·전산통신장비 도입·교체가 대상에 해당`). 두 문장 모두 **술어가 "대상" 하나뿐이고 나머지는 전부 항목명**이어서, 읽는 사람에게 판단·귀결을 전달하지 못하고 목록을 문장 모양으로 위장한 상태다. **판정 기준(괄호 밖 기준)** — 쉼표 3개 이상 또는 가운뎃점 5개 이상이면 위반. 정상 서술문의 실측 분포는 쉼표 0~2개·가운뎃점 0~4개다. **처리 순서** — ① 나열 항목을 **표로 배출**하고 계층 문구에는 그 표가 말하지 않는 판단만 남긴다(style-guide §3 배출구 원칙·R044 ②와 동일 층위), ② 표를 만들 만큼 항목이 많지 않으면 **대표 항목 2~3개만 남기고 "등"으로 닫되 술어를 세운다**, ③ 열거 자체가 정보의 전부라면 그 항목은 본문이 아니라 붙임 표에 속한다. **적용 예외**: `□ 출처`·`참고문헌`·`근거 목록` 계열 절은 열거가 형식 자체이므로 대상이 아니다(린트가 직전 □ 절 제목으로 자동 판별). 린트 `list-dump`가 기계 검출한다 (근거[실측]: '26.8.5 사용자 확정 — R044 박제 후에도 재발해 검출 기준을 밀도로 성문화)

## R061

### '26.9.25 규칙 문구 줄이기 전 원문

- R061 [export] **※ 단서와 ＊ 각주도 본문 계층과 동일하게 처리한다 — 양쪽 정렬(JUSTIFY) + 2줄 압착 대상**: R032가 □·ㅇ·대시만 JUSTIFY로 돌리고 ※·＊는 왼쪽 정렬로 두었는데, ※·＊도 본문 폭을 꽉 채우는 서술이라 우측 끝이 들쭉날쭉해 계층 문단과 어긋난다. `postprocess_hwpx.py`의 `JUSTIFY_KINDS`와 `LINE_FIT_KINDS`에 `"cham"`·`"star"`를 모두 넣어 정렬·압착을 동일 적용한다. **한때 "＊는 짧은 용어 풀이라 대상 아님"으로 ＊만 제외했으나, 두 줄로 넘어가는 각주에서 ※와 다르게 보이는 비대칭이 생겨 '26.8.7 사용자 지시로 통일했다.** 계층 간 나머지 차이(리터럴 공백 □0·ㅇ1·대시3·※＊5칸, 내어쓰기 22.5·30·37.5·52pt, 크기 15·15·15·13pt)는 양식 실측값이므로 그대로 둔다 — 내어쓰기는 `left=0·intent=-hang` 인코딩으로 둘째 줄이 첫 줄 본문 시작에 정렬되며, SVG 렌더 실측에서 네 계층 모두 오차 2pt 이내로 확인됐다 (근거[실측]: '26.8.7 사용자 지시 + 렌더 실측)

## R062

### '26.9.25 규칙 문구 줄이기 전 원문

- R062 [draft][export] **서술 문구는 렌더 2줄을 넘지 않는다 — 1줄은 35자이며 자간·장평으로 90자까지만 흡수된다**: R050이 "1줄 ≈ 75자"로 적어 78~115자를 2줄로 오인했으나, SVG 렌더 실측 결과 **휴먼명조 15pt·장평 95·본문폭 170mm에서 1줄은 35자**(중앙값 35·최대 47)다. 즉 90자 문구는 3줄로 나가 style-guide §3 철칙(2줄 이내)을 어기고 쪽수를 그만큼 늘린다. **상한은 마커 제외 90자, 하한은 60자**다(R050 흡수 — 1줄로 끝나는 문구는 목차처럼 읽히므로 인접 항목과 통합하거나 근거·수치·귀결을 덧붙여 2줄 밀도로 채운다. 자수를 채우려 같은 말을 반복하는 것은 금지). 상한 초과는 그 이상은 린트 `body-line-overflow`가 검출한다. 변환 단계에서는 `apply_line_fit`(□·ㅇ·대시·※·＊ 전 계층 — R061)이 2줄 초과 문단의 자간을 0→-10까지, 부족하면 장평을 95→90까지 조여 맞춘다 — **자간 -10 미만·장평 90 미만은 가독성 하한이라 사용하지 않으며**, 그래도 3줄인 문단은 `overflow`로 보고해 텍스트를 줄이게 한다(타이포로 못 푸는 분량) (근거[실측]: '26.8.7 사용자 지시 + SVG 렌더 실측)

## R063

### '26.9.25 규칙 문구 줄이기 전 원문

- R063 [export] **표는 페이지를 넘겨도 깨지지 않게 셀 분할·머리행 반복을 강제한다**: 표가 페이지 경계에 걸릴 때 통째로 다음 장으로 밀리거나 머리행 없이 잘려 나가면 표를 읽을 수 없다. `apply_table_pagination`이 모든 표에 **`pageBreak="CELL"`(셀 단위 분할 허용)과 `repeatHeader="1"`(첫 행을 넘긴 페이지에 반복)** 을 보장한다. **여기에 배치 속성 `textWrap="TOP_AND_BOTTOM"`·`textFlow="BOTH_SIDES"`·`lock="0"`를 함께 보장한다('26.9.10 신설)** — 배치가 정해지지 않으면 한글이 표를 본문 흐름 밖 개체로 다뤄 페이지 경계에서 나뉘지 않고 통째로 다음 장으로 밀리므로, `pageBreak="CELL"`만으로는 듣지 않는다. kordoc 산출 표에는 이 셋이 아예 없다(인도본 17건 실측 — 표 246개 중 218개 누락, 붙어 있던 28개는 전부 이식 자산인 머리말 배너·제목 박스였다). 한컴 저장본과 기관 양식 원본은 전 표에 이 값을 달고 있다. 기존 값이 있으면 존중하고 없을 때만 채운다(멱등). 더해 **행 22개 이상이거나 6행 이상이면서 최장 셀이 90자를 넘는 표**는 `oversized`로 보고한다 — 이 경우 대응은 ① 셀 텍스트를 줄여 행 높이를 낮춘다 ② 열을 쪼개 표를 둘로 나눈다 ③ 붙임으로 내린다 순으로 적용하고, 표 자체를 페이지에 맞추려 글자를 줄이지 않는다(표 12pt는 R023 확정값) (근거[추론]: '26.8.7 사용자 지시)

## R064

### '26.9.25 규칙 문구 줄이기 전 원문

- R064 [draft][export] **본문 산식은 계층 서술에 섞지 말고 1행 1열 표(박스)로 내린다**: `- 순절감시간 = (도입 전 소요 − 도입 후 총소요) × 처리 건수로 하며, …`처럼 산식을 서술 문장 안에 넣으면 ① 문구가 길어져 2줄을 넘고 ② 산식이 줄바꿈으로 쪼개져 읽히지 않으며 ③ 위원이 산식만 따로 보기 어렵다. **md 단계에서 1열 GFM 표(헤더 행 하나)로 쓰고** 계층 문구에는 그 산식이 무엇을 뜻하는지만 남긴다. `apply_formula_box`가 1행 1열 표의 문단을 가운데 정렬로 규격화한다. 린트 `formula-inline`이 계층 문구 안의 `=`+`×÷−` 조합을 검출한다 (근거[관례]: '26.8.7 사용자 지시)

## R065

### '26.9.25 규칙 문구 줄이기 전 원문

- R065 [draft] **나열형 수치는 항목명 뒤 괄호에 담는다 — `심사자료(80→2분)` 형태**: `심사자료 80→2분, 항공 실기 90→6분, …`처럼 항목명과 수치를 공백으로만 끊어 늘어놓으면 어디까지가 한 항목인지 경계가 흐려지고, 쉼표가 항목 구분과 수치 구분을 겸해 읽는 사람이 다시 세어야 한다. **항목명 바로 뒤에 괄호를 붙여 수치를 넣으면** 경계가 괄호로 확정되고, 변환 단계에서 `apply_paren_small`(R033·R039)이 괄호 구간을 자동으로 **13pt로 축소**해 항목명 15pt와 시각적 위계까지 생긴다 — 즉 이 표기는 가독성과 서식이 한 번에 붙는 형태다. 적용 대상은 □·ㅇ·대시 서술 안의 **수치 변화(→)·측정치 나열**이며, 표 셀은 열이 이미 경계를 주므로 대상이 아니다. 린트 `metric-unparenthesized`가 계층 문구에서 괄호 밖 →를 검출한다 (근거[관례]: '26.8.7 사용자 지시)

## R066

### '26.9.25 규칙 문구 줄이기 전 원문

- R066 [export] **규약 문서와 코드 상수의 드리프트를 테스트로 막는다 — `tests/test_value_drift.py`**: 같은 값이 rules.md·rules-seed.md·format-profile.kca.md·md-profile.md·hwpx-recipe.md·코드·테스트 **최대 8곳에 손으로 복제**돼 있어(실측: 본문 15pt 8곳·ㅇ→대시 6pt 6곳), 한 곳만 고치면 나머지가 조용히 거짓말을 시작한다. **format-profile.kca.md를 단일 출처로 삼고** 코드 상수가 그와 어긋나면 실패시킨다 — 간격표 pt↔HWPUNIT 정합, 첫 □·블록 경계 간격, 계층별 공백 칸수·내어쓰기, 자간·장평 하한, **시드가 운영 규칙을 빠짐없이 담는지**, 폐기값(75자) 잔존 여부. 값을 바꾸려면 프로파일과 코드를 함께 고쳐야 회귀가 통과한다. 배경: R062 정정 때 시드를 놓쳐 새 설치가 틀린 값을 물려받을 뻔했고, R060은 코드·프로파일·테스트 3곳을 동시에 고쳐야 했다 (근거[실측]: '26.8.7 복제 실태 계측)

## R067

### '26.9.25 규칙 문구 줄이기 전 원문

- R067 [export] **인도 전에 조판 부피를 추정해 보고한다 — `estimate_layout`**: 린트 0·구조검증 0·팩트체크 0을 전부 통과한 문서가 실제로는 15페이지였다. **정적 검사가 "몇 줄로 렌더되는가"를 보지 않기 때문**이다. 후처리가 문단별 예상 줄 수(문자 가중·장평·자간·들여쓰기 반영)와 표 행 높이(셀 텍스트가 열 폭 안에서 접히는 줄 수)로 총 높이를 추정해 `layout.est_pages`·`over_two_lines`로 낸다 — **긴 셀을 가진 표가 쪽수를 지배**하므로 표 행을 평면 상수로 세면 크게 빗나간다(평면 24pt 가정 시 8쪽 vs 셀 기반 11쪽, 실측 13쪽). 오차 15% 수준의 가늠자이므로 정밀 판정이 아니라 **"목표 분량을 넘었는지"를 인도 전에 드러내는 용도**다. 정확한 쪽수는 한글 실조판으로 확인한다 (근거[실측]: '26.8.7 SVG 렌더 13쪽 대비 대조)

## R068

### '26.9.25 규칙 문구 줄이기 전 원문

- R068 [draft][export] **규칙은 10건 늘 때마다 통폐합한다 — `consolidate_rules.py` + 회귀 테스트**: 규칙이 "결과 보고 → 박제"로만 늘고 정리가 없어 두 달에 43건이 쌓였고, [export] 프리플라이트 로드가 6,557토큰에 이르렀다. 길어지면 읽히지 않고, 안 읽히면 지켜지지 않고, 안 지켜지니 같은 실수가 다시 나 또 박제되는 순환이다. **마지막 통합 마커(`<!-- consolidated-at: R0NN -->`) 대비 10건이 늘면 테스트가 실패**하고, 통폐합 후 `--mark`로 갱신해야 통과한다. 함께 검사하는 것 — 파서 정합(다중 태그 규칙 누락 회귀), 죽은 참조, 근거 등급 누락, 피참조 0건 통합 후보. **자동 병합은 하지 않는다**: 실측 결과 "대체됨" 표기 5건 중 넷이 base ← 확장/정정 관계여서(R015←R035, R019←R025, R033←R039, R036←R041·R042) 기계가 지웠다면 base 규칙이 사라져 체계가 무너졌다. 기계 판정의 한계는 **피참조 0건**까지이고 고유 내용의 흡수 여부는 사람이 읽어야 안다 — 스크립트는 후보 제시와 임계 강제까지만 한다 (근거[실측]: '26.8.7 축적 실태 계측 + 통합 후보 5건 건별 참조 문맥 확인)

## R069

### '26.9.25 규칙 문구 줄이기 전 원문

- R069 [draft][export] **검토·감사 서브에이전트는 게이트② 승인 이후에만 스폰한다 — 초안 확정 전 스폰 금지**: 초안이 바뀌면 그 판본에 걸린 검토 결과가 통째로 무효화된다. 실측 — 초안 6판을 거친 건에서 검토 에이전트 2종(외부위원 관점·정합성)이 **28만 토큰**을 쓰고 중간 판본에 걸려 재사용 불가였다. 게이트② 전의 스타일 감사는 **메인 컨텍스트 자가감사**(에이전트 0개)로 하고, 승인 후 최종본에 한해 교차 감사를 선택적으로 붙인다. 같은 이유로 **hwpx 변환도 초안 확정 후 1회**가 원칙이다 — `generate_document`는 마크다운 전문을 매번 실어야 해 왕복 1회가 약 1.8만 토큰이고, 재변환 4회에 7만 토큰이 나갔다 (근거[실측]: '26.8.7 세션 토큰 계측 — 서브에이전트가 총 소비의 65%)

## R070

### '26.9.25 규칙 문구 줄이기 전 원문

- R070 [export] **첫 실행 이탈을 막는 자가진단을 제공한다 — `/report-doctor` + `doctor.py`**: kordoc MCP가 연결되지 않으면 파이프라인이 ③ draft까지 정상 동작하고 **④ export에서 hwpx 없이 md만 남는데**, 처음 쓰는 사람은 이를 실패로 인지하지 못한 채 결과를 받는다. README 증상 표는 수동 대조라 실패 지점을 스스로 찾아야 했다. 스크립트가 Python·npx·스크립트 8종·규약 문서·설정 경로·**인증 방식(구독 vs API 키 — 과금이 갈리는 지점)**·규칙 체계를 순서대로 찍고 exit 0/1/2로 심각도를 낸다. **MCP 연결만은 스크립트가 확인할 수 없어**(모델만 호출 가능) 커맨드가 모델에게 `parse_document` 시험 호출을 지시한다 (근거[관례]: '26.8.7 배포 장벽 분석 — 설치는 2줄이나 첫 실행 실패 대비가 0이었다)

## R071

### '26.9.25 규칙 문구 줄이기 전 원문

- R071 [draft] **게이트⓪은 문서의 형태가 아니라 산출물 요구를 뽑는다 — "이 보고서가 반드시 뱉어야 하는 것"을 묻는다**: 종전 4문항(유형·분량·논지·맺음말)은 전부 *형태*만 물었고 *무엇이 나와야 하는가*를 묻지 않았다. 그 결과 한 건에서 초안이 **여섯 번 반려**됐고 실제 요구("실측값이 나오는 산식 + 과제별 적용")는 3턴 만에 드러났다. 기록된 반려 — "누가·어디에 등록하나만 다루고 어떻게 재나가 빠져 두루뭉술", "산식을 일반형으로만 적고 과제별로 각 항이 무엇으로 채워지는지를 안 써서 너무 추상적", "개정 대조 틀로 썼는데 독립 최종안을 원했다". **형태를 아무리 정확히 물어도 내용 요구는 안 나온다.** 문항 3을 산출물 요구(판단·결정 / 수치·산출식 / 절차·기준 / 현황·동향, 복수 선택)로 교체하고 그 답을 총괄표 설계에 직결한다. 맺음말은 유형이 정하고(style-guide §7) 표·도식은 게이트①이 정하므로 게이트⓪에서 빼 자리를 만든다. **기존 문서가 `research/provided/`에 있으면 승계 관계(개정본 vs 독립 최종안)를 함께 확인**한다 — 착수 후 되돌리기가 가장 비싼 결정이다 (근거[실측]: '26.8.7 반려 6건 lessons 대조)

## R074

### '26.9.25 규칙 문구 줄이기 전 원문

- R074 [draft] **집필 전 `style-guide.md`를 반드시 읽고, 초안은 `lint_md_profile.py` exit 0 그리고 `audit_style.py` violations 0일 때만 확정한다 — 생성기 자체 린터의 문체 경고는 style-guide 하위다**(R082 흡수 '26.9.24): 린트는 마크다운 문법·계층·밀도만 보고 종결어미·절 제목·문서 제목은 스타일 감사가 2차 방어(md-profile §4)인데, 그 방어를 사람 판단에 맡겨 초안 전체가 `~함/~됨`으로 나간 일('26.8.15)과, kordoc의 문체 경고('~다' 서술형 0%)를 따라 `~임/~함`으로 "교정"해 §4 철칙을 정면 위반한 재발('26.9.10)이 있었다 — 근본 원인은 style-guide를 읽지 않고 md-profile·rules만 들고 집필해 **측정하기 쉬운 것만 측정**한 것이다. **처리** — ① `[draft]` 프리플라이트에 style-guide 로드 필수, ② lint 직후 같은 자리에서 `audit_style.py`를 돌려 exit 1이면 수정 후 재실행(warning은 사람 판단), ③ kordoc 문체·표기법 경고(`DA_ENDING`·`DATE_2DIGIT_YR`·`MONEY_CHEONWON`·`AI_EM_DASH`)는 전부 style-guide 하위이며 이를 근거로 원문을 고치지 않는다 (근거[실측]: '26.8.15 위반본 재검사 — 종결어미 58건·제목 접미 1건·절 번호 8건 · '26.9.10 kca-ax-invest 재발)

## R075

### '26.9.25 규칙 문구 줄이기 전 원문

- R075 [draft] **조문은 한국식 전체 표기로 쓰고 `§` 기호를 쓰지 않는다**: `영 §123②2`가 아니라 **`영 제123조제2항제2호`**로 적는다. `§`는 해외 문헌 표기이고 기관 문서 관례가 아니며, 항·호를 원문자(①②)로 축약하면 결재선에서 조문을 되짚기 어렵다. 범위는 물결이 아니라 `제10조~제13조`처럼 조 단위를 모두 적고, 가지 조문은 `제123조제1항제1호의7` 형태로 쓴다. 표 셀에서도 축약하지 않는다 — 근거 조문 열이 길어지는 것은 감수하고 정확도를 택한다. `audit_style.py`의 `article-symbol`이 검출한다 (근거[관례]: '26.8.15 사용자 지시)

## R076

### '26.9.25 규칙 문구 줄이기 전 원문

- R076 [draft] **절은 서사 순서로 배치한다 — 배경 → 현황·문제 → 과제 → 효과 → 방법 → 향후계획**: '26.8.15 3기관 협의자료에서 「추진 방법」이 「추진 과제(안)」보다 앞에 놓여, 무엇을 할지 말하기 전에 어떻게 할지부터 설명하는 순서가 됐다. 사용자 지적은 "두서없이 왔다 갔다 함"이었다. style-guide §7이 전형 흐름(배경→내용·현황→판단→향후계획)을 적어 두었으나 **절 제목 어휘 풀만 있고 순서 검사가 없어** 역전이 그대로 통과했다. 판정 기준 — 어휘 풀 절 제목에 서사 순위를 부여하고(배경 1 · 현황 2 · 과제·내용 3 · 효과 4 · 방법·체계 5 · 향후계획 6), 앞 순위 절이 뒤 순위 절보다 **뒤에 오면 역전**으로 본다. 같은 순위끼리는 순서를 따지지 않는다. **절을 늘리기 전에 합칠 수 있는지 먼저 본다** — 같은 이야기가 여러 절에 흩어지면 순서가 맞아도 두서없이 읽힌다(실측: 8절→6절로 합치자 흐름이 잡혔다). `audit_style.py`의 `section-order`가 검출한다 (근거[실측]: '26.8.15 사용자 지적)

## R077

### '26.9.25 규칙 문구 줄이기 전 원문

- R077 [draft] **본문에 조문 번호를 나열하지 않는다 — 근거 법령은 붙임 대조표로 배출한다**: '26.8.15 3기관 협의자료 본문에 `제123조제2항제2호` 형태의 조문 인용이 20개소 넘게 박혀 사용자가 "법령·시행령 지침만 나열함"으로 지적했다. R026이 이미 "본문=두괄식 결정·요지, 붙임=검토 근거 상세"를 규정했으나 **조문이 그 '근거'에 해당한다는 연결이 없어** 본문에 그대로 남았다. 조문은 **근거이지 내용이 아니다** — 본문은 업무 언어로 무슨 일이 일어나는지 쓰고(`하나의 무선국을 두고 개설허가와 검사를 서로 다른 기관이 기록`), 조문은 붙임에 **「본문 내용별 근거 법령」 2열 대조표**로 모은다. **예외**: 그 조문 자체가 보고의 주제인 경우(법령 개정 검토·규제 해석)와 시행일·제명처럼 조문 번호 없이는 특정이 안 되는 경우. `audit_style.py`의 `article-in-body`가 본문 조문 밀도를 검출한다 (근거[실측]: '26.8.15 사용자 지적)

## R078

### '26.9.25 규칙 문구 줄이기 전 원문

- R078 [draft] **상위 계획서가 있으면 그 문체를 실측해 맞춘다 — 판단을 앞세우고 근거를 연결어로 잇는 두괄식 한 문장으로 쓴다**: '26.9.6 3기관 협의자료가 "너무 딱딱하다, 나열식이 아닌 두괄식 서술형으로"라는 반려를 받았다. 실측하니 계층 문구 39개 중 `~상태` 종결이 8회 반복되고 연결어는 4종 8회뿐이었다 — 사실을 관찰 서술로 늘어놓은 결과다. 반면 상위 계획서(협의체 추진안)는 `~을 통해 ~를 위한 ~ 제정`·`~에 따라 ~ 필요`처럼 **목적·수단·귀결을 연결어로 이어 한 흐름**으로 쓰고 종결도 제정·미비·필요·논의로 갈린다. **절차** — ① 상위 계획서·선행 보고서의 계층 문구를 뽑아 종결어 분포와 연결어 종류를 센다, ② 초안을 같은 방식으로 세어 대조한다, ③ ㅇ는 판단·결론을 앞세우고 근거를 연결어로 이어 붙인다. **결함 신호**: 같은 관찰 종결(`~상태`·`~수준`)이 3회 넘게 반복되거나 연결어가 5종 미만이면 문체가 어긋난 것이다(절 단위 연결어 중복은 린트 `connective-repeat`가 검출) (근거[실측]: '26.9.6 사용자 반려 — 종결 8회·연결어 4종 → 재작성 후 종결 12종·연결어 16종)

## R079

### '26.9.25 규칙 문구 줄이기 전 원문

- R079 [draft] **본문은 수신자의 업무 언어로 쓰고 기술 자료는 붙임으로 분기한다 — 추상 진단은 사례 1건으로 대체한다**: 같은 건에서 "내용이 너무 어렵고 직관적이지 않아 이해당사자 설명이 어렵다"·"표만 나열이고 서술이 적다"는 반려가 잇따랐다. 실측 — 본문에 설명 없는 시스템 약어 9종 46회, DB 타입 표기 15회(`VARCHAR2`·`STRING(20)`), 컬럼·테이블 같은 개발 용어 16회가 남아 있었고 본문 표가 11개였다. **처리 3가지** — ① **용어**: 물리 컬럼명·DB 타입·시스템 약어를 본문에서 쓰지 않고 업무 언어로 바꾼다(`VARCHAR2(15)`→`15자리`, `NUMBER(12,6)`→`소수점 좌표`), ② **사례**: 집계·대조 결과만 늘어놓지 말고 업무 한 건이 어떻게 흐르는지 보여주는 표 1개를 앞세운다(기지국 한 곳의 허가→검사→측정→감시 4단계), ③ **분기**: 본문 표는 판단에 쓰이는 3개 이내로 두고 나머지는 붙임으로 내리며 표마다 그 표가 말하는 바를 ※나 대시로 한 줄 붙인다. 어려운 표기를 걷어내면 쪽수도 함께 준다(8.5p→5.7p 실측) (근거[실측]: '26.9.6 사용자 반려 3회 — 약어 46회·본문 표 11개 → 약어 0회·본문 표 3개)

## R080

### '26.9.25 규칙 문구 줄이기 전 원문

- R080 [draft] **같은 종결 명사를 절 안에서 되풀이하지 않는다 — 상태명사로 닫지 말고 판단·행위 명사로 끝낸다**: '26.9.6 3기관 협의자료가 "실정·상황 등이 중복으로 나온다"는 반려를 받았다. 실측하니 계층 문구 39개의 종결이 7종뿐이었고 절 단위로 `확인` 3회·`실정` 2회·`필요` 2회가 겹쳤으며, 문서 전체로도 `필요` 6회·`확인` 4회였다. 원인은 **상태명사 종결**(실정·상황·구조·한계·부재 9개)로 문장을 닫은 습관이다 — style-guide §4의 표준은 동작명사(구축·추진·마련·확보·검토)인데 상태명사로 닫으면 판단이 아니라 관찰만 남아 문서가 딱딱해진다. **처리** — ① 절 안에서 같은 종결이 겹치면 뜻이 가까운 행위 명사로 갈아 쓴다(`곤란`→`단절`, `실정`→`대조`, `선행`→`합의`), ② `※ 붙임 N 참조`류 안내가 3개를 넘으면 가리키는 붙임이 겹치는 것부터 지운다, ③ 갈아 쓸 말이 없으면 그 문구는 인접 항목과 통합한다. `audit_style.py`의 `ending-repeat`가 절 단위 3회 이상을 violation, 2회를 warning으로 검출한다 (근거[실측]: '26.9.6 사용자 반려 — 종결 7종·절 내 중복 3개 절 → 재작성 후 33종·중복 0)

## R084

### '26.9.25 규칙 문구 줄이기 전 원문

- R084 [export] **제목 박스 판정은 '첫 □ 이전 표 전부'가 아니라 '첫 □ 이전의 1열·유텍스트 표 중 첫 번째' 하나다**: kordoc 보고서 산출물은 첫 □ 앞 구간에 제목 박스뿐 아니라 요약 박스(1행 1열 #DFE6F7)·문서정보표(2열)·장 배너(3열)를 함께 싣는다('26.9.10 실측 — 첫 □ 앞 표 5개가 전부 제목 박스로 판정). 종전 판정에서 `apply_title_box_form`이 요약 박스를 제목 박스로 개조해 음영을 지우고 밴드로 감쌌고, `apply_form_sizes`는 요약 문장을 20pt로 부풀렸다. 제목 박스는 문서에 하나뿐이므로 `_title_box()`가 단수로 판정하고 `apply_title_box_borderless`도 같은 판정을 쓴다. **제목 행은 텍스트가 있는 첫 행**이며 `report_info`가 붙은 2행 산출물은 제목 행만 감싸 4행이 된다(부가 행 불변). 제목 행 위에 행이 있으면 이미 원형이라 무동작 — 채움 유무로 판정하지 않는다(밴드가 채움을 잃은 상태를 행 삽입으로 고치면 행이 불어난다. R022 '행 삭제 금지'의 대칭). 건너뛴 사유는 summary `skipped`로 남긴다 — '대상 없음'과 '조용한 실패'가 구분되지 않던 결함의 대책 (근거[실측]: '26.9.10 kordoc 산출물 3종 재현 — 기본·요약박스·report_info)

## R085

### '26.9.25 규칙 문구 줄이기 전 원문

- R085 [export] **인도본은 현재 초안에서 나온 것이어야 한다 — 개정 건은 변환 전 `validate_hwpx.py freshness`로 초안↔prepared 대응을 먼저 확인한다**: 작업폴더가 인터페이스인데 산출물끼리 어느 초안에서 나왔는지를 기록하지 않아, 초안을 고치고 재변환을 안 하면 인도본이 조용히 낡는다. 사용자에게는 '초안대로 안 담겼다'로 보인다. 실측('26.9.10 인도 4건 전수): 3건에서 `20_draft.md`가 `40_prepared.md`보다 최신이었고(최대 12일) 본문 수치까지 달랐다 — 초안 `KCA·CRMS 16건` vs 인도본 `17건`, 초안 「경로가 열린 것은 셋」 vs 인도본 「적합성평가 인증 조회 하나뿐」. **변환 구간 자체는 무손실이다**(같은 4건에서 `43_convert_input` → hwpx → 되읽기 문장 대조 소실 0) — 증상의 실체는 변환 손실이 아니라 판 어긋남이다. 처리 — 새 산출물을 만들지 않고 **현재 초안을 다시 prep해** 저장된 prepared와 대조한다(`prepared-stale`이면 prep부터 재실행). 아울러 `compare`가 개수·수치·마크다운 잔재만 보던 것을 **문장 단위 대조(`content-dropped`)**로 넓혔다 — 문장이 통째로 갈려도 총량과 수치가 맞으면 지나갔다. 대조 전 되읽기 이스케이프(`\~`)·선두 기호 매핑(`- `→`ㅇ `·`□ `)·수치 표기는 정규화한다(안 하면 정상 변환이 전량 오탐된다) (근거[실측]: '26.9.10 인도 4건 전수 대조)

## R088

### '26.9.25 규칙 문구 줄이기 전 원문

- R088 [export] **그림은 픽셀을 줄이지 않는다 — 표시 크기·배치는 후처리가 정한다**: kordoc은 그림 크기를 1px = 75 HU(96dpi)로 잡고 종전 `check_image_size.py`도 96dpi 고정 판정으로 축소를 권해, 1257px 원본을 556px로 줄인 인도본이 147×90mm·실효 96dpi로 뭉개졌다('26.8.24 1814건 r05). **처리** — ① `check_image_size.py`는 크기 초과가 아니라 실효 해상도(150dpi)를 판정한다, ② 도식·그림은 `figures/{슬러그}.json` 명세를 `render_diagram.py`가 `{판본폴더}/figures/fig{NN}`(도식 300dpi·research 원본 그대로, 영문 파일명 — kordoc은 하위 폴더·한글명을 넣지 않는다)으로 모아 `image_dir`로 임베드한다, ③ `apply_figure_fit`이 원본 해상도로 잰 크기를 본문 폭 − 1mm × 90mm에 비율 유지로 넣고(키우지 않는다) 그림 문단을 가운데·줄간격 100%(R041), 위 캡션을 표 캡션 서식·다음 문단과 함께 두며 그림 뒤 ㅇ·대시·표(산식 박스)는 6pt로 띄운다, ④ 그림 문단은 `figure` 종류다 — 빈 줄로 보면 캡션 내장이 그림을 지운다. R006(1/3쪽 상자·출처 캡션)은 유효 (근거[실측]: '26.9.24 A/B — 같은 원본 147.3×90mm 217dpi vs 축소본 96dpi, 요약본 도식 4종 시험 변환에서 그림 삭제·한글명 누락을 재현한 뒤 해소)

## R089

### '26.9.25 규칙 문구 줄이기 전 원문

- R089 [export] **흐름·비교·체계·일정 도식은 그림이 아니라 한글 표로 넣는다 — `diagram_table.py`가 같은 명세를 좌표 격자로 조립한다**: 그림(PNG)으로 넣은 도식은 한글에서 글자를 고칠 수 없고 확대하면 흐려지며, 같은 내용이 표보다 12~22mm 높다('26.9.25 시험 변환: 흐름 62.7→49.1mm·체계 77.8→56.7mm). 카드 = 테두리 셀(머리 음영 + 본문), 카드 사이 = 테두리 없는 셀, 체계 연결선 = 선만 있는 빈 셀, **화살표 머리 = 셀 안 삼각형 도형** — 기관 도식 Pool 원본이 화살표를 표 셀 안 도형(hp:polygon 17개, 셀 대각선 0건)으로 그리는 방식을 따른다(셀 테두리·대각선만으로는 속이 빈 선 화살표밖에 안 된다, '26.9.25 사용자 지시 — 비교도 '착시 차단' 띠를 화살표 모양으로). 실행은 이미지 주입 뒤·후처리 앞(recipe §3-1): 후처리 캡션 내장(R034)이 `[ 제목 ]` 줄을 표 캡션으로 넣고, 도식 표(첫 셀 이름 `__harness_figure`)는 열 폭·행 높이·셀 정렬·셀 12pt·병합 음영·셀 단위 쪽 나눔·폭 축소에서 빠지며 쪽에서 나누지 않는다(pageBreak=NONE). 리뷰(게이트②)도 같은 격자로 그려 모양·높이·쪽 추정이 인도본과 같다. **선은 셀마다 준 값만 긋는다** — 카드형(흐름·비교·체계)의 빈 칸(간격·화살표 열)과 표 바깥 테두리에는 선이 없고, 리뷰 화면도 일반 표의 위·아래 굵은 선을 상속하지 않는다('26.9.25 게이트② 지적 — 비교도 '착시 차단' 열 위·아래까지 선이 이어져 표가 어색). 명세에 `"render": "image"`를 주거나 4유형 밖이면 그림으로 남긴다. compare는 그림 감소분만큼 표가 늘면 합으로 대조한다 (근거[실측]: '26.9.25 시험 변환 trial_r04 — 한글에서 표 캡션·도식 표 확인)

## R090

### '26.9.25 규칙 문구 줄이기 전 원문

- R090 [draft][export] **도식 색은 역할로 정하고 명세에는 역할만 쓴다 — 글자 4.5:1·그래픽 3:1·강조 1곳·색만으로 전하지 않기**: 값의 단일 출처는 format-profile §8(test_value_drift가 코드와 대조하고 명도대비·흑백 휘도 차를 계산으로 검사). 실측 결함 2건을 고친 규칙이다 — 청색 #0080C0 위 흰 글자는 4.33:1로 기준 미달이라 글자를 얹는 채움은 #006BA6(5.75:1), 비교도 종전·이번 머리는 휘도 차 0.011로 흑백 인쇄에서 같아 보여 종전 #F2F2F2 + 점선 테두리·이번 #BDD7EE(차 0.23). 강조는 `"tone": "accent"` 한 곳만(주황 #B34700 머리 + 굵은 #D55E00 테두리 — 두 번째 채널, 2곳 이상이면 `check_palette_use`가 거부), 일정 상태는 `"status"`로 같은 청색 계열 명도 3단계(완료·진행·예정) + 라벨 — 초록·청색은 휘도 0.18·0.19로 흑백에서 겹친다. 기준은 범정부 디자인시스템(KRDS) 색 원칙(강조 5% 이하·색만으로 정보 전달 금지·명도대비) (근거[실측]: '26.9.25 사용자 지시 — 대비·강조 배색 추천과 도식 반영, KRDS 공식 토큰 대조)

## R091

### '26.9.25 규칙 문구 줄이기 전 원문

- R091 [draft] **본문은 알기 쉬운 말로, 절 첫 ㅇ은 결론으로 — `audit_style.py` 쉬운 말 경고 6종을 고치고 되말하기 점검을 1회 한다**: 국어기본법 제14조(공문서는 일반 국민이 알기 쉬운 용어와 문장)와 국립국어원 「쉬운 공문서 쓰기 길잡이」(2022 — 중요도 높은 사항 먼저·한 문장에 정보 하나·명사 나열 금지·다듬은 말·줄임말 풀이·독자 처지 검토)를 경고로 결정론화했다: `plain-word`(style-guide §11 용어표)·`abbr-unexplained`·`clause-chain`(이음 3개 이상)·`noun-chain`(명사 5개 이상)·`history-narration`(경위 표지어)·`lead-not-conclusion`(절 첫 ㅇ가 배경으로 시작). R026·R078·R079가 이미 있었지만 서술 규칙뿐이라 집필 중 잊으면 막을 장치가 없었다 — 1127 요약본 실측 본문 평균 78자·'산출' 13회·'로그' 9회·경위 표지 4문장. 글자 수 기준은 길잡이에 없어 쓰지 않는다. 고친 뒤 `--skeleton`(□ 제목·절 첫 ㅇ)으로 '무엇을·왜·언제'를 되말해 어긋난 절만 고친다(길잡이 56쪽 환언 검사). 붙임은 검사하지 않는다(기술 사양의 자리, R079) (근거[관례]: '26.9.25 사용자 지시 — 기술적·경위 서술을 두괄식 핵심으로)

## R092

### '26.9.25 규칙 문구 줄이기 전 원문

- R092 [research][analyze][draft] **외부 자료 인용은 본문 문구 + 시각화 + 출처 ※ 줄 세 가지로 넣는다 — 분석 단계에서 '인용 재료 목록'을 만들고 이용 조건을 확인한다**: 인용이 필요한 수치·원문·구조를 05_analysis.md 목록(출처 쪽·기준시점·이용 조건·쓰임)으로 뽑아 게이트①에서 승인받고, 성격에 따라 인용 차트(`chart` — 시계열·비교, `source` 필수)·재작도 도식·원문 인용 블록·표 재구성으로 시각화한다. **도식만 두지 않고 본문 문구를 반드시 함께 쓰며, 그 문구에도 R091을 똑같이 적용한다**. 출처 줄은 `※ 자료: 기관, 「자료명」(연도), 쪽` + 기준시점, 재가공은 `재구성·재계산`. 이용 조건: 저작권법 제28조(정당한 범위·공정한 관행)·제37조(출처 명시 의무)·제24조의2(국가·지자체 공표 저작물 자유이용), 공공누리 제3·4유형(변경금지)은 재작도하지 않는다. 도식·차트 안 수치도 `validate_hwpx.py numbers`가 대조한다. **분량(쪽수)을 이유로 시각화를 선택지에서 빼지 않는다** — 넣을지는 사용자가 정한다. 출처 줄이 빠진 외부 인용 ※는 `audit_style.py`가 `source-line-missing`으로 경고하고, 출처 줄 자체는 약어·종결 검사에서 뺀다 (근거[관례]: '26.9.25 사용자 지시 — 인용 도식화와 함께 문구를 본문에 포함 / 근거[실측]: '26.9.25 1127 검증 — 분량을 이유로 차트를 선택지에 올리지 않아 인용 5건이 모두 ※ 문구로만 들어가고 규격 출처 줄 0건)

## R093

### '26.9.25 규칙 문구 줄이기 전 원문

- R093 [draft][export] **도식 카드 문장은 서술형 공문서 문장으로 쓰고, 넘친 줄은 첫 줄 글자에 맞춰 내어 쓴다**: 카드 본문 항목은 무엇을 어떻게 하는지를 목적어와 동작명사로 끝맺는다(`기능 13분류 중 주기능 1개로 과제 배정`). 칸 이름·필드 목록 같은 기술 사양을 늘어놓지 않고 무엇을 기록하고 어떻게 받는지로 쓴다(`처리 번호·접수~완료 시각 5개…10칸` → `AI가 처리한 업무를 1건씩 같은 양식에 기록`). 숫자로 끝나는 나열(`과제당 3개, 18건 54개`)·등호 정의식(`처리시간 = …`)은 쓰지 않고 산식은 도식 아래 1열 표로 둔다(R064) — 초안의 `도해:`가 가리키는 명세를 `audit_style.py`가 `diagram-fragment`·`diagram-formula`로 경고한다. 내어쓰기는 `diagram_table.py`가 표 도식과 리뷰 화면에 함께 적용한다(한글: left 0·intent -1500 + 부호 뒤 탭 + 내어쓰기 자동 탭, 값은 format-profile §8-1) — 공백으로 흉내 내지 않는다 (근거[실측]: '26.9.25 1127 요약본 게이트② f11 등호 정의식 비교도·f12 숫자 나열 흐름도와 둘째 줄이 부호 아래로 밀린 카드·f14 기술 사양만 늘어놓은 수집 경로 체계도)

## R094

### '26.9.25 규칙 문구 줄이기 전 원문

- R094 [draft] **보고서는 목적·결론부터 세운다 — 게이트⓪ 끝에 `## 보고 설계`(00_context), 게이트① 전에 `## 설계 점검`(10_outline)을 쓰고 `check_craft.py`로 칸을 확인한다**: 기준은 `references/report-craft.md`(목적 상기·결재자가 내릴 결정·예상 결론·상황 전제 / 제목=결론·피라미드·겹치지 않는 근거 2~4개·절별 So What·구성요소 대조·상위 계획 요건 1:1 / 흔한 실수 점검표). 상황 전제(결정 전/후·대응 쟁점 원문·내부 상위 계획)는 05로 불명확할 때만 묻는다. 칸의 존재만 결정론으로 보고 칸 내용의 품질은 게이트에서 사용자가 본다. 일반 개조식 교재의 `~함·~임` 종결 권고는 따르지 않는다 — 기관 철칙(style-guide §4)이 이긴다 (근거[실측]: '26.8.5 발주 완료가 게이트②에서·유관부서 쟁점 6건이 인도 뒤에, '26.9.15 경영평가 관리계획이 조사 3단위 뒤에 드러나 논지 재작성 / 근거[관례]: claudian 위키 '26.7.15 보고서 작성법 수집 8건·개념 4종, '26.9.25 사용자 지시 — 공공 보고서 작성법을 스킬로 만들어 context·outline·draft에 적용)
