# rules — 회귀 방지 체크리스트 (승격분만, 근거 건 링크)
<!-- consolidated-at: R068 -->

> **표기 규약**: 규칙 앞머리의 `**[대체됨 → R0NN]**`·`**[… 정정됨 → R0NN]**`는 그 규칙이 뒤 규칙에 의해
> 갈음됐다는 뜻이다 — **집필·변환 시 따를 값은 화살표 뒤 규칙**이고, 앞 규칙은 판단 근거와 이력을 남기려고
> 보존한다. 새 규칙을 덧붙일 때 대체된 구 규칙에 이 표기를 달지 않으면 틀린 값이 계속 살아 움직인다
> (실제 사례: R050의 '1줄 ≈ 75자'가 정정 없이 남아 문구를 3~4줄로 만들고 쪽수를 2배로 불렸다 — '26.8.7).

> 배포 시드 — `state_dir/rules.md`가 없을 때 SKILL.md §0-4가 1회 복사하는 원본이다.
> 실제 운영 축적분(R001~R071, 2026-08-09 기준) 전량을 승계했다. 이후 승격은 운영
> `state_dir/rules.md`에 먼저 반영되고, 배포 전 이 파일로 동기화한다.
> 동기화 누락(번호·본문 드리프트)은 `test_value_drift.py::test_seed_carries_every_operational_rule`이,
> 룰 번호 중복은 `test_references_consistency.py`의 가드가 차단한다 — 신규 설치자가 낡은
> 규칙만 받거나 번호가 겹친 규칙을 받는 사고를 막기 위한 장치다.
- R001 [draft][export] 서술 문장 안에 마크다운 기호(-, *)를 남기지 않는다 — lint·QA 기계 검출 (근거[관례]: 과거 에이전트 반복 시행착오)
- R002 [draft] 사실(지적·조사결과)과 주장(건의·제안)은 절을 분리한다 (근거[관례]: 260714 PIMS건)
- R003 [draft] 모든 수치·인용에 확정/추정/경계 태깅과 출처를 남긴다
- R004 [export] prep 정규화는 내용 무손실 — 지문 불일치 시 중단 (근거[관례]: 레거시 개행 삼킴 버그)
- R005 [draft] 표는 6열 이하·병합 금지, 나열 3개+속성 2개↑는 표 전환
- R006 [export] 이미지는 170×90mm 이내만 주입, 캡션에 출처 필수
- R007 [export] kordoc generate 입력은 리터럴 기호가 아니라 리스트 깊이 문법(-, 2칸, 4칸 = □/ㅇ/-)으로 변환 후 전달한다 (근거[관례]: 20260722건 계층 평탄화)
- R008 [export] generate_document 호출 시 KCA 프로파일 파라미터를 반드시 전달한다 — fonts(heading=HY헤드라인M, body=휴먼명조, ref=맑은고딕, table=맑은 고딕)·sizes(dae15·cham13·table12·bodyTitle20)·bullet2=ㅇ(U+3147, 한글 이응 — U+110B 초성·U+25CB 원 기호 아님)·body_title_box·line_spacing160 (근거[관례]: 20260722건 양식 미적용 · '26.8.10 format-profile.kca.md §71 정합 정정 — U+25CB 오생성 사례)
- R009 [draft][export] 붙임은 3열 배너 표(| 붙임 1 | | 제목 |)로 작성하고 변환까지 유지한다 (근거[관례]: 20260722건, style-guide §8)
- R010 [export] 제목은 h1(`# `)로 변환 입력에 전달하고, 폰트 검증은 선언이 아니라 문단별 charPr '사용'을 확인한다 (근거[관례]: 20260722건 제목 본문체 출력)
- R011 [export] ＊ 각주 문단은 생성 후 참고 charPr로 zip 패치한다 — kordoc은 ※만 인식 (근거[관례]: 20260722건)
- R012 [export] 발신 줄(< 날짜, 부서 >)은 <right> 래핑으로 우측 정렬한다 (근거[관례]: 양식 바이너리 실측, 20260722건)
- R013 [export] 계층 간격은 postprocess_hwpx.py --spacing으로 양식 실측값(발신→□8pt/□→ㅇ6pt/ㅇ→ㅇ6pt/대시→ㅇ6pt/ㅇ→-6pt/-→＊3pt/블록15pt/＊→표10pt)을 재현한다 (근거[실측]: 동일)
- R014 [export] 간격 검증은 스페이서 개수가 아니라 **실효 간격**(스페이서+paraPr 여백 합산 pt)으로 한다 — postprocess가 콘텐츠 paraPr prev/next를 0화해 스페이서 단독 체계 유지 (근거[관례]: 20260722건 이중 간격)
- R015 [export] **[예외 → R035 / 캡션 → R034]** **('26.7.28 사용자 확정으로 정정)** 본문 콘텐츠 표는 **우측 정렬**(표 래퍼 문단 RIGHT paraPr), 캡션은 별도 문단이 아니라 **표 caption 내장**(→R034). 예외: 제목 박스 CENTER 유지, 붙임·참고 배너 표는 좌측(→R035). postprocess `apply_table_alignment`가 배정 (구 규칙 '캡션·표 가운데 정렬'은 폐기)
- R016 [draft][export] ㅇ 괄호 리드는 볼드(**(한 계)**) — 코퍼스 관례 (근거[관례]: 20260722건 사용자 확정)
- R017 [export] 제목 박스 표는 무테두리·배경 보존(원본 borderFill 복제 변형 — 테두리만 NONE, 그라데이션 유지) — 양식 실측: 제목 셀 4변 type=NONE (근거[관례]: 동일)
- R018 [export] 발신 줄 글자 크기 12pt — 양식 바이너리 실측 확정 (근거[실측]: 동일)
- R019 [export] **[정렬 확장 → R061 / ☞ 확장 → R025]** 계층 시작은 원래 폰트의 리터럴 띄어쓰기(□0·ㅇ1·대시3·＊※5칸), 줄바꿈 하위는 자동 들여쓰기 — 내어쓰기 폭은 **그 폰트 글자 단위**로 산정: □ 1.5글자(22.5pt)·ㅇ 2글자(30pt)·대시 2.5글자(37.5pt)·＊※ 4글자(52pt), left=0·intent=-글자폭 (한글 내어쓰기 의미론: 첫 줄=left=0 → 띄어쓰기만이 첫 줄 위치 결정, 랩 줄=|intent| 위치. left에 값을 넣으면 첫 줄이 밀림 — 금지). pt는 폰트 크기가 아니라 글자폭 환산 거리 — 리포트에는 글자 단위 병기. 표 셀 텍스트 CENTER, 표-문단 간격 캡션→표 3pt·표→※ 3pt·문단→캡션 6pt (근거[관례]: 20260722건 사용자 확정 — --star-indent 훅은 폐기)
- R020 [export] 편집용지 여백은 양식 규격 강제 — 좌우20·위10·아래15·머리말15·꼬리말10mm (kordoc preset 위 15mm 결함 후처리, 근거: 20260722건 제목표 상단 여백)
- R021 [export] compare markdown-leftover 중 볼드(**)·h1 제목·대시 선행공백 트림은 파서의 정상 재현 — hwpx XML 실물 검증(리터럴 기호 0건·charPr 실사용 확인)으로 무해 판정 후 진행한다 (근거[관례]: 20260722건 2회 관찰)
- R022 [export] 제목표 상단여백 미세 제거는 앵커 문단 줄간격 100% 치환 + 표 outMargin top 0 — 단 **이 두 치환만으로는 사용자가 보는 상단 여백이 사라지지 않는다('26.7.28 3차 조사 정정)**: 치환 적용본의 제목표 위치는 한컴 저장 실무 보고서와 0.2mm 이내 일치(제목 텍스트 상단 28.1mm vs 실무본 27.9mm, 실무본 lineseg 캐시 vertpos=0 교차검증)했으므로 기하 여백은 이미 0이고, 잔존 '여백'의 실체는 **머리말 부재**(→R030). **상단 얇은 행(3.8pt)은 양식 원형의 배경 밴드이므로 행 삭제 금지**(행 삭제 시 그라데이션 소실 회귀, 20260724건 2회 왕복으로 확정). 밴드 채움 실측 정정: kordoc 산출본은 상단 행 단색 #0080C0·하단 행 RADIAL #0080C0→#3CBFFF이고, 양식·실무본 원형은 상·하단 모두 LINEAR 90° #3057B9↔#DFE6F7(상단 진→연, 하단 연→진) — 색·방향 차이는 미고침 관찰 사실 (근거[관례]: 20260724건 사용자 확정 + 20260722건 양식 OLE 실측 + 20260728건 0839 hwpx·260331 실무본 XML 실측). **'26.7.28 4차 재검증(앵커 문단 가설)**: 사용자 관찰 "문단에 [표1]로 내용"은 실무본과 동일 구조(제목표 = 앵커 문단 속 treatAsChar 표) — 앵커 문단 기여 실측 0pt(prev=0·줄간격 100%·outMargin top 0), kordoc 렌더 밴드 상단 25.31mm vs 실무본 제목 상단 27.95mm로 오히려 타이트. 본문 쪽 제거 가능 잔여 없음
- R023 [export] 표 캡션(타이틀)·표 셀 글자는 12pt — 폰트는 유지하고 높이만 치환, 제목 박스 셀은 제외 (근거[실측]: 20260724건 사용자 확정)
- R024 [export] □ 절 제목은 항상 볼드 — postprocess가 dae 문단 run charPr에 bold 변형 배정 (근거[관례]: 20260724건 사용자 확정)
- R025 [export] ☞ 등 결론 유도 기호는 ＊·※와 동일 계층 처리 — 선두 5칸 리터럴 띄어쓰기 + 내어쓰기 left=0·intent=-4글자폭(15pt 본문 기준 60pt), 인접 간격 3pt 준용 (근거[추론]: 20260724건 사용자 확정, R019 확장)
- R026 [draft] 조사 기반 보고서는 본문=두괄식 결정·요지, 붙임=「검토 근거 상세」(2p 이내, R009 배너 표)로 분담한다 — 본문에 쓴 근거의 원문 인용·수치 산출 근거·차용 논리를 붙임에 담아 제3자 검증 가능하게 (근거[관례]: 20260724건 사용자 확정, style-guide §8 분담 원칙의 draft 단계 강제)
- R027 [export] 붙임·참고 배너(3열 표, 첫 셀 '붙 임'/'붙임 N'/'참고N')는 양식 참고 블록 정합 — 셀 글자 HY헤드라인M 16pt, 배너 앵커 문단 pageBreakBefore=1로 별도 페이지에서 시작, R023 12pt 강제 대상에서 제외. **셀 스타일은 양식 '참고1' OLE 재실측값('26.7.24 정정)**: 라벨 셀 4변 SOLID 0.5mm #1B1760+채움 #2B2D63(남색)+흰 글자, 스페이서 좌변만 SOLID, 제목 셀 상·하변 SOLID, 행 높이 28.3pt, 셀 폭 라벨 21.1mm·스페이서 2.0mm·제목 잔여. HWP COLORREF는 0x00BBGGRR — hex를 그대로 읽으면 R/B가 반전되며 최초 기록(#60171B/#632D2B 적갈)이 이 오류였음. 색 실측은 반드시 바이트 순서 검증 후 기록 (근거[실측]: 20260724건 사용자 2회 확정 — 교차검증: 표 헤더 bf14=#D2EBEE·제목박스 그라데이션 #3057B9 남색 계열 정합)
- R028 [draft] 붙임은 각 1장 이내로 설계한다 — 계층형 문구(ㅇ·대시)는 렌더 기준 2줄 이내(휴먼명조 15pt, 1줄 35자 = 90자 상한 — R062)로 쓰고, 넘치는 상세는 표 전환·항목 분리·삭제로 압축한다. 본문 계층 문구도 동일 상한 준용 (근거[관례]: 20260724건 사용자 확정)
- R029 [draft] ㅇ 항목이 (괄호) 리드로 시작하면 하위 대시는 (괄호) 리드를 쓰지 않는다 — 대시는 명사구·서술로 바로 시작해 리드 중복 계층을 만들지 않는다 (근거[관례]: 20260724건 사용자 확정)
- R030 [export] 제목표 상단여백의 **확정 원인은 머리말(hp:header) 부재** — kordoc 산출본은 머리말이 없어 편집용지 위 10mm+머리말 15mm(R020 양식 규격)=25mm가 통째로 빈 흰 띠로 남고, 이것이 제목표 위 여백으로 보인다(줄간격·outMargin·행높이·편집용지 후보는 전부 XML 실측으로 배제 — R022 참조). 양식 원형(표준양식 .hwp Section0 head ctrl)과 실무 보고서는 이 영역을 **KCA 로고 배너 표(1×2, 높이 11.3mm, 로고 png+슬로건 bmp)**가 채운다. 처리: postprocess `--header-banner`(--all 포함)가 실무본 이식 자산(assets/kca-header-banner/: fragment.xml+resources.xml+이미지 2종)을 id 재배정·manifest 등록과 함께 주입한다. hp:header 기존재 시 스킵(멱등). 편집용지 여백 축소로 대응 금지(양식 규격 위반) (근거[관례]: 20260728건 0839 3차 조사 — 260331 실무본 이식, 주입 전후 kordoc 렌더 바이트 동일로 본문 무영향 확인). **'26.7.28 4차 재검증**: 주입된 배너 서브트리는 실무본 원형과 id 재배정 외 완전 동일(diff 실측), 참조 id·manifest 전부 유효, 배너 라인 39.4pt < 머리말 영역 42.5pt로 본문 밀림 없음 — 배너가 여백을 늘리지 않았고 되돌릴 사유 없음. 잔여 흰 영역 = 위 10mm + 배너 아래 3.15pt(실무본 동일, 양식 구조). **kordoc reflow 렌더는 머리말·hp:caption·pageBreakBefore를 그리지 않으므로 렌더 스크린샷으로 상단여백·캡션·배너 페이지 시작을 판정하지 말 것 — 한글 실물 확인 필요**
- R031 [export] 본문 서술 중 용어 뒤 ＊ 표지는 **위첨자** — charPr에 `<hh:supscript/>` 자식 추가한 복제본으로 run 분리(높이·offset은 유지, 한글이 축소 렌더 — 260223 AX전략·260331 실무본 실측 인코딩). 하단 `＊ 용어 : 설명` 각주 문단(선두 ＊)은 평문 유지(R011 참고 charPr 13pt 그대로). postprocess `apply_superscript_star` (근거[관례]: '26.7.28 사용자 확정)
- R032 [export] 본문 계층 문단(□·ㅇ·대시)은 **양쪽 정렬(JUSTIFY)** — 우측 들쭉날쭉 방지. ＊·※ 각주·표 캡션·발신 줄은 기존 정렬 유지. postprocess `apply_body_justify` (근거[관례]: '26.7.28 사용자 확정)
- R033 [export] **[적용 범위 확장 → R039]** 본문 서술 중 `(…)` 괄호 구간은 **13pt**(본문 15pt 대비 축소) — run 분할로 괄호 포함 구간만 높이 1300 치환(폰트·볼드 유지). **ㅇ 선두 괄호 리드(`(교 육)` 등 R016 라벨)는 제외** — 서술이 아니라 라벨이므로 15pt 볼드 유지. 표 셀(이미 12pt)·＊※ 각주(13pt) 비대상. 괄호가 run 경계를 넘으면 건너뛰고 개수 보고. postprocess `apply_paren_small` (근거[실측]: '26.7.28 사용자 확정)
- R034 [export] 표 캡션(`[ … ]`)은 표 바깥 문단이 아니라 **hp:tbl 안 `hp:caption`(side=TOP)에 내장** — 실무본(260331) 실측 원형: outMargin 다음 위치, 캡션 문단 CENTER+볼드, 크기는 R023 12pt 일괄 적용. 내장 시 캡션↔표 사이 스페이서 빈 문단은 제거하고 전환 간격은 X→표로 승계(＊→표 10pt, ㅇ/대시/※→표 6pt). postprocess `apply_caption_embed` (근거[실측]: '26.7.28 사용자 확정)
- R035 [export] 붙임·참고 배너 표(R027 3열)는 **좌측 정렬**(앵커 문단 LEFT) — R015 표 우측 정렬의 명시 예외 (근거[실측]: '26.7.28 사용자 확정)
- R036 [export] **[상한값 정정 → R042 · 메커니즘은 본 규칙 유효]** **표 총 폭(표 폭 + outMargin 좌우)은 편집용지 본문 폭을 넘지 않는다** — 초과 시 표 폭·셀 폭·내부 그림을 같은 비율로 축소한다(`postprocess_hwpx.py` `apply_fit_page_width` — 플래그와 무관하게 항상 실행. '26.7.29 코드 실측 정정, 종전 '--all 포함' 표기는 오기). 근거: KCA 실보고서 12건 전수 실측에서 **머리말 배너 표 폭 == 본문 폭**이 예외 없이 성립(좌우 20mm 문서 12건 → 배너 170mm / 좌우 15mm 문서 3건 → 배너 179mm+여백 = 180mm). 머리말 배너 자산을 좌우 15mm 문서(260331)에서 이식한 결과 좌우 20mm 문서에서 **배너 10mm·제목박스 1mm 초과**가 발생했고, 이것이 R022·R030 3차례 조사에서 못 찾은 **제목표 상단 여백의 실제 원인**이다 — 여백 속성이 아니라 **폭 초과**였다. **'26.7.28 6차 정정 → R042**: 축소 목표 '이내'(본문 폭과 정확히 같게)가 slack 0을 만들어 또 다른 여백을 낳았다 — 상한을 '미만'(여유 283 hu)으로 정정 (근거[실측]: 20260728건 4차 조사, 사용자 3회 재보고)
- R037 [export] 붙임·참고 배너 표(R027 3열)의 **제목 셀(3번째) 문단은 양쪽 정렬(JUSTIFY)** — 라벨('붙임 N')·스페이서 셀은 가운데 정렬(CENTER) 현행 유지. R035(배너 표 자체의 좌측 **배치**)와는 별개 층위 — 이 규칙은 표 **안** 제목 셀의 문단 정렬이다. postprocess `apply_annex_banner` ⑤가 배정하고 `apply_center_cell_text`는 배너 제목 셀을 제외한다(R023 12pt의 배너 셀 제외와 같은 패턴), 요약 JSON `annex_banner.title_justified` 보고 (근거[실측]: '26.7.28 사용자 확정)
- R038 [export] **※·＊ 단서/각주 뒤 ㅇ 복귀 전환 간격 6pt(600)** — R013 전환 표에 미정의라 간격 0으로 붙어 보이던 결함의 보완(TRANSITIONS `cham→yo`·`star→yo` 추가). 값은 실측이 아니라 **정합 추론**: 표준양식(250609)은 ※ 기호 자체가 부재(코드포인트 전수 스캔)·실무본(260331)도 ※·＊ 문단 부재로 이 전환의 실물이 없어, R013 체계의 X→ㅇ 전환(□→ㅇ·ㅇ→ㅇ·대시→ㅇ) 전부 6pt인 정합값을 적용. R013의 전환 표 확장이다 (근거[추론]: '26.7.28 사용자 확정 — 20260728건 1633 ※→ㅇ 무간격 지적)
- R039 [export] 본문 괄호 13pt(R033)는 **괄호 구간이 run 경계를 넘어도 적용** — run 단위가 아니라 **문단 전체 텍스트 기준**으로 `(…)` 구간을 찾고, 걸친 run들을 각각 분할해 걸친 부분만 그 run 고유 charPr의 13pt 복제본으로 치환한다(서식 보존 — 볼드 run 안 괄호는 13pt 볼드). 문장 안 볼드(`**T1**` 등)로 run이 쪼개져 약 70%가 건너뛰어진 결함의 항구 수정(1127건 13/63 → 20260728건 13/45 — 2회 반복 관찰 승격). `cross_run_skipped`는 분할 불가 run(그림 등 중첩 개체)에 걸린 구간만 남으며 **통상 0이어야 한다**(스모크 실측: 32→0). R033의 예외(ㅇ 선두 리드 15pt 볼드·표 셀·＊※ 각주 비대상)는 그대로 유지 (근거[관례]: '26.7.28 사용자 확정)
- R040 [draft][export] **강조 2계층 — 강조=볼드(style-guide §6 현행), 특히 강조=노란색 음영 하이라이트**: 초안 md는 `==문구==`로 표기(md-profile §1-3, 문서당 극소수 지점만·짝 불일치는 lint `highlight-unpaired` 검출)하고, kordoc 변환은 리터럴 통과 → postprocess `apply_highlight`가 마커 제거 후 해당 구간 run에 charPr **`shadeColor="#FFFF00"`+볼드** 복제본 배정(run 경계 걸침도 분할 처리, 잔존 `==`는 compare `markdown-leftover` 최종 검출). 인코딩은 260331 실무본 'AI검증 후 최종결과물 변환' run(charPr id=82: shadeColor="#FFFF00"+bold, 문서 내 유일 음영 run) 실측 — **hwpx XML 색은 #RRGGBB 직독**(#FFFF00=노랑, 사용자 관찰 정합. 교차검증: 같은 문서 제목박스 그라데이션 #3057B9=남색 정합, BGR로 읽으면 #B95730 적갈로 불일치). R027의 COLORREF 0x00BBGGRR 반전은 **hwp OLE 바이너리 전용**이며 hwpx XML엔 미적용 — 바이트 순서 검증 완료 (근거[실측]: '26.7.28 사용자 확정)
- R041 [export] 머리말 배너(R030) 주입 시 **앵커 문단 paraPr lineSpacing을 PERCENT 100으로 강제 + 머리말 subList textWidth를 현 문서 본문 폭으로 보정** — 제목표 상단 여백의 실원인은 도너(260331, 좌우 15mm)에서 이식된 **앵커 lineSpacing 150%**: treatAsChar 배너 표(11.3mm)의 줄 높이를 17.0mm로 부풀려 머리말 영역 15mm(4251)를 초과시키고 신규 조판 시 본문을 ≈4.6mm 밀어냈다(실기동 A/B: 150%=30.42mm / 100%=25.79mm / 실무본 25.5mm — 0.3mm 이내 일치). 배너 셀 내부 문단(160%)은 비대상, 기주입 문서도 재실행 시 소급 수리(멱등). 미세조정안(115%·FIXED)은 기각 — 배너를 실무본 좌석(로고 12.5mm)까지 내리려면 줄 높이가 다시 15mm를 넘어 본문이 밀리므로 lineSpacing만으로는 양립 불가(XML 기하 판정), 본문 위치 정합(이 결함의 본질)을 택함. R030(머리말 주입 자체)은 유효 — 이 규칙은 '주입된 앵커의 lineSpacing이 여백의 실제 원인이었다'는 R030 계열 정정이다. R022(앵커 줄간격·outMargin)·R036(배너 폭 초과)은 실재한 별개 결함이었으나 세로 여백의 원인은 아니었다. textWidth 51026(도너 180mm)→본문 폭 실측치 치환 (근거[실측]: '26.7.28 실기동 A/B 실측 + XML 기하 판정)
- R042 [export] **[R036 상한값 정정]** 표 총 폭(sz + outMargin 좌우)은 본문 폭 **미만** — 여유 283 hu(1.0mm)를 남긴다. 본문 폭과 정확히 같으면(slack 0) 같은 문단에 선행 요소가 있을 때 한컴이 표를 다음 줄로 내려 표 위에 15pt 빈 줄이 생긴다. `apply_fit_page_width`가 초과 표를 본문 폭 − 283으로 비례 축소하고, 내부 그림의 scaMatrix·rotationInfo 파생 캐시도 함께 재계산한다. **정적 XML로는 판정 불가 — 실기동 픽셀 계측이 결정타** (근거[실측]: '26.7.28 사용자 확정 6차 실기동 A/B 30.29→25.02mm · 경위 → `rules-history.md#R042`)
- R043 [export] **hwpx 패키지는 정품 프로파일로 정본화한다** — `canonicalize_package`가 version.xml·settings.xml·manifest·container.rdf 등 필수 멤버를 보강하고 디렉터리 엔트리를 제거하며 정품 압축 프로파일로 재작성한다(플래그 무관 상시 적용). 내부망 반입 판별이 이 패키지 형태를 본다 (근거[관례]: '26.7.28 확정 · 경위 → `rules-history.md#R043`)
- R044 [draft] **구성요소 이름을 가운뎃점으로 늘어놓고 "N단 구조"·"N중 차단"·"N종 구현"으로 부르는 라벨 나열 금지 — 무엇을 하는지 서술한다**: `핵심결론·본문·보충안내·출처표기 4단 구조로 규격화`, `인젝션·개인정보·내부정보 3중 차단 적용`, `진입 가드·질의 재작성·지식 검색·컨텍스트 정제·생성 전송 5단계로 처리` 같은 표기는 항목명만 나열해 독자에게 **동작·효과를 전달하지 못한다**(목차를 본문에 옮겨 적은 상태). 대체 원칙 — ① 그 구조가 **무엇을 보장하는지**를 술어로 쓴다(`결론을 먼저 제시하고 근거를 뒤에 붙이는 순서로 고정해 질문 요지에 즉답`), ② 구성요소 목록 자체가 필요하면 **표로 배출**한다(style-guide §3 나열 3개 초과+속성 2개↑ 표 전환과 동일 판단), ③ 이미 같은 목록을 지탱하는 표가 있으면 본문은 **그 표가 말하지 않는 요지·판단만** 쓴다(R026 본문·붙임 분담과 같은 층위). **적용 예외**: 기술스택·자료 규모처럼 고유명사·수치 자체가 정보인 사양 나열(`Node.js 24.x · Next.js 16.2 · React 19.2`)과 style-guide §4의 조사 생략 병렬 압축(`근거 소실·실적 누락·작성부담 해소` — 이것은 항목명이 아니라 **해소 대상 상태**의 병렬)은 대상이 아니다. 판별 기준은 "가운뎃점으로 묶인 것이 **고유명사·상태**인가, 아니면 **내가 방금 만든 구조의 부품명**인가" — 후자면 위반 (근거[관례]: '26.7.30 사용자 확정, 이음5G AX파일럿 결과보고건)
- R045 [draft] **상위 계층 하나에 딸리는 하위 계층은 최대 2개 — 초과분은 통합 서술로 합친다**: ㅇ 아래 대시는 2개까지만 둔다(□ 아래 ㅇ도 같은 원칙을 준용하되, 절 단위 ㅇ 수는 절 구성상 필요한 만큼 허용). 하위 항목이 3개 이상 나오면 **개수를 줄이는 방향이 아니라 문장을 합치는 방향**으로 처리한다 — 성격이 가까운 항목끼리 하나의 서술로 묶어 연결어(~하고, ~하며, ~와 함께)로 압축하고, 그래도 안 합쳐지면 상위 ㅇ를 둘로 쪼개 각자 2개 이하를 갖게 한다. **금지 예시**: `ㅇ (응답 표준화·UX)` 아래에 `- 순서 고정` `- 표 정리 지정` `- 진행상태 표시·중지` `- 모바일 최적화·안내문` 4개를 나란히 다는 것(코퍼스에 없는 밀도이며 계층이 목록으로 전락). **처리 예시**: 앞 2개는 답변 서술 규격이므로 한 문장으로, 뒤 2개는 이용 편의이므로 한 문장으로 합쳐 대시 2개로 만든다. 상세를 잃지 않으려면 대시를 늘리지 말고 표로 배출한다(style-guide §3 배출구 원칙·R044 ②와 동일 층위). 린트 `bullet-overflow`가 3개째부터 기계 검출한다 (근거[관례]: '26.7.30 사용자 확정, 이음5G AX파일럿 결과보고건)
- R046 [draft] **본문 서술의 날짜는 `'yy.m월` 월 단위로 표기 — 연월일 풀 표기(`2026. 7. 30.`·`'26. 7. 30(목)`)를 쓰지 않는다**: 보고 본문에서 일자·요일까지 특정하면 문서가 특정 시점에 고착돼 재사용·후속보고 때마다 손봐야 하고, 대부분의 서술에서 일자는 정보 가치가 없다. `'26.7월 실측 결과`, `'26.6월 기준 진도율 70%`처럼 **월 단위로 끊는다**(월 단위는 붙여쓰기 — style-guide §5 `'25.6월` 관례). 기간 범위는 물결로 `'26.3월 ~ '26.7월`, 연도만 필요하면 `'27년`, 반기 단위는 `'26년 하반기`. **예외 2가지**: ① **발신 줄**은 style-guide §1 철칙(`< '26. 7. 30.(목), 본부 팀 >`)이 지배하므로 실제 보고일을 일자·요일까지 적는다, ② 특정 일자 자체가 보고의 핵심 사실인 경우(회의 개최일·시행일·마감일 등)는 `'26.6.23(화)` 축약형으로 적되 4자리 연도는 쓰지 않는다. kordoc 변환기가 내는 `[DATE_2DIGIT_YR] 연도는 네 자리로` 경고는 기관 관례에 반하므로 **무시한다**(경고이지 오류가 아님) (근거[관례]: '26.7.30 사용자 확정)
- R047 [draft] **본문 수치는 팩트체크를 통과한 값만 쓰고, 신뢰를 깎는 과잉정밀·단발측정 수치를 피한다**: 표본 1회 측정치를 소수점까지 적는 것(`응답 0.59초`)은 정밀해 보이지만 재현성이 없어 **오히려 신뢰를 떨어뜨린다** — 이런 값은 `1초 이내`처럼 **의미 있는 구간으로 올림**하거나 판정 결과(`정상`)로 대체한다. 판단 기준 3가지 — ① 제3자가 같은 방법으로 재보면 같은 값이 나오는가(아니면 구간화), ② 자릿수가 측정 정밀도를 넘지 않는가(단발 curl 측정에 소수 2자리는 과잉), ③ 근거 원문에 그 수치가 실제로 있는가(R003 확정/추정 태깅과 연동). 코드 상수·자료 건수·공표 실적처럼 **원본에 확정값이 있는 수치는 그대로 인용**한다(`488건`·`상위 6건`·`유사도 0.4`·`20회/60초`). export 단계 경량 팩트체크(`validate_hwpx.py numbers`)가 근거 없는 수치를 걸러내지만, 과잉정밀은 기계가 못 잡으므로 **집필 단계에서 판단**한다 (근거[관례]: '26.7.30 사용자 확정)
- R049 [draft] **□ 절 하나에 딸리는 ㅇ는 최대 3개 — 초과하면 통합 서술로 합치거나 절을 나눈다**: R045가 "ㅇ 아래 대시 2개"를 규정하면서 "□ 아래 ㅇ 수는 절 구성상 필요한 만큼 허용"으로 열어 두었으나, 실제로는 한 절에 ㅇ가 6개까지 늘어나 계층이 목록으로 전락하는 결함이 반복됐다. **처리 순서** — ① 성격이 가까운 ㅇ끼리 하나의 요지로 합치고 각자의 상세는 대시로 내린다(예: '초기 구축'+'한계 확인' → 한 요지의 두 대시가 아니라, 시간 순 서사면 그대로 두 요지로 유지), ② 그래도 4개 이상이면 **부수적 사실은 ※(단서·후속 참조)로 배출**한다 — ※는 ㅇ 수에 계상되지 않으므로 신규 역량·운영 기능처럼 본문 요지에서 밀려난 항목의 정규 배출구다, ③ 그래도 남으면 □ 절 자체를 둘로 쪼갠다(단 사용자가 절 구성을 지정한 경우 절 신설 금지 — ①②로 해결). 붙임의 □도 동일 적용. R045(ㅇ→대시 2개)와 한 쌍으로 **1절 = ㅇ 3 × 대시 2 = 최대 9항목**이 계층형 본문의 밀도 상한이다 (근거[관례]: '26.8.3 사용자 확정, cert-poc PoC 결과보고건)
- R048 [export] **산출물·조사자료는 `reports_dir`(하네스 report/)에 쌓는 것이 정본이고, vault(claudian) 적재는 하네스가 하지 않는다 — 수확은 vault가 pull한다**: 작업폴더 `{reports_dir}/{YYYYMMDD}/{HHMM}_{슬러그}/`가 **유일한 1차 저장소**이며 그 자체로 완결된다(vault 없이도 파이프라인 전 구간 동작). 처리 원칙 — ① export·research 종료 시 인도 보고에 **"vault 적재 후보 N건 — claudian `/ingest`로 수확"** 1줄만 알린다, ② **하네스는 vault의 어떤 파일도 쓰지 않는다** — `acquired/`·`raw/`·`wiki/log.md` 장부까지 예외 없다(사용자가 적재를 요청해도 하네스가 직접 복사하지 않고 vault 쪽 `/ingest`로 넘긴다), ③ 착지 판단(조사 raw→`acquired/fetched/{건}--{주제}`, 제공자료 원본→`raw/`, 파싱본→`acquired/parsed/`)·복합 슬러그 네임스페이싱·스키마 변환·멱등 마커(`research/_promoted.json`)는 전부 vault 계약이므로 claudian `CLAUDE.md` §5.2.1이 소유한다. 배경: vault raw에 자동 적재하면 원본 수집물과 파생 보고서가 뒤섞여 출처 추적이 깨진다. 여기에 '26.8.15 실측으로 **승인 게이트형 push의 실패**가 확인돼 pull로 전환했다(적재율 0% — 32폴더·raw 99건 전량 미승격) (근거[실측]: '26.8.15 사용자 확정 · 경위 → `rules-history.md#R048`)
- R051 [draft] **ㅇ 괄호 리드는 3~5음절 구체 명사구 — 2음절 추상어를 벌려쓰기로 때우지 않는다**: `(품 질)`·`(배 포)`·`(구 축)`·`(교 육)`처럼 2음절로 두면 그 절이 무엇을 다루는지 리드만 보고 알 수 없다. 실무 개선본은 이를 전부 **의미 해상도를 올린 명사구**로 교체했다 — `(서비스 품질)`·`(서비스 배포)`·`(서비스 구축)`·`(고급 교육)`, 나아가 `(이용 양상)`→`(AI맥락 주입)`·`(확 산)`→`(접근성 보장)`처럼 **리드 자체를 더 정확한 개념으로 재명명**한 사례도 있다. 자간(벌려쓰기) 문제가 아니라 **어휘 선택** 문제다. **적용 범위 한정**: style-guide §2의 2음절 벌려쓰기 관례는 **표 헤더 전용**으로 남긴다(`구 분`·`시 기`·`회 차`·`비 고`·`단 계`·`역 할` — 개선본에서도 유지됨). 린트 `lead-too-short`가 ㅇ 선두의 2음절 괄호 리드를 벌려쓴 형태·붙인 형태 모두 검출한다 (근거[관례]: '26.8.4 사용자 개선본 2건 대조 — ICT어드바이저에서 6건 전량 교체)
- R052 [draft] **각주가 필요한 기술용어는 각주를 달지 말고 용어를 바꾼다 — 각주 상한 4개**: 개선본은 용어 각주를 7개→0개, 8개→4개로 줄였는데 각주를 삭제한 것이 아니라 **각주가 필요한 단어를 안 쓰게** 고쳤다. `holistic＊ 단일콜(문항당 1콜+11-enum)` → `문항당 1회 호출`, `run_pipeline() 단일 소스로 CLI·웹 공유` → `명령행과 웹 어느 쪽으로 실행해도 동일한 결과`, `A01~A21 분류체계` → `11개 유형으로 분류`, `hp:equation 스크립트 직접 추출` → 삭제. 판단 기준 — **수신자(원장·협의체·현업 부서장)가 그 단어를 모르면 각주가 아니라 다른 단어를 찾는다.** 코드 식별자(`함수명()`·`파일명.확장자`·내부 enum·XML 태그)는 보고서 본문에 쓰지 않는다. 린트 `footnote-overflow`가 5개째부터 검출 (근거[관례]: 동일 대조 — cert-poc 용어＊ 7→0)
- R053 [draft] **본문은 자기완결 — 괄호로 붙임에 설명을 미루지 않는다**: `(상세 붙임1 참조)`처럼 본문 문장 안에서 설명을 유보하면 읽는 사람이 두 번 이동해야 하고 본문이 목차로 전락한다. 개선본은 이 표기를 6회→0회로 전량 제거했다. 붙임은 **검증하려는 사람만 펴보는 원자료**이지 본문의 각주가 아니다(R026 분담과 같은 층위 — 본문은 요지를 스스로 끝내고, 붙임은 그 요지를 뒷받침하는 전수 데이터를 따로 담는다). **예외**: 절 맺음의 `※ 세부내용은 붙임 참조`는 코퍼스 합법 관례이므로 유지한다(문장 안 유보가 아니라 절 종료 후 안내). 린트 `annex-crossref`가 괄호 유보형만 검출 (근거[관례]: 동일 대조 — cert-poc)
- R054 [draft] **결과보고 절 구성 표준 — 추진 배경 → 추진 내용 → 추진 성과 → 운영 전환 → 향후 계획**: `추진 성과`는 **실측 정량치 전용 절**이다(오류율·응답시간·이용건수·검출률 등 측정된 값). 정성적 기대효과를 이 절에 넣지 않는다 — 측정값이 없으면 절을 세우지 말고 그 사실을 드러낸다. `운영 전환`은 배포 시점·접근 경로·공개 서비스 방어선을 담는 별도 절로, 서비스형 과제에서 `추진 내용`(무엇을 만들었나)과 분리한다. 과제 성격상 해당 없는 절은 생략 가능하되 **순서는 뒤집지 않는다** (근거[관례]: '26.8.4 ICT어드바이저 개선본 5절 구성)
- R055 [draft] **향후 계획은 실행 액션 — 주체·시점·이미 일어난 근거사실을 함께 적는다**: `외부용역 사업 전환을 추진 중(업체 미팅 '26.7.1 완료)`처럼 **이미 발생한 사실**을 괄호로 붙여 실행 의지를 뒷받침한다. 성과 나열이나 일반론("품질을 지속 개선")으로 절을 채우지 않는다. 각 ㅇ는 `누가(주체) 무엇을(액션) 언제까지(시점)`가 읽히도록 쓰고, 시점은 R046에 따라 `'yy.m월`·`'yy년 하반기` 단위로 적는다 (근거[관례]: '26.8.4 cert-poc 개선본 — 사업 전환·기술지원·확장 연계 3항목 전부 주체·근거사실 포함)
- R056 [draft] **표 캡션은 일련번호 없이 표가 무엇을 보여주는지 서술한다**: `[ 표1. 추진 총괄 요약 ]`이 아니라 `[ 월별 바이브코딩 교육 진행 ]`·`[ 질의 1건의 처리 흐름 ]`·`[ 출력강제 방식별 실험 결과 ]`처럼 **측정 축·단위·조건**을 캡션에 담는다. 표본이 한정적이면 조건을 병기한다 — `[ 사용량·품질·API 실측(테스트운영) ]`. 본문에서 표를 번호로 부르지 않으므로(style-guide §8 무참조 관례) 번호는 유지비용만 남는다. 린트 `caption-numbered`가 검출 (근거[관례]: '26.8.4 개선본 2건 — 캡션 11개 전부 번호 없음)
- R057 [draft] **연결어를 반복하지 않는다 — 한 절에서 같은 연결어 3회 초과 금지**: 실무 개선본은 한 문서에 연결어 10종을 쓰는데(`하고`6·`하여`3·`하며`3·`함께`2·`되어`·`만큼`·`도록`·`없이`·`순차`·`시켜`) 하네스 산출본은 3종(`하고`4·`도록`2·`우선`)에 그쳐 문장이 단조롭다. **연결어 후보** — 병렬·나열: `~하고`·`~하며`·`~와 함께`·`~해`, 목적: `~하고자`·`~도록`·`~위해`, 원인·근거: `~되어`·`~발생해`·`~인 만큼`·`~에 따라`, 수단: `~으로`·`~을 통해`·`~시켜`, 배제·한정: `~없이`·`~만으로는`·`~에 국한되지 않는`, 첨가: `~도`·`~까지`, 동시: `~면서`·`~되며`. 같은 어미가 인접 두 문장에 연달아 오면 하나를 바꾼다. **주의**: 다양성 자체가 목적이 아니라 **관계를 정확히 표시**하는 것이 목적이다 — 인과를 `~하고`로 뭉개지 말고 `~되어`·`~발생해`로 드러낸다 (근거[관례]: '26.8.4 개선본 2건 연결어 분포 실측)
- R058 [draft] **대시는 ㅇ 서술의 근거를 명시하는 부연 — 같은 내용을 표현만 바꿔 반복하지 않는다**: ㅇ가 판단·조치를 진술하면 대시는 **그 판단이 선 근거**(왜 그렇게 했는가·무엇이 그것을 뒷받침하는가)를 댄다. 실측 예 — `ㅇ (과제 선정) 규정·지침·법령이 분산되어 담당자별 해석이 갈리는 문제를 해소하고자 과제로 선정` / `- 외부 기관 문의 대응 시 부서별·담당자별 답변 차이가 발생해 근거 조문에 기반한 해석 일관성 확보가 시급`: ㅇ는 '선정했다', 대시는 '왜 시급했는가'. **금지 형태** — ㅇ의 명사구를 풀어 쓰기만 한 대시(`ㅇ … RAG 파이프라인으로 구축` / `- RAG 파이프라인은 검색과 생성을 분리한 구조`), ㅇ에 이미 있는 수치를 되풀이하는 대시. 근거로 쓸 것이 없으면 **대시를 달지 말고 ㅇ만 둔다** — 빈 부연은 분량만 늘린다 (근거[관례]: '26.8.4 사용자 확정)
- R059 [draft] **향후 계획에서도 주어를 생략한다**: style-guide §4의 주어 생략 관례(`주어(우리 기관·당 팀)는 생략`)를 향후 계획 절에도 그대로 적용한다 — `ICT자격본부 검정관리팀이 외부용역 사업 전환을 추진 중` → `외부용역 사업 전환 추진 중`. 수행 주체를 반드시 드러내야 하면 **주어가 아니라 방향·대상으로 녹인다**(`이음5G사업팀으로 운영 이관`·`검정관리팀 대상 기술 자문`). 보고 문서의 주체는 발신 줄(`< 날짜, 본부 팀 >`)이 이미 밝히고 있으므로 본문에서 반복하면 군더더기가 된다 (근거[관례]: '26.8.4 사용자 확정 — 개선본의 주어 명시 사례는 채택하지 않음)
- R060 [draft] **계층 문구를 항목 나열로 채우지 않는다 — 구분자 밀도가 넘으면 표로 배출한다**: R044가 "N단 구조·N중 차단" 같은 **라벨 나열**을 금지했으나, 라벨을 붙이지 않고 항목만 길게 늘어놓는 형태는 그대로 남아 반복 재발했다(실측: '26.8.5 reachy-mini건 붙임 2줄 — `국가기밀·외교국방·국가기반시설 정보통신망 구축, 대규모 정보시스템 구축(10억 이상), 개인정보 100만명 이상 처리, 망 연동, 무선네트워크 시스템 구축 등이 대상`, `웹시스템·화상회의·CCTV·백업·콜센터 등 단일기능 시스템 구축과 방화벽·스위치·라우터·서버·교환기 등 정보보호제품·전산통신장비 도입·교체가 대상에 해당`). 두 문장 모두 **술어가 "대상" 하나뿐이고 나머지는 전부 항목명**이어서, 읽는 사람에게 판단·귀결을 전달하지 못하고 목록을 문장 모양으로 위장한 상태다. **판정 기준(괄호 밖 기준)** — 쉼표 3개 이상 또는 가운뎃점 5개 이상이면 위반. 정상 서술문의 실측 분포는 쉼표 0~2개·가운뎃점 0~4개다. **처리 순서** — ① 나열 항목을 **표로 배출**하고 계층 문구에는 그 표가 말하지 않는 판단만 남긴다(style-guide §3 배출구 원칙·R044 ②와 동일 층위), ② 표를 만들 만큼 항목이 많지 않으면 **대표 항목 2~3개만 남기고 "등"으로 닫되 술어를 세운다**, ③ 열거 자체가 정보의 전부라면 그 항목은 본문이 아니라 붙임 표에 속한다. **적용 예외**: `□ 출처`·`참고문헌`·`근거 목록` 계열 절은 열거가 형식 자체이므로 대상이 아니다(린트가 직전 □ 절 제목으로 자동 판별). 린트 `list-dump`가 기계 검출한다 (근거[실측]: '26.8.5 사용자 확정 — R044 박제 후에도 재발해 검출 기준을 밀도로 성문화)
- R061 [export] **※ 단서와 ＊ 각주도 본문 계층과 동일하게 처리한다 — 양쪽 정렬(JUSTIFY) + 2줄 압착 대상**: R032가 □·ㅇ·대시만 JUSTIFY로 돌리고 ※·＊는 왼쪽 정렬로 두었는데, ※·＊도 본문 폭을 꽉 채우는 서술이라 우측 끝이 들쭉날쭉해 계층 문단과 어긋난다. `postprocess_hwpx.py`의 `JUSTIFY_KINDS`와 `LINE_FIT_KINDS`에 `"cham"`·`"star"`를 모두 넣어 정렬·압착을 동일 적용한다. **한때 "＊는 짧은 용어 풀이라 대상 아님"으로 ＊만 제외했으나, 두 줄로 넘어가는 각주에서 ※와 다르게 보이는 비대칭이 생겨 '26.8.7 사용자 지시로 통일했다.** 계층 간 나머지 차이(리터럴 공백 □0·ㅇ1·대시3·※＊5칸, 내어쓰기 22.5·30·37.5·52pt, 크기 15·15·15·13pt)는 양식 실측값이므로 그대로 둔다 — 내어쓰기는 `left=0·intent=-hang` 인코딩으로 둘째 줄이 첫 줄 본문 시작에 정렬되며, SVG 렌더 실측에서 네 계층 모두 오차 2pt 이내로 확인됐다 (근거[실측]: '26.8.7 사용자 지시 + 렌더 실측)
- R062 [draft][export] **서술 문구는 렌더 2줄을 넘지 않는다 — 1줄은 35자이며 자간·장평으로 90자까지만 흡수된다**: R050이 "1줄 ≈ 75자"로 적어 78~115자를 2줄로 오인했으나, SVG 렌더 실측 결과 **휴먼명조 15pt·장평 95·본문폭 170mm에서 1줄은 35자**(중앙값 35·최대 47)다. 즉 90자 문구는 3줄로 나가 style-guide §3 철칙(2줄 이내)을 어기고 쪽수를 그만큼 늘린다. **상한은 마커 제외 90자, 하한은 60자**다(R050 흡수 — 1줄로 끝나는 문구는 목차처럼 읽히므로 인접 항목과 통합하거나 근거·수치·귀결을 덧붙여 2줄 밀도로 채운다. 자수를 채우려 같은 말을 반복하는 것은 금지). 상한 초과는 그 이상은 린트 `body-line-overflow`가 검출한다. 변환 단계에서는 `apply_line_fit`(□·ㅇ·대시·※·＊ 전 계층 — R061)이 2줄 초과 문단의 자간을 0→-10까지, 부족하면 장평을 95→90까지 조여 맞춘다 — **자간 -10 미만·장평 90 미만은 가독성 하한이라 사용하지 않으며**, 그래도 3줄인 문단은 `overflow`로 보고해 텍스트를 줄이게 한다(타이포로 못 푸는 분량) (근거[실측]: '26.8.7 사용자 지시 + SVG 렌더 실측)
- R063 [export] **표는 페이지를 넘겨도 깨지지 않게 셀 분할·머리행 반복을 강제한다**: 표가 페이지 경계에 걸릴 때 통째로 다음 장으로 밀리거나 머리행 없이 잘려 나가면 표를 읽을 수 없다. `apply_table_pagination`이 모든 표에 **`pageBreak="CELL"`(셀 단위 분할 허용)과 `repeatHeader="1"`(첫 행을 넘긴 페이지에 반복)** 을 보장한다. kordoc `generate_document`가 대개 이미 부여하지만 후처리로 주입한 표(머리글 배너 등)는 빠지므로 매 실행 검사한다. 더해 **행 22개 이상이거나 6행 이상이면서 최장 셀이 90자를 넘는 표**는 `oversized`로 보고한다 — 이 경우 대응은 ① 셀 텍스트를 줄여 행 높이를 낮춘다 ② 열을 쪼개 표를 둘로 나눈다 ③ 붙임으로 내린다 순으로 적용하고, 표 자체를 페이지에 맞추려 글자를 줄이지 않는다(표 12pt는 R023 확정값) (근거[추론]: '26.8.7 사용자 지시)
- R064 [draft][export] **본문 산식은 계층 서술에 섞지 말고 1행 1열 표(박스)로 내린다**: `- 순절감시간 = (도입 전 소요 − 도입 후 총소요) × 처리 건수로 하며, …`처럼 산식을 서술 문장 안에 넣으면 ① 문구가 길어져 2줄을 넘고 ② 산식이 줄바꿈으로 쪼개져 읽히지 않으며 ③ 위원이 산식만 따로 보기 어렵다. **md 단계에서 1열 GFM 표(헤더 행 하나)로 쓰고** 계층 문구에는 그 산식이 무엇을 뜻하는지만 남긴다. `apply_formula_box`가 1행 1열 표의 문단을 가운데 정렬로 규격화한다. 린트 `formula-inline`이 계층 문구 안의 `=`+`×÷−` 조합을 검출한다 (근거[관례]: '26.8.7 사용자 지시)
- R065 [draft] **나열형 수치는 항목명 뒤 괄호에 담는다 — `심사자료(80→2분)` 형태**: `심사자료 80→2분, 항공 실기 90→6분, …`처럼 항목명과 수치를 공백으로만 끊어 늘어놓으면 어디까지가 한 항목인지 경계가 흐려지고, 쉼표가 항목 구분과 수치 구분을 겸해 읽는 사람이 다시 세어야 한다. **항목명 바로 뒤에 괄호를 붙여 수치를 넣으면** 경계가 괄호로 확정되고, 변환 단계에서 `apply_paren_small`(R033·R039)이 괄호 구간을 자동으로 **13pt로 축소**해 항목명 15pt와 시각적 위계까지 생긴다 — 즉 이 표기는 가독성과 서식이 한 번에 붙는 형태다. 적용 대상은 □·ㅇ·대시 서술 안의 **수치 변화(→)·측정치 나열**이며, 표 셀은 열이 이미 경계를 주므로 대상이 아니다. 린트 `metric-unparenthesized`가 계층 문구에서 괄호 밖 →를 검출한다 (근거[관례]: '26.8.7 사용자 지시)
- R066 [export] **규약 문서와 코드 상수의 드리프트를 테스트로 막는다 — `tests/test_value_drift.py`**: 같은 값이 rules.md·rules-seed.md·format-profile.kca.md·md-profile.md·hwpx-recipe.md·코드·테스트 **최대 8곳에 손으로 복제**돼 있어(실측: 본문 15pt 8곳·ㅇ→대시 6pt 6곳), 한 곳만 고치면 나머지가 조용히 거짓말을 시작한다. **format-profile.kca.md를 단일 출처로 삼고** 코드 상수가 그와 어긋나면 실패시킨다 — 간격표 pt↔HWPUNIT 정합, 첫 □·블록 경계 간격, 계층별 공백 칸수·내어쓰기, 자간·장평 하한, **시드가 운영 규칙을 빠짐없이 담는지**, 폐기값(75자) 잔존 여부. 값을 바꾸려면 프로파일과 코드를 함께 고쳐야 회귀가 통과한다. 배경: R062 정정 때 시드를 놓쳐 새 설치가 틀린 값을 물려받을 뻔했고, R060은 코드·프로파일·테스트 3곳을 동시에 고쳐야 했다 (근거[실측]: '26.8.7 복제 실태 계측)
- R067 [export] **인도 전에 조판 부피를 추정해 보고한다 — `estimate_layout`**: 린트 0·구조검증 0·팩트체크 0을 전부 통과한 문서가 실제로는 15페이지였다. **정적 검사가 "몇 줄로 렌더되는가"를 보지 않기 때문**이다. 후처리가 문단별 예상 줄 수(문자 가중·장평·자간·들여쓰기 반영)와 표 행 높이(셀 텍스트가 열 폭 안에서 접히는 줄 수)로 총 높이를 추정해 `layout.est_pages`·`over_two_lines`로 낸다 — **긴 셀을 가진 표가 쪽수를 지배**하므로 표 행을 평면 상수로 세면 크게 빗나간다(평면 24pt 가정 시 8쪽 vs 셀 기반 11쪽, 실측 13쪽). 오차 15% 수준의 가늠자이므로 정밀 판정이 아니라 **"목표 분량을 넘었는지"를 인도 전에 드러내는 용도**다. 정확한 쪽수는 한글 실조판으로 확인한다 (근거[실측]: '26.8.7 SVG 렌더 13쪽 대비 대조)
- R068 [draft][export] **규칙은 10건 늘 때마다 통폐합한다 — `consolidate_rules.py` + 회귀 테스트**: 규칙이 "결과 보고 → 박제"로만 늘고 정리가 없어 두 달에 43건이 쌓였고, [export] 프리플라이트 로드가 6,557토큰에 이르렀다. 길어지면 읽히지 않고, 안 읽히면 지켜지지 않고, 안 지켜지니 같은 실수가 다시 나 또 박제되는 순환이다. **마지막 통합 마커(`<!-- consolidated-at: R0NN -->`) 대비 10건이 늘면 테스트가 실패**하고, 통폐합 후 `--mark`로 갱신해야 통과한다. 함께 검사하는 것 — 파서 정합(다중 태그 규칙 누락 회귀), 죽은 참조, 근거 등급 누락, 피참조 0건 통합 후보. **자동 병합은 하지 않는다**: 실측 결과 "대체됨" 표기 5건 중 넷이 base ← 확장/정정 관계여서(R015←R035, R019←R025, R033←R039, R036←R041·R042) 기계가 지웠다면 base 규칙이 사라져 체계가 무너졌다. 기계 판정의 한계는 **피참조 0건**까지이고 고유 내용의 흡수 여부는 사람이 읽어야 안다 — 스크립트는 후보 제시와 임계 강제까지만 한다 (근거[실측]: '26.8.7 축적 실태 계측 + 통합 후보 5건 건별 참조 문맥 확인)
- R069 [draft][export] **검토·감사 서브에이전트는 게이트② 승인 이후에만 스폰한다 — 초안 확정 전 스폰 금지**: 초안이 바뀌면 그 판본에 걸린 검토 결과가 통째로 무효화된다. 실측 — 초안 6판을 거친 건에서 검토 에이전트 2종(외부위원 관점·정합성)이 **28만 토큰**을 쓰고 중간 판본에 걸려 재사용 불가였다. 게이트② 전의 스타일 감사는 **메인 컨텍스트 자가감사**(에이전트 0개)로 하고, 승인 후 최종본에 한해 교차 감사를 선택적으로 붙인다. 같은 이유로 **hwpx 변환도 초안 확정 후 1회**가 원칙이다 — `generate_document`는 마크다운 전문을 매번 실어야 해 왕복 1회가 약 1.8만 토큰이고, 재변환 4회에 7만 토큰이 나갔다 (근거[실측]: '26.8.7 세션 토큰 계측 — 서브에이전트가 총 소비의 65%)
- R070 [export] **첫 실행 이탈을 막는 자가진단을 제공한다 — `/report-doctor` + `doctor.py`**: kordoc MCP가 연결되지 않으면 파이프라인이 ③ draft까지 정상 동작하고 **④ export에서 hwpx 없이 md만 남는데**, 처음 쓰는 사람은 이를 실패로 인지하지 못한 채 결과를 받는다. README 증상 표는 수동 대조라 실패 지점을 스스로 찾아야 했다. 스크립트가 Python·npx·스크립트 8종·규약 문서·설정 경로·**인증 방식(구독 vs API 키 — 과금이 갈리는 지점)**·규칙 체계를 순서대로 찍고 exit 0/1/2로 심각도를 낸다. **MCP 연결만은 스크립트가 확인할 수 없어**(모델만 호출 가능) 커맨드가 모델에게 `parse_document` 시험 호출을 지시한다 (근거[관례]: '26.8.7 배포 장벽 분석 — 설치는 2줄이나 첫 실행 실패 대비가 0이었다)
- R071 [draft] **게이트⓪은 문서의 형태가 아니라 산출물 요구를 뽑는다 — "이 보고서가 반드시 뱉어야 하는 것"을 묻는다**: 종전 4문항(유형·분량·논지·맺음말)은 전부 *형태*만 물었고 *무엇이 나와야 하는가*를 묻지 않았다. 그 결과 한 건에서 초안이 **여섯 번 반려**됐고 실제 요구("실측값이 나오는 산식 + 과제별 적용")는 3턴 만에 드러났다. 기록된 반려 — "누가·어디에 등록하나만 다루고 어떻게 재나가 빠져 두루뭉술", "산식을 일반형으로만 적고 과제별로 각 항이 무엇으로 채워지는지를 안 써서 너무 추상적", "개정 대조 틀로 썼는데 독립 최종안을 원했다". **형태를 아무리 정확히 물어도 내용 요구는 안 나온다.** 문항 3을 산출물 요구(판단·결정 / 수치·산출식 / 절차·기준 / 현황·동향, 복수 선택)로 교체하고 그 답을 총괄표 설계에 직결한다. 맺음말은 유형이 정하고(style-guide §7) 표·도식은 게이트①이 정하므로 게이트⓪에서 빼 자리를 만든다. **기존 문서가 `research/provided/`에 있으면 승계 관계(개정본 vs 독립 최종안)를 함께 확인**한다 — 착수 후 되돌리기가 가장 비싼 결정이다 (근거[실측]: '26.8.7 반려 6건 lessons 대조)
- R072 [export] **hwpx를 만드는 모든 호출은 변환 레시피를 탄다 — 적용 여부를 '입력이 무엇인가'가 아니라 '출력이 hwpx인가'로 가른다**: `hwpx-recipe.md` §0이 입력을 `20_draft.md`로만 적어 둔 탓에 **초안이 아닌 원문을 변환할 때 레시피 밖으로 분류하는 이탈**이 발생했다('26.8.10 — vault 위키 페이지를 "파이프라인 밖 일회성 변환"으로 판단, `generate_document`를 preset·표지 옵션만 주고 직접 호출 → R008 0개 전달·후처리 미실행·검증 미실행 → `structural` exit 1로 R043의 내부망 반려 조건 재현). **[생성 경로 한정 → R073]** **문서 종류와 무관하게 항상 타는 3단계** — ① R008 프로파일 파라미터 전량 전달, ② `postprocess_hwpx.py --all`, ③ `validate_hwpx.py structural`. 문서 성격상 해당 없는 규칙(발신 줄 R012·붙임 배너 R009·도식 마커)은 **그 항목만** 비적용이나, **패키지 정합(R043)·표 폭 정합(R042)은 서식이 아니라 파일 유효성이므로 예외가 없다**. 초안이 아닌 원문은 md-profile 초안을 먼저 만들어 태우고, 산출 위치는 `{work_dir}/final/`이 정본이며 외부 폴더 직접 생성은 금지한다(R048 확장). **왕복 `parse_document`는 내용 확인이지 구조 검증이 아니다** — 왕복이 표·장을 정상 재현해 구조 결함을 가린 것이 오판을 굳혔으므로, §4-1을 건너뛰고 왕복만으로 검증됐다고 판단하지 않는다 (근거[실측]: '26.8.10 사용자 지적 · 경위 → `rules-history.md#R072`)
- R073 [export] **기존 hwpx가 있는 개정 건은 재생성하지 않고 원본을 제자리로 고친다 — `generate_document`는 문서를 새로 짓는 도구라 사람이 맞춰 둔 서식을 승계하지 못한다**: 사용자 조정본을 다시 뽑았더니 `section0.xml` 서식 참조 종류가 borderFill 33→25·charPr 73→30·paraPr 45→16으로 주저앉고 병합 표가 평탄화, 셀 내 줄바꿈이 소실됐다. 표 개수는 같아 **왕복 compare가 통과해도 서식은 이미 죽어 있다**. 판정 기준은 입력이 md냐가 아니라 **개정 대상 hwpx 원본이 있느냐**다. **제자리 개정 4원칙** — ① 텍스트는 `hp:t`만 교체(paraPr·charPr 불변), ② 신설 문단은 같은 계층 기존 문단을 복제해 서식을 상속하고 첫 `hp:run`만 남긴 뒤 `linesegarray`(레이아웃 캐시)는 제거, ③ 표 행 추가는 `hp:tr` 복제 + 후속 `rowAddr` 재배정 + `hp:tbl/@rowCnt` 증가, ④ **`postprocess_hwpx.py`를 돌리지 않는다** — 원본이 이미 양식 정합이라 사용자 조정을 덮어쓴다(R072 3단계는 생성 경로 한정). **재압축은 원본 `ZipInfo` 전량 계승**(압축 방식·타임스탬프·외부속성·멤버 순서) — R043 정품 프로파일을 새로 씌우면 오히려 원본과 달라져 자료교환 반입 판별이 갈린다. **검증**: header.xml·settings.xml·BinData·content.hpf MD5 바이트 동일 + 서식 참조 종류 원본 일치 + `structural` exit 0 (근거[실측]: '26.8.18 1814_AI성과측정-방안개선 v3 — 경위는 rules-history.md)
- R074 [draft] **집필 전 `style-guide.md`를 반드시 읽고, 집필 직후 `audit_style.py`를 린트와 함께 돌린다**: '26.8.15 3기관 협의자료에서 초안 전체가 `~함/~됨` 종결로 작성돼 style-guide §4 철칙(동작명사 종결 표준, `~함/~임/~음` 본문 금지)을 전면 위반했는데도 `lint_md_profile.py`를 통과했다. 린트는 마크다운 문법·계층·밀도만 보고 **종결어미·절 제목·문서 제목은 md-profile §4가 "스코프 아웃 — 스타일 감사가 2차 방어"로 넘겨 둔 영역**이며, 그 2차 방어를 사람 판단에만 맡긴 결과 통째로 누락됐다. 근본 원인은 **집필자가 style-guide.md를 읽지 않고 md-profile(변환 문법)과 rules.md(개별 규칙)만 들고 집필**한 것 — 그래서 자가감사도 수치로 박힌 항목(60~90자·ㅇ 3개·대시 2개)만 검사하고 문서의 성격을 정하는 §1 제목 접미·§4 종결어미·§7 절 제목 어휘 풀은 검사 목록에 아예 없었다. **측정하기 쉬운 것만 측정하는 실패**다. 조치 — ① `[draft]` 프리플라이트에 `style-guide.md` 로드를 필수로 넣고, ② lint 통과 후 `audit_style.py`를 같은 자리에서 실행해 exit 1이면 수정 후 재실행한다(사용자에게 보이기 전 단계). 이 스크립트는 오탐 없는 항목만 `violations`(exit 1)로, 문서 유형에 따라 갈리는 항목은 `warnings`(exit 0)로 분리해 낸다 (근거[실측]: '26.8.15 위반본 재검사 — 종결어미 58건·제목 접미 1건·절 번호 8건 검출)
- R075 [draft] **조문은 한국식 전체 표기로 쓰고 `§` 기호를 쓰지 않는다**: `영 §123②2`가 아니라 **`영 제123조제2항제2호`**로 적는다. `§`는 해외 문헌 표기이고 기관 문서 관례가 아니며, 항·호를 원문자(①②)로 축약하면 결재선에서 조문을 되짚기 어렵다. 범위는 물결이 아니라 `제10조~제13조`처럼 조 단위를 모두 적고, 가지 조문은 `제123조제1항제1호의7` 형태로 쓴다. 표 셀에서도 축약하지 않는다 — 근거 조문 열이 길어지는 것은 감수하고 정확도를 택한다. `audit_style.py`의 `article-symbol`이 검출한다 (근거[관례]: '26.8.15 사용자 지시)
