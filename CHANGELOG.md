# Changelog

## 0.5.5 (2026-09-25)

- **kordoc 버전 고정(`kordoc@4.15.3`)** — 플러그인 `.mcp.json`이 떠다니는 최신판 대신 오늘 실변환·실전 점검 3회를
  통과한 판을 띄운다(그 사이 4.15.4가 나왔다). 새 판은 실전 점검을 통과한 뒤에만 올린다. 자가진단의 복구 명령도
  `.mcp.json`의 고정 버전을 읽고, 문서에 적힌 `kordoc@버전`이 고정 버전과 어긋나면 테스트가 잡는다.
- **리뷰 서버 없이도 게이트② 전에 예상 쪽수** — `render_review_html.py` 출력에 `est_pages`·`pages_by_part`가 붙는다.
  변환 뒤 후처리가 보고하는 추정(R067)과 같은 식(문단 줄 수·표 행 높이·도식 격자·머리말 배너·제목 상자·붙임별 쪽)이라,
  오늘 실변환 5건과 대조해 쪽수가 모두 같고 pt 오차는 0~3%다. 게이트⓪ 분량을 넘으면 게이트②에서 줄일 곳을 묻는다
  (실전 점검 2·3회차 연속 교훈 — 정적 페이지만 쓸 때 분량 초과를 변환 뒤에야 알았다).

## 0.5.4 (2026-09-25)

> 0.5.3 변경분 코드 리뷰(#4) 반영.

- **＊ 위첨자 처리의 글자 손실(기존 결함)** — 용어 뒤 ＊를 위첨자로 나눌 때 run을 앞 글자만으로 다시 지어, 같은 run의
  `<hp:nbSpace/>`와 그 뒤 글자가 지워졌다('월 180만 원씩 지출' → '월 180만'). 인라인 요소가 섞인 run을 먼저 나눈다.
- **줄 맞춤·쪽수 추정의 글자 크기** — 문단 본문 크기를 '가장 긴 run'이 아니라 charPr별 글자 수 합으로 정한다. 0.5.3의
  run 분해 뒤 13pt 괄호 조각이 기준이 되어 줄 수를 적게 세, 넘침 경고와 예상 쪽수가 빠질 수 있었다. 본문 사이
  nbSpace run도 함께 조인다.
- 괄호·강조 구간 안의 nbSpace run도 같은 서식(13pt·노란 음영)을 받는다.
- **제목 한 줄 검사의 따옴표** — 인도본처럼 둥근따옴표(전각)로 바꿔 잰다. `curly()`를 후처리 모듈로 옮겨 리뷰 화면과
  함께 쓴다(웹앱에도 실림).
- **실전 점검 도구** — 설치 사본이 저장소와 다르면 멈춤, 규칙 판본은 시드 번호를 모두 담아야 함(빠지면 첫 단계가
  되살려 A/B 무효)·실행 뒤 `rules_drift` 보고, 세션 오류·검사 종료 코드를 인도 판정에 반영, 결과 폴더 재사용 거부,
  `--score-only` 인자 처리.

## 0.5.3 (2026-09-25)

> 하네스 실전 점검 — 설정 경로 환경변수로 운영 폴더와 떼어 놓고 헤드리스 세션으로 가상 자료 → 초안 → hwpx 전 구간을
> 돌려(9분·60턴, 질문 없이 인도) CI가 못 보는 결함을 찾아 고쳤다.

- **실전 점검 도구(`scripts/smoke_pipeline.py`)** — 헤드리스 파이프라인 + 결정론 채점(린트·감사·설계 칸·구조·되읽기
  대조·예상 쪽수·턴·비용). `--rules`로 규칙 판본을 바꿔 넣으면 규칙 슬림화 같은 변경의 A/B가 된다.
- **R054 결과보고 절 어휘** — `추진 성과`·`운영 전환`을 절 어휘 풀·서사 순위(내용 단계)에 넣었다. 규칙대로 쓴 초안이
  `section-title-offpool`로 걸렸다. R054 절 순서의 제목이 모두 풀에 있는지 테스트로 대조한다.
- **제목 한 줄 경고(`title-two-lines`)** — 제목이 제목표 한 줄(24pt)을 넘으면 집필 단계에서 경고한다(후처리·리뷰
  화면과 같은 `fit_title`). 변환 뒤에야 2쪽이 된 것을 알고 초안을 줄이던 것을 앞으로 당겼다.
- **compare 머리말 그림 오계수** — '끝.'이 없는 문서(붙임 없는 결과 보고)에서 되읽기가 문서 끝에 다시 내보내는 머리말
  그림 2장을 본문 그림으로 세던 것(`count-mismatch:figures` 0→2)을 고쳤다.
- **승인 뒤 압축 금지** — 예상 쪽수가 분량을 넘어도 게이트② 승인된 초안을 스스로 줄이지 않는다. 1줄로 알리고 줄일
  곳은 사용자가 고른다(세션이 승인 뒤 항목을 합쳐 1쪽에 맞췄다). 도구로 다시 돌린 점검에서 승인본을 그대로 인도하고
  줄일 곳을 선택지로 내는 것을 확인했다.
- **괄호 축소가 nbSpace run을 건너뛰던 결함** — kordoc이 '180만 원' 사이에 `<hp:nbSpace/>`를 넣으면 그 run이 분할
  불가가 되어 같은 run 안 괄호가 15pt로 남았다(R033·R039). 인라인 요소가 섞인 run을 글자 조각·요소 run으로 나눠
  괄호 축소·강조(R040)가 처리한다(같은 charPr·순서라 조판 불변, 재실행 멱등).
- **게이트①에서 제목 한 줄 확인** — `check_craft.py outline`이 제목 후보가 한 줄을 넘으면 `warnings`로 알린다.

## 0.5.2 (2026-09-25)

> 0.5.1 뒤 같은 날 고친 것 — 작은 결함·지원 편의.

- **검토 화면 강제 다크 차단** — 초안·일반 md 렌더러가 `color-scheme: only light`를 선언한다. 쪽은 한글 종이처럼 흰
  바탕이어야 하는데, 선언이 없는 페이지는 브라우저 강제 다크(Chrome Auto Dark)에서 배경만 반전돼 글자가 안 보인 적이
  있다(운영 교훈 '26.8.7).
- **설정 파일 경로 환경변수(`REPORT_HARNESS_CONFIG`)** — 시험 실행·평가(A/B)가 운영 산출 폴더·규칙을 건드리지 않게
  설정을 떼어 놓는다. 없는 경로를 주면 설정 없이 실행 위치 기준 기본값으로 돈다.
- **자가진단에 하네스 버전** — 문의·버그 보고 때 먼저 묻는 값. 갱신은 버전으로 판단된다.
- 문서 — 웹앱 설치 문서의 모순 문단(`dist/` 추적 여부) 정정·릴리스 페이지 안내, 설치 문서에 경위 로그 복사·대체 표기
  처리·로컬 규칙 R9NN 번호, README 구조도(시드 계보·경위 로그), 개발자 문서에 릴리스 절차(플러그인 규약 태그).

## 0.5.1 (2026-09-25)

> 0.5.0을 배포 저장소에 올린 뒤 같은 날 고친 것. 0.5.0을 받은 설치자는 `claude plugin marketplace update
> report-harness` → `claude plugin update report-harness@report-harness` → 재시작으로 받는다(버전을 올려야 갱신이
> 도달한다 — 같은 버전이면 'already at the latest version'으로 끝나는 것을 격리 설치로 확인했다).

### 규칙이 설치자에게 제대로 도달하게
- **시드 계보(`references/rules-seed.lineage.json`)** — 배포한 시드 줄의 지문. `sync_rules.py`가 운영본 줄이
  손대지 않은 옛 시드 줄이면 새 줄로 바꾸고(시드에서 폐지된 번호면 지운다), 설치자가 고친 줄·쌓은 규칙은 번호가
  겹쳐도 보존하고 보고만 한다. 격리 설치로 0.4.0 설치자의 갱신을 재현하니, 계보 없이는 시드가 다시 쓴 옛 줄 7건을
  '번호 충돌'로 오판해 "로컬 규칙을 옮기라"고 잘못 안내했고 폐지된 R053이 살아 있었다. 옛 시드 판본 5개 모두
  `--apply` 한 번으로 현재 시드와 같아지고 자가진단 경고 0. 시드를 고치면 `scripts/build_seed_lineage.py`(테스트가 강제).
- **경위 로그·통합 마커 동기화** — 새 설치에 `rules-history.md`가 복사되지 않아 흡수된 번호 6건을 '죽은 참조'로
  경고하던 것, 옛 통합 마커(R068)에 머물러 설치자가 풀 수 없는 '통합 요구'가 뜨던 것을 고쳤다. R9NN 로컬 규칙은
  통합 증가분에서 뺀다(R901 하나로 +833이던 것).
- 번호만 같은 설치자 로컬 규칙은 대체 표기보다 먼저 판정해 덮지 않는다(코드 리뷰 #2). 설치자가 고친 줄에 시드가
  대체·정정 표기를 달면 줄을 통째로 바꾸지 않고 표기만 붙인다(코드 리뷰 #3 — 덧붙인 내용이 사라졌다).
- **새 규칙 번호(`sync_rules.py --next-id`)** — 즉석 승격 절차가 `R0NN`을 쓰라고 해 설치자 로컬 규칙이 시드 번호
  대역에 들어가, 다음 판의 같은 번호 시드 규칙이 충돌로 막혔다. 이제 설치자 환경은 R9NN, 저자 환경(운영 규칙이 시드를
  배포하는 저장소 안)은 결번까지 건너뛴 시드 대역 번호를 받는다.
- **배포 가드 버전 경고** — 배포 저장소와 플러그인 내용이 다른데 버전이 같으면 `package_check.sh`가 경고한다(작업
  트리 기준 — 버전을 올리고 커밋하기 전에 돌려도 맞게 본다).
- 계보 갱신 도구는 계보 파일이 깨졌으면 멈춘다(빈 계보로 넘겨 과거 지문을 잃지 않게).

### 검사 정밀도 — 운영 초안 14건 재점검
- **외부 인용(R092)** — 외부 주체(국가·국제기구·기관 약칭·자료명)와 근거어(연구·조사·통계·실적·% 등)가 함께 있을
  때만 외부 인용으로 본다. 내부 약호 `(T3)`·법령명·강조 괄호 「잠정」·내부 문서 참조·`※ 출처:` 줄까지 잡던 오탐
  12건을 걸렀다(14 → 2건, 남은 둘은 실제 출처 누락). 잇단 인용이 `;`로 함께 적은 출처 줄·`※ 주:` 단서 줄도 인정한다.
  한글 이름 + 괄호 원어·약칭(맥킨지(McKinsey)·진흥원(NIA))은 외부 주체로 보고, '방법·기법'으로 끝나는 자료명은 법령으로
  빼지 않는다(코드 리뷰 #3).
- 절 제목 어휘 검사는 풀 어휘에 붙은 번호·부제(`검토 결과 ① : …`)를 떼고 본다(19 → 15건).
- 종결어미 명사 목록에 운임·주임·상임·보관함 등 추가(명사 목록 전환 뒤 막히던 흔한 명사).
- compare — 글자 없는 표는 표 수와 따로 세어(`blank_tables`) 되읽기에 느는 빈 표는 넘기고 원본 빈 표(서명란)가
  빠지면 잡는다. 인용 면제는 기호뿐인 조각을 면제하지 않고, 인용 줄 + 본문 조각은 인용 부분만 뺀다.

### 웹앱·리뷰어
- **웹앱 변환 절차 정정** — 마지막 대조가 `--hwpx 출력.hwpx`로 앞 단계의 `결과.hwpx`와 달라 문서대로 하면 exit 2로
  끝났다. 배포 패키지 5종을 빈 폴더에 풀어 문서 절차 그대로 돌려 전 단계 통과를 확인했고, SKILL.md 변환 명령 블록을
  그대로 실행하는 테스트를 넣었다.
- 리뷰 서버 — 같은 출처를 Origin과 Host 머리로 가려 포트 포워딩(SSH·VS Code)으로 연 화면의 POST가 403이던 것을
  고쳤다. `/clear` 전 wait가 강제 종료돼 남은 임대는 새 세션이 넘겨받고, 살아 있으면 멈출 wait pid를 알린다.
  쪽 계산을 건너뛸 때 기존 경계선을 지우지 않고 다시 잰다.
- 자가진단 '설치 사본'은 저장소 `skills/` 전체를 본다(낡은 cross-verify 사본이 드러남).
- 변환 단계는 같은 세션에서 이미 읽은 `[draft][export]` 공통 규칙을 다시 읽지 않는다(규칙 문구 슬림화는 A/B 확인
  필요로 보류).

## 0.5.0 (2026-09-25)

> 0.4.0 이후 커밋 50여 개를 묶은 릴리스다. 운영 기록(교훈 330여 건·규칙 R094까지·작업폴더 17건)을 분석해
> 반복 손실을 결정론으로 막고, 개선이 팀 설치자에게 실제로 도달하게 배포 경로를 고쳤다.
> 계획서: `docs/superpowers/plans/2026-09-25-production-improvement.md`.

### 보고서를 세우는 법 — 설계 지침 (R094)
- **`references/report-craft.md` 신설** — 한 장 보고서·피라미드 원칙·A3·내러티브 메모 등 보고서 작성법을
  '무엇을 어떤 순서로 세우나'로 재구성. 게이트⓪ 끝 `00_context.md`의 `## 보고 설계`(보고 목적·결재자가 내릴
  결정·예상 결론·상황 전제), 게이트① 전 `10_outline.md`의 `## 설계 점검`(제목=결론·근거 구조·절별 So What·
  구성요소·상위 계획 대응), 초안 자가감사의 흔한 실수 점검표. 문장 규칙과 부딪히면 style-guide가 이긴다.
- **`check_craft.py`** — 위 칸이 있는지만 결정론으로 확인(내용 품질은 게이트에서 사람).
- **게이트⓪ 상황 전제 문항** — 이미 발주·계약했는지, 대응할 외부 지적 원문, 먼저 받을 상위 계획서를
  분석으로 불명확할 때만 묻는다(착수 뒤 드러나 초안을 통째로 버린 5건).
- **`report-writing` 스킬** — 같은 지침을 따로 부르는 입구("보고서 설계 점검해줘"). 웹앱판에도 지침 사본을 싣는다.

### 초안을 화면에서 본다 — 라이브 리뷰어·허브
- **`review_server.py`** — 게이트①·② 초안을 hwpx 양식에 가깝게(쪽 경계·예상 쪽수·장평) 그리고, 문단마다 코멘트·
  직접 수정을 받아 CLI(`wait`)로 넘긴다. 초안을 고치면 바뀐 항목만 다시 그린다(SSE, 스크롤·작성 중 메모 유지).
- 허브(여러 보고서 한 서버)·탐색 서랍·⌘K 빠른 이동·되돌리기, 처리 세션 임대·하네스 잠금(여러 보고서 동시 정리),
  서브에이전트 분담(`references/review-worker.md`).
- 창 폭에 맞춘 쪽 배치 — 서랍을 열면 폭과 관계없이 쪽을 밀고, 모자라면 메모 폭 → 쪽 배율 순으로 줄인다
  (종전 1640px 미만에서 서랍이 본문을 덮었다). `/clear` 뒤 같은 프로세스의 새 세션은 임대를 넘겨받는다.
- **다른 출처 요청 차단** — Host가 로컬 주소가 아니거나 Origin이 같은 로컬 출처가 아니면 403. 사용자가 연 다른
  웹페이지가 브라우저를 통해 코멘트(CLI가 사용자 지시로 처리)를 넣거나 초안을 고치거나 서버를 끄던 경로(CSRF),
  DNS 리바인딩으로 보고서를 읽어 가던 경로를 막는다.

### 도식·인용
- **표 도식(R089)** — 흐름·비교·체계·일정 도식을 그림 대신 한글 표로 조립(`diagram_table.py`). 한글에서 글자를
  고칠 수 있고 흐려지지 않으며 12~22mm 낮다. **배색 역할(R090)** — 대비 4.5:1·흑백 구분·강조 1곳.
- **도식 카드 서술형·내어쓰기(R093)** — 둘째 줄이 첫 줄 글자에 맞춰 시작(한글: 부호 뒤 탭 + 내어쓰기 자동 탭),
  카드 문장의 등호·숫자 나열은 `diagram-formula`·`diagram-fragment` 경고. 비교도 화살표는 라벨 높이에 맞춰 카드를 넓힌다.
- **외부 인용(R092)** — 본문 문구 + 시각화(인용 차트·재작도·원문 인용 블록·표) + `※ 자료:` 출처 줄. 출처 줄이 없는
  외부 인용 ※는 `source-line-missing` 경고. 분량을 이유로 시각화를 선택지에서 빼지 않는다.
- **쉬운 말·두괄식(R091)** — 국어기본법 제14조·「쉬운 공문서 쓰기 길잡이」를 `audit_style.py` 경고 6종으로.

### 변환 검증
- **문장 단위 대조·산출물 신선도(R085)**, 작업폴더 이력 분리(R086)·파생물 루트 제거(R087), 제목 박스 판정(R084),
  열 폭 하한 실폭 기준(R063).
- **compare 오탐 제거** — 되읽기의 볼드 재직렬화(`**`)·원문 제목과 같은 `# 제목`·곧은/굽은 따옴표·`📑 문서 구조`
  머리 블록·문단으로 되읽힌 1칸 상자를 잔재·손실로 세지 않는다(과거 판본 14건 재대조: 잔재 1,000→2건, 남은 것은
  실제 불일치). 대신 `compare --hwpx`가 인도본 XML 글자의 `**`·`==`·백틱·`~~`를 직접 센다(`literal-markup`).
- 짧은 라벨 열('구 분')이 하한 축소로 한 글자씩 쪼개지던 결함 정비.
- 글자 없는 배치 표(웹앱 되읽기의 머리말 배너)는 표 수에서 뺀다 — 웹앱 compare도 `issues: []`가 정상이다.
- **실변환 검증** — 운영 초안 사본을 kordoc MCP로 끝까지 변환(generate → 도식 표 치환 → postprocess → 되읽기 →
  `compare --hwpx`)해 확인했다. 같은 인도본에서 수정 전 compare는 잔재 30건·개수 불일치·누락을 냈고 수정 후 0건,
  도식 표 6개·카드 항목 36개의 내어쓰기 인코딩도 전부 맞았다. 원문 인용 블록 글자는 `literal-markup`에서 뺀다.

### 배포·운영
- **규칙 시드 동기화(`sync_rules.py`)** — 종전에는 설치자 `rules.md`가 첫 실행 시드에 고정돼 새 규칙이 도달하지
  않았다. 이제 시드에만 있는 규칙을 운영본 끝에 덧붙이고(설치자 규칙은 보존), 대체 표기가 붙은 규칙은 그 줄을
  바꾼다. 자가진단에 '규칙 동기화' 항목.
- **초안 린트 훅을 플러그인에 편입(`hooks/lint_draft_hook.py`)** — `20_draft.md`를 쓰거나 고치면 린트·감사 위반을
  block. 파생 md(되읽기 기록 등)는 대상이 아니다.
- **교훈 스키마** — `kind`(content·defect·feature·preference)·`resolved_by`. 자가진단은 미해결 결함·기능만
  센다(종전 fix 키워드 판정은 끝난 조치까지 세어 '미조치 43건'으로 부풀었다).
- **배포 5종** — claude·chatgpt·gemini에 codex·antigravity 추가, `dist/` 추적(결정론 빌드).
- **CI 복구** — 워크플로가 테스트 러너 pytest까지 설치하지 않아 8월부터 모든 push에서 실패하던 것을 고쳤다
  (런타임은 여전히 표준 라이브러리만).
- **자가진단** — '설치 사본'(저장소 체크아웃에서 ~/.claude/skills 사본과 대조), '규칙 동기화', 교훈 스키마 기반
  '미조치 lesson'. `check_craft.py analysis`로 분석 문서 네 절(논지 후보·총괄표·근거 공백·인용 재료) 확인.
- 배포 가드 — 기관 양식 원본(경영관리 프레임워크)·디자인 시안을 무시·금지 목록에 추가.
- 스타일 감사 도입(R074), 산식 부호 하이픈 환원, 발표자료 저장소 분리.

### 운영 교훈 정리
- 자가진단에 남아 있던 옛 교훈 22건을 판정해 반영: 문체 감사의 명사 종결 오탐('포함·위임·책임·결함')과 질의서·요청
  문서 어휘, 게이트⓪ `(권장)`을 요청 동사에서 정하기, 근거 공백을 '조사로 메울 항목'과 '범위 판단 항목'으로 나누기,
  조사 도구 대체 경로(WebFetch 실패 시 curl 재시도·법령 본문 조회 실패 시 법제처 DRF 오픈API), 도식 카드에 기술 사양
  나열 금지(R093 보강), 성과 지표는 결과 단계로(보고서 설계 지침).
- 코드 리뷰(high)로 오늘 변경분의 결함 10건을 찾아 정비 — 종결어미 예외 범위, 인용 면제 방향, 명세 오류 시 감사
  중단, 출처 줄 탐색 범위, 규칙 동기화의 대체·번호 충돌·점검 시 쓰기, 임대 인계 조건, 쪽수 계산, 비교도 화살표.

## 0.4.0 (2026-08-09)

> 0.3.1 이후 흩어져 있던 'Unreleased' 절 5개를 통합한 릴리스다. 릴리스된 적 없는 변경은
> 전부 여기 담기며, 아래 소절은 작업일 내림차순이다.

### 2026-08-05 ~ 08-09 — 웹앱 배포판·안전망·검증 체계

- **웹앱 배포판 `webapp/kca-report-hwpx` 수록** — MCP 없이 자립하는 ③초안+④변환 스킬.
  md2hwpx(동결 헤더 + section0 직접 조립)·assert_postprocess(침묵실패 차단)·roundtrip_md
  (python-hwpx→stdlib 폴백) 신규 3종 + 하네스 스크립트 4종 무수정 복사.
  `scripts/build_webapp_skill.py`가 3플랫폼(claude `.skill`·chatgpt/gemini `.zip`) 패키지를
  빌드하며 기준 9종(룰 최신성·범위 필터·드리프트 0·dangling 0·사장 자산 0·출처 고정·서드파티
  고지·SKILL.md 단일·PII 0)을 전부 통과해야 산출한다. `dist/`는 gitignore — 배포는 재빌드로.
- **`hooks/verify_hwpx_hook.py` 수록** — hwpx 생성 직후 구조 검증을 강제하는 PostToolUse
  훅('26.8.7 검증 우회 산출 사고 재발 방지, 모델 협조 없이 걸리는 결정론 안전망).
- **`/report-doctor` 자가진단** — 환경·의존·규칙 체계 7항목 점검(`doctor.py`).
- **CI 신설(`.github/workflows/test.yml`)** — fresh clone에서 pytest·배포 가드·웹앱 빌드를
  매 push/PR 검증. 테스트의 `report/_harness` 하드 의존을 시드 폴백·skipif로 제거해
  '로컬에서만 초록'을 해소.
- **시드 완전 동기화 + 본문 비교 가드** — rules-seed가 운영본과 본문까지 일치(근거 등급 누락
  59건·중복 헤더·통합 마커 부재 해소). `test_seed_carries_every_operational_rule`을 ID 비교 →
  본문 비교로 강화. R050 결번 기록을 `references/rules-history.md`(배포 시드)로 승격.
- **R018 발신 줄 12pt를 `--all` 기본값으로 승격** — `--sender-size 12` 문자열이 문서 9곳에
  복제되고 누락 시 규칙 위반본이 나가던 구조를 `SENDER_SIZE_PT` 상수 + 프로파일 대조
  테스트로 교체. `--sender-size PT`는 재정의 전용.
- **전수 팩트체크·이미지 차용 기준을 `references/factcheck.md`로 승격** — 배포되지 않는
  설계문서 §7-1·§7-2를 가리키던 dangling 앵커 5곳 해소. webapp SKILL 게이트 수 자기모순
  (description 2회 vs 본문 3회)을 '최대 3회'로 통일.
- **PII 가드 관할 확장** — 웹앱 빌드에 PII 검사 필수 편입(위반 시 rules.md 원복),
  package_check 금지 목록에 `report/` 추가, 스캔 범위를 webapp·hooks·scripts·docs로 확장.
- **문서 4종 신설**(developing·install-harness·install-webapp·licenses) + README 서사 재작성.

### 2026-08-04 — 시드 동기화 부채·룰 번호 사고·실무 개선본 성문화
- **배포 시드 동기화 부채 해소 + 룰 번호 사고 수습**. `report/`를 gitignore한 뒤(R048) 운영
  `rules.md`가 저장소에서 빠지면서, 코드(린트)는 반영됐는데 **룰 텍스트는 R043까지만 배포**되는
  상태가 됐다 — 신규 설치자가 R044~R059(16종)를 못 받는다. rules-seed.md에 전량 승계.
  아울러 승격 시 기존 번호를 확인하지 않아 **R049·R050이 서로 다른 두 규칙에 중복 부여**된
  사고를 발견해, 이번 세션 후반 추가분 9종을 R051~R059로 재번호하고 참조처(SKILL.md·lint 주석·
  md-profile·테스트·CHANGELOG) 전부 정합. 재발 방지 가드 2종 추가 —
  `test_rules_seed_has_no_duplicate_ids`(번호 중복)·`test_rules_seed_ids_are_contiguous`
  (R001부터 빈틈없이 이어지는지 = 시드 동기화 실패 신호). 테스트 170 → 172.
- **실무 개선본 대조 반영 (R051~R056 + 린트 4종)**. 사용자가 하네스 산출 초안을 실제 보고용으로
  개선한 2건(ICT기금 AI 어드바이저·자격검정 시험지 AI 자동검수 PoC)을 원본과 정량 대조해
  집필 관례를 성문화. 대조 결과: 각주 8→4·7→0, ㅇ 18→10(cert-poc 44% 감축), 2음절 괄호리드
  6건 전량 교체, `(상세 붙임N 참조)` 6→0회.
  - **R051 괄호 리드 구체화** — 2음절 추상어(`품 질`·`배 포`) 금지, 3~5음절 구체 명사구
    (`서비스 품질`·`접근성 보장`). style-guide §2 벌려쓰기 관례는 **표 헤더 전용**으로 한정
    (개선본도 `구 분`·`시 기`는 유지). 린트 `lead-too-short`.
  - **R052 각주 없는 언어** — 각주가 필요한 기술용어는 각주를 달지 말고 용어를 교체
    (`holistic 단일콜(11-enum)`→`문항당 1회 호출`). 코드 식별자 본문 사용 금지.
    린트 `footnote-overflow`(5개째 검출).
  - **R053 본문 자기완결** — 괄호로 붙임에 설명을 미루지 않는다. 절 맺음의 `※ 세부내용은 붙임
    참조`는 합법 예외. 린트 `annex-crossref`.
  - **R054 결과보고 5절 표준** — 추진 배경 → 추진 내용 → **추진 성과(실측 정량치 전용)** →
    운영 전환 → 향후 계획. 측정값이 없으면 성과 절을 세우지 않는다.
  - **R055 향후계획은 실행 액션** — 주체·시점·이미 일어난 근거사실을 함께
    (`외부용역 전환 추진 중(업체 미팅 '26.7.1 완료)`).
  - **R056 표 캡션 무번호** — `[ 표1. X ]`가 아니라 `[ 월별 추진 실적 ]`처럼 측정 축·조건을
    서술. 린트 `caption-numbered`. 린트 룰 8종 → **12종**, 테스트 162 → 170.
- **SKILL.md 조사 계약 보강** — 구축·운영 과제의 필수 수집 항목(운영 실측·구축 실적·실험 비교
  데이터)을 ①단계에 명시. **화면 캡처는 하네스가 촬영하지 않고 사용자에게 요청**하되 캡션
  후보를 번호와 함께 제안한다(코드 분석으로 화면 구성을 알고 있으므로 제목은 하네스가 뽑는 것이
  정확). 게이트⓪에 실측 확보 여부·캡처 제공 여부 확인 2건 추가.
- **R057~R059 서술 문형 성문화** — 개선본의 문장 운용을 추가 반영.
  **R057 연결어 반복 금지**(한 절 3회 초과) — 개선본 10종 vs 하네스 산출본 3종(`하고`·`도록`만
  반복)이던 단조로움 해소. 후보군을 관계별로 제시(병렬 `하고/하며/함께`, 목적 `하고자/도록`,
  근거 `되어/발생해/인 만큼`, 배제 `없이/만으로는`, 첨가 `도/까지`) — 다양성이 목적이 아니라
  **관계를 정확히 표시**하는 것이 목적. **R058 대시는 ㅇ의 근거 부연** — ㅇ가 판단·조치를
  진술하면 대시는 그 판단이 선 근거를 댄다. ㅇ의 명사구를 풀어 쓰기만 한 대시·수치를 되풀이하는
  대시 금지, 근거가 없으면 대시를 달지 않는다. **R059 향후 계획도 주어 생략** — style-guide §4
  주어 생략 관례를 전 절에 일관 적용, 주체는 방향·대상으로 녹인다(`이음5G사업팀으로 운영 이관`).
  개선본의 주어 명시 사례는 채택하지 않음(사용자 확정).

- **table-pool 부품 3종 등재** — 3-A 과제개요 표, 3-B 화면 캡처 표(사용자 제공 전제),
  3-C 진화 이력 표(버린 안과 채택한 안을 같은 축으로 비교).

### 2026-07-30 — R045~R047
- **R045: 상위 계층 하나에 딸리는 하위 계층은 최대 2개**. ㅇ 아래 대시 4개를 나란히 다는 밀도는
  코퍼스에 없으며 계층이 목록으로 전락한다(사용자 확정). 핵심은 **개수를 쳐내는 것이 아니라
  문장을 합치는 것** — 하위가 3개 이상이면 성격이 가까운 항목끼리 연결어(~하고·~하며·~와 함께)로
  통합 서술하고, 안 합쳐지면 상위 ㅇ를 둘로 쪼개 각자 2개 이하를 갖게 한다. 상세를 잃지 않아야
  하면 대시를 늘리지 말고 표로 배출(style-guide §3 배출구·R044 ②와 같은 층위).
  `lint_md_profile.py` `bullet-overflow` 임계 **6 → 3**(3개째부터 검출), md-profile §1-1 서술·
  §3 룰표 개정. 테스트 161 → 162(2개 허용 케이스 신설, 기존 2건 상한 기준 갱신).
- 함께 확정된 집필 규칙 2종(rules.md 전용 — 스크립트 변경 없음):
  **R046** 본문 날짜는 `'yy.m월` 월 단위 표기(연월일 풀 표기 금지, 발신 줄·핵심 일자는 예외,
  kordoc `DATE_2DIGIT_YR` 경고는 기관 관례에 반하므로 무시). **R047** 본문 수치는 팩트체크
  통과분만 쓰되 과잉정밀·단발측정치를 구간화(단발 curl `0.59초` → `1초 이내`) — 경량 팩트체크는
  근거 유무만 보므로 과잉정밀은 집필 단계 판단 사항.

### 2026-07-29 — R043 패키지 정합
- **R043: hwpx 패키지 정합 — 내부망 자료교환 반입 거부 해소**. kordoc 산출물은 hwpx 최소
  패키지(version.xml 부재·디렉터리 엔트리 3개·전량 STORED)라서 mimetype+container.xml만으로는
  EPUB류 일반 OCF와 지문이 같아, 심층 구조 검사를 하는 반입 시스템이 hwpx로 판별하지 못하고
  octet-stream → 미등록 확장자로 반려했다(타 hwpx는 동일 시스템 정상 유통 — 산출물 문제로 확정,
  한컴 정품 '도식 Pool.hwpx' 엔트리 전수 대조 실측). postprocess `canonicalize_package`가 플래그
  무관 상시 적용: version.xml·settings.xml(hpf manifest 등재)·META-INF/manifest.xml·container.rdf
  보강(정품 실측 템플릿), 디렉터리 엔트리 제거, container.xml rootfiles 정본화, 엔트리 순서·압축
  프로파일 정품 일치, 멱등. `validate_hwpx.py structural`에 OCF 시그니처(mimetype 첫 엔트리·
  STORED·offset 0·extra 0·38바이트째 평문)·필수 멤버·디렉터리 엔트리 검사 추가(반입 거부 유형
  5종 회귀 테스트). exit 1 의미 정정: 스타일 대상 0건(패키지 정합만 적용됐을 수 있음).
- **R043 보강: 한글 재저장본 Scripts 스텁 제거** — 실반려 파일 실측으로 제2 반려 경로 확정.
  한글 재저장 시 삽입되는 기본 빈 JScript(`Scripts/headerScripts`·`sourceScripts`)는 확장자
  없는 멤버 + hpf `application/x-javascript` 등재 활성콘텐츠라서 압축 내부까지 검사하는 반입
  엔진이 '등록되지 않은 확장자'로 반려한다(실반려 오류 문구 'header script' = 이 멤버명).
  canonicalize가 Scripts/ 전량 제거 + hpf item/itemref 등재 철회 동반 수행(스텁은 기능 0).
  테스트 152 → 161

### 2026-07-29 — 배포 정리 (구 'Unreleased' 표류분, 0.2.0 아래에 있던 절)
- 제거: `inject_images.py` — SKILL.md·commands·references·tests 어디서도 호출되지 않는 고아
  스크립트(참조 0곳, 131줄 전량 미실행). repo·installed 양쪽 삭제. 이미지 주입이 필요해지면
  `/tmp/_hwpx` 무조건 rmtree(:66-67,:144)와 재실행 시 zip 엔트리 중복(:124-130)을 고친 뒤
  파이프라인에 배선해 재도입한다 (이력: `git show 9312f63:skills/report-pipeline/scripts/inject_images.py`)
- 배포 제외 확대: `form/` 전체를 추적 해제(12.6MB, 배포 페이로드의 94.9%) — 런타임 참조 0곳이고
  양식 자산은 `assets/` 번들 사본(양식 hwp md5 동일·도식Pool 경량본)으로 충족. `.gitignore`·
  README §5·`package_check.sh` 가드 동기화. 로컬 원본은 유지, 과거 추적분은 git 이력에 보존
- **축적룰 전량 승계: `rules-seed.md` R001~R006 → R001~R042**. 그간 승격분 36종(R007~R042)이
  운영 `state_dir/rules.md`에만 있어 신규 설치자는 6종만 받던 격차를 해소 — 실제 운영 기준으로
  배포. 이후 승격은 운영 파일에 먼저 반영하고 배포 전 시드로 동기화한다
- 문서 정정: README `state_dir` 기본값 `~/.claude/report-harness-state/` → 실제값
  `{cwd}/.report-harness` (`harness_config.py:23`)

### 2026-07-28 — R038~R042
- R038: ※·＊ 단서/각주 뒤 ㅇ 복귀 전환 6pt 스페이서 추가 (postprocess TRANSITIONS cham→yo·star→yo)
- R039: 본문 괄호 13pt(R033)를 문단 전체 텍스트 기준 run 분할로 개정 — 문장 안 볼드로 run이 쪼개진 괄호도 처리(볼드 보존), cross_run_skipped 32→0
- R040: `==문구==` 노란 음영 하이라이트 도입 — md-profile §1-3 문법·lint `highlight-unpaired`(8종째)·postprocess `apply_highlight`(shadeColor=#FFFF00+볼드, 260331 실무본 실측)·compare `==` 잔존 검출
- R041: 머리말 배너(R030) 앵커 문단 lineSpacing 100% 강제 + subList textWidth 본문 폭 보정 — 도너 150%가 본문을 ≈4.6mm 밀던 제목표 상단 여백 실원인 정정, 기주입 문서 소급 수리(멱등)
- R042: 표 총 폭은 본문 폭 '미만' — 여유 283 hu(1.0mm) 확보. R036('이내')의 계열 정정으로, slack 0 축소가 같은 문단의 선행 요소 때문에 표를 다음 줄로 밀어 표 위에 15pt 빈 줄을 만들던 결함(사용자 6회 보고분의 정체)을 해소. `apply_fit_page_width`가 본문 폭 − 283으로 비례 축소하고, 그림 축소 시 scaMatrix e1/e5·rotationInfo centerX/Y 파생 캐시를 재계산(위생 동반 수정)
- 동기화 부채 해소: installed↔repo (`inject_images.py`·`assets/kca-header-banner/` repo 누락분 복사)
- 테스트 118 → 142
- README 재구성(설명 친절화): ①개요(목적·활용 시나리오·구조도 3종)→②주요기능→③의존성(번들/별도설치
  구분·URL 명시)→④설치 순으로 재편. 사전 요구사항·설치 확인·문제 해결 표를 신설해 처음 쓰는
  사람이 막히는 지점을 메웠다. 구조도는 한글 폭 때문에 ASCII 박스가 깨지므로 Mermaid로 교체
  (mermaid-cli 실렌더 검증), 폴더 트리는 경로부를 ASCII 플레이스홀더로 통일해 주석 컬럼 정렬 고정

## 0.3.1 (2026-07-29)
- **MIT 라이선스 명문화**: 루트 `LICENSE` 신설 + `plugin.json` `license: "MIT"` + README 절 추가.
  공개 저장소에 라이선스가 없어 법적으로 all rights reserved이던 상태를 해소(포크·수정·재배포
  권리 부여). 번들 서드파티 `skills/humanizer/`(MIT © DaleSeo)는 원본 고지를 유지하고
  루트 LICENSE에 별도 명시. 회귀 가드 `test_license_present_and_declared` 추가

## 0.3.0 (2026-07-29)
- **MCP 번들**: `.mcp.json` 신설 — `kordoc`(필수, 키 불필요)·`korean-law`(선택) 2종이 플러그인
  설치 시 함께 붙는다. 종전에는 필수 의존성인 kordoc을 사용자가 문서만 보고 직접 설치해야 했다.
  `korean-law`의 법제처 인증키는 `${LAW_OC}` 환경변수 참조로만 두고 실값은 커밋하지 않는다
  (회귀 가드 `test_mcp_bundle_valid`가 실값 유출을 차단)
- README 전면 개편: "30초 시작 → 무엇을 해주나 → 사용법 → **스킬 구조** → 준비물 → 설정"
  순으로 재구성. 설정을 §3→§6으로 내리고 '없어도 동작'을 앞세웠으며, 배포 제외·패키징 가드·
  테스트는 개발자용 부록으로 분리. 개인 절대경로가 박혀 있던 설정 예시를 일반화
- 의존성 정리: `opendart`(공시·재무, 기관보고서와 접점 희박)·`deep-research`(제3자 마켓플레이스
  플러그인) 문서에서 제외. `insane-search`는 의존성 표에서 '있으면 좋은 것'으로 강등.
  tool-playbook·report-research SKILL.md 동반 정리 — 스킬 동작은 불변(도구 부재는 원래 대체 흡수)

## 0.2.0 (2026-07-29)
- **배포 가능 상태 도달**: `.claude-plugin/marketplace.json` 신설 — 저장소가 플러그인이자
  마켓플레이스를 겸한다(루트 `source: "./"`, superpowers·vercel·firecrawl과 동일 패턴).
  `/plugin marketplace add kca-deep/report-harness` → `/plugin install report-harness@report-harness`
- plugin.json: 0.1.0 → 0.2.0, homepage·repository·keywords 추가(29커밋째 버전 고정 해소)
- 회귀 가드: `test_marketplace_manifest_valid`·`test_marketplace_version_matches_plugin` —
  두 매니페스트 버전이 어긋나면 설치본과 카탈로그가 불일치하므로 테스트로 고정
- R036 원문 정정: `apply_fit_page_width`의 '--all 포함' → '플래그와 무관하게 항상 실행'
  (코드 실측). 배포 시드와 운영 `state_dir/rules.md` 양쪽 동시 반영해 42종 동기화 유지

## 이력 백필 — R007~R037 (2026-07-22 ~ 07-28 승격분)

> 0.1.0 이후 승격된 규칙 31종의 변경 이력이 비어 있던 것을 사후 보전한 항목이다. 규칙 본문 정본은
> `skills/report-pipeline/references/rules-seed.md`이며 아래 요약은 그 원문을 압축한 것이다.
> 괄호 안은 해당 규칙이 승격된 커밋. 승격 당시 번호가 저장소 파일에 기록되지 않고 운영
> `state_dir/rules.md`에만 있던 규칙은 `번호 기록:` 으로 저장소 최초 기록 시점을 병기했다.

### 2026-07-22 — 실전 1건 시각 피드백 (`8be9607`·`7d88335`)
- R007: kordoc generate 입력은 리터럴 기호가 아니라 리스트 깊이 문법(`-`, 2칸, 4칸 = □/ㅇ/-)으로 변환해 전달 — 계층 평탄화 방지 (`8be9607`)
- R008: `generate_document` 호출 시 KCA 프로파일 파라미터 필수 — fonts(heading=HY헤드라인M·body=휴먼명조·ref=맑은고딕·table=맑은 고딕)·sizes(대15·참13·표12·제목20)·`bullet2=ᄋ`·body_title_box·line_spacing160 (`8be9607`, bullet2 실 enum 값 정정 `ef66021`)
- R009: 붙임은 3열 배너 표(`| 붙임 1 | | 제목 |`)로 작성하고 변환까지 유지 (`8be9607`)
- R010: 제목은 h1(`# `)로 변환 입력에 전달하고, 폰트 검증은 선언이 아니라 문단별 charPr '사용'을 확인 (`7d88335`)
- R011: ＊ 각주 문단은 생성 후 참고 charPr로 zip 패치 — kordoc은 ※만 인식 (`7d88335`)

### 2026-07-22 — postprocess 후처리 도입 (`7751904`~`d97591f`)
- R012: 발신 줄(`< 날짜, 부서 >`)은 `<right>` 래핑으로 우측 정렬 (`7751904`)
- R013: 계층 간격은 `postprocess_hwpx.py --spacing`이 양식 실측값(발신→□ 8pt·□→ㅇ 6pt·ㅇ→ㅇ 6pt·대시→ㅇ 6pt·ㅇ→- 6pt·-→＊ 3pt·블록 15pt·＊→표 10pt)으로 재현 (`7751904`; ㅇ→＊ 3pt 유추 `907fa8e`, ㅇ→ㅇ·대시→ㅇ 6pt 사용자 확정 `49d5e64`)
- R014: 간격 검증은 스페이서 개수가 아니라 **실효 간격**(스페이서+paraPr 여백 합산 pt) 기준 — `effective_gaps`가 리포트하고 `apply_zero_margins`가 콘텐츠 paraPr prev/next를 0화해 스페이서 단독 체계를 유지 (`3a5c2d8`, 번호 기록: `e32e05b`)
- R015: 표 정렬 규칙 도입 — 최초 형태는 '표 캡션·표 래퍼 문단 가운데 정렬'(`83ecab2`). '26.7.28 사용자 확정으로 **본문 콘텐츠 표 우측 정렬 + 캡션은 표 caption 내장(→R034)**으로 정정되고 구 규칙은 폐기, `apply_table_alignment`가 배정(제목 박스 CENTER·배너 표 좌측은 예외) (정정·번호 기록: `9312f63`)
- R016: ㅇ 괄호 리드는 볼드(`**(한 계)**`) — 코퍼스 관례 (`7e4fc17`)
- R017: 제목 박스 표는 무테두리·배경 보존 — 원본 borderFill 복제 변형으로 테두리만 NONE, 그라데이션 유지 (박제 `7e4fc17`, 구현 `0a53cbc`, 그라데이션 보존 재수정 `a45451f`, 번호 기록: `e32e05b`)
- R018: 발신 줄 글자 크기 12pt — 양식 바이너리 실측 확정 (박제 `7e4fc17`, 구현 `0a53cbc`, 번호 기록: `9312f63`)
- R019: 계층 시작은 원래 폰트의 리터럴 띄어쓰기(□0·ㅇ1·대시3·＊※5칸), 줄바꿈 하위는 자동 들여쓰기 — 내어쓰기 폭은 그 폰트 **글자 단위**로 산정(□ 1.5자·ㅇ 2자·대시 2.5자·＊※ 4자), `left=0`·`intent=-글자폭`. 표 셀 텍스트 CENTER, 표-문단 간격 캡션→표 3pt·표→※ 3pt·문단→캡션 6pt (박제 `7e4fc17` → 띄어쓰기 계층 전환 `a73bf51`·`8f39c81`(`--star-indent` 훅 폐기) → 내어쓰기 복원 `e77b213` → 글자 단위 병기 `019dd57` → `left=0`·`intent=-hang` 인코딩 정정 `da691b6`, 번호 기록: `e32e05b`)
- R020: 편집용지 여백 양식 강제 — 좌우 20·위 10·아래 15·머리말 15·꼬리말 10mm(`PAGE_MARGINS`), kordoc preset 위 15mm 결함 후처리 (`d97591f`)
- R021: compare `markdown-leftover` 중 볼드(`**`)·h1 제목·대시 선행공백 트림은 파서의 정상 재현 — hwpx XML 실물 검증(리터럴 기호 0건·charPr 실사용 확인)으로 무해 판정 후 진행 (**대응 커밋 없음** — 코드·문서 변경을 수반하지 않는 QA 판정 규칙, 번호 기록: `e32e05b`)

### 2026-07-24 — 20260724건 사용자 확정 (`3cf1611`~`848a9f6`)
- R022: 제목표 상단여백 미세 제거는 앵커 문단 줄간격 100% 치환 + 표 outMargin top 0. **상단 얇은 행(3.8pt)은 양식 원형의 배경 밴드이므로 행 삭제 금지** — 삭제 시 그라데이션 소실 회귀 (`3cf1611`, 재구현·회귀 복구 `2ad9e02`). 이후 조사에서 잔존 여백의 실체는 머리말 부재(→R030)·폭 초과(→R036)·slack 0(→R042)으로 계열 정정됨
- R023: 표 캡션·표 셀 글자는 12pt — 폰트는 유지하고 높이만 치환, 제목 박스 셀 제외 (`3cf1611`)
- R024: □ 절 제목은 항상 볼드 — `apply_dae_bold`가 dae 문단 run charPr에 bold 변형 배정 (`3cf1611`)
- R025: ☞ 등 결론 유도 기호는 ＊·※와 동일 계층 처리 — 선두 5칸 리터럴 띄어쓰기 + `left=0`·`intent=-4글자폭`, 인접 간격 3pt 준용 (R019 확장) (`3cf1611`)
- R026: 조사 기반 보고서는 본문=두괄식 결정·요지, 붙임=「검토 근거 상세」(2p 이내, R009 배너 표)로 분담 — style-guide §8 분담 원칙의 draft 단계 강제 (`2ad9e02`)
- R027: 붙임·참고 배너(3열 표)는 양식 참고 블록 정합 — 셀 글자 HY헤드라인M 16pt·앵커 문단 `pageBreakBefore=1`로 별도 페이지 시작·R023 12pt 강제 대상에서 제외 (`beaed03`). 셀 스타일 OLE 실측 증보(`0a6dc2b`) 후 **재실측 정정**(`848a9f6`): 라벨 셀 4변 SOLID 0.5mm #1B1760+채움 #2B2D63(남색)+흰 글자·행 28.3pt — HWP COLORREF는 `0x00BBGGRR`이라 hex 직독 시 R/B가 반전되며 최초 기록(적갈색)이 이 오류였음
- R028: 붙임은 각 1장 이내로 설계 — 계층형 문구는 렌더 기준 2줄 이내(휴먼명조 15pt ≈ 75자), 넘치는 상세는 표 전환·항목 분리·삭제로 압축 (`848a9f6`, 번호 기록: `e32e05b`)
- R029: ㅇ 항목이 (괄호) 리드로 시작하면 하위 대시는 (괄호) 리드를 쓰지 않는다 — 리드 중복 계층 방지 (`848a9f6`, 번호 기록: `e32e05b`)

### 2026-07-28 확정 — 제목표 상단여백 3·4차 조사 + 서식 정밀화 (일괄 커밋 `9312f63`)
- R030: 제목표 상단여백의 **확정 원인은 머리말(hp:header) 부재** — 편집용지 위 10mm+머리말 15mm=25mm가 통째로 빈 흰 띠로 남던 것. `apply_header_banner`(`--header-banner`·`--all`)가 실무본 이식 자산(`assets/kca-header-banner/`)을 id 재배정·manifest 등록과 함께 주입하고 hp:header 기존재 시 스킵(멱등). 편집용지 여백 축소로 대응 금지(양식 규격 위반). kordoc reflow 렌더는 머리말·hp:caption·pageBreakBefore를 그리지 않으므로 렌더 스크린샷으로 판정 금지
- R031: 본문 서술 중 용어 뒤 ＊ 표지는 **위첨자** — charPr에 `<hh:supscript/>`를 추가한 복제본으로 run 분리, 하단 `＊ 용어 : 설명` 각주 문단은 평문 유지(R011 참고 charPr 13pt). `apply_superscript_star`
- R032: 본문 계층 문단(□·ㅇ·대시)은 **양쪽 정렬(JUSTIFY)** — ＊·※ 각주·표 캡션·발신 줄은 기존 정렬 유지. `apply_body_justify`
- R033: 본문 서술 중 `(…)` 괄호 구간은 **13pt**(본문 15pt 대비 축소) — ㅇ 선두 괄호 리드(R016 라벨)는 라벨이므로 15pt 볼드 유지, 표 셀·＊※ 각주는 비대상. `apply_paren_small`
- R034: 표 캡션(`[ … ]`)은 표 바깥 문단이 아니라 `hp:tbl` 안 **`hp:caption`(side=TOP)에 내장** — 캡션 문단 CENTER+볼드, 캡션↔표 스페이서 빈 문단은 제거하고 전환 간격은 X→표로 승계(＊→표 10pt, ㅇ/대시/※→표 6pt). `apply_caption_embed`
- R035: 붙임·참고 배너 표(R027 3열)는 **좌측 정렬**(앵커 문단 LEFT) — R015 표 우측 정렬의 명시 예외
- R036: **표 총 폭(표 폭 + outMargin 좌우)은 본문 폭을 넘지 않는다** — 초과 시 표 폭·셀 폭·내부 그림을 같은 비율로 축소(`apply_fit_page_width`). 근거는 KCA 실보고서 12건 전수 실측에서 '머리말 배너 표 폭 == 본문 폭'이 예외 없이 성립한 것. 좌우 15mm 문서(260331)에서 이식한 배너가 좌우 20mm 문서에서 배너 10mm·제목박스 1mm 초과를 낳았고, 이것이 R022·R030 조사에서 못 찾은 상단 여백의 실제 원인이었다(여백 속성이 아니라 폭 초과)
- R037: 붙임·참고 배너 표의 **제목 셀(3번째) 문단은 양쪽 정렬** — 라벨·스페이서 셀은 CENTER 유지. `apply_annex_banner` ⑤가 배정하고 `apply_center_cell_text`는 배너 제목 셀을 제외(R023 12pt의 배너 셀 제외와 같은 패턴)

## 0.1.0 (2026-07-22)
- 최초 릴리스: report-pipeline(4단계 조합형 오케스트레이터)·report-research(산출 계약 조사)·humanizer(번들, MIT DaleSeo) 스킬 3종 + 슬래시 커맨드 4종
- 결정론 툴체인: harness_config / lint_md_profile(룰 7종) / prep_report_md(모호 입력 거부·삭제 회계) / validate_hwpx(structural·compare·numbers) / check_image_size / extract_format_profile + pii_scan 패키징 가드
- KCA 자산: style-guide(실보고서 34건 코퍼스)·format-profile 시드·도식 Pool 판정표·표 부품 카탈로그 26유형·rules 시드 R001~R006
- 검증: pytest 66개 + 설정 0 통합 스모크 8/8 (hwpx 1회 변환·왕복 QA 통과)
